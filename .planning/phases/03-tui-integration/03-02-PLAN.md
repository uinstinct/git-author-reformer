---
phase: 03-tui-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main.rs
  - src/tui/mod.rs
  - src/tui/app.rs
  - src/tui/event.rs
  - src/tui/render.rs
  - src/error.rs
autonomous: false
requirements:
  - CORE-01
must_haves:
  truths:
    - "Running the binary in a git repo enters a ratatui alternate screen and shows a two-option main menu"
    - "Arrow keys / j-k move the selection; Enter selects; q or Esc exits"
    - "SIGTERM during the event loop calls ratatui::restore() before exit — terminal is left clean"
    - "Panic during event handling does not leave the terminal in raw mode (ratatui::init() panic hook fires)"
    - "Selecting either menu option transitions to a placeholder Screen variant (NotImplemented for now) — Wave 3+ replaces the placeholders"
  artifacts:
    - path: "src/tui/app.rs"
      provides: "App struct + Screen enum with MainMenu + placeholder variants"
      contains: "pub enum Screen"
    - path: "src/tui/event.rs"
      provides: "handle_key dispatcher matched on Screen"
      contains: "pub fn handle_key"
    - path: "src/tui/render.rs"
      provides: "render(frame, app) dispatcher + render_main_menu"
      contains: "pub fn render"
    - path: "src/main.rs"
      provides: "SIGTERM-aware ratatui shell — init, run, restore on all paths"
      contains: "signal_hook::flag::register"
  key_links:
    - from: "src/main.rs"
      to: "signal_hook::flag::register"
      via: "SIGTERM flag registered BEFORE ratatui::init()"
      pattern: "signal_hook::flag::register"
    - from: "src/main.rs"
      to: "ratatui::restore"
      via: "called on every exit path, including error path"
      pattern: "ratatui::restore"
    - from: "src/tui/mod.rs"
      to: "src/tui/app.rs, event.rs, render.rs"
      via: "submodule declarations + pub fn run event loop"
      pattern: "pub mod app"
---

<objective>
Build the TUI shell: SIGTERM-aware ratatui init/restore wrapper in main.rs, central App state machine with a Screen enum, a centralized event dispatcher, and a render dispatcher. The only screen implemented in this wave is the two-option MainMenu (CORE-01). Selecting an option moves to a `Screen::NotImplemented(&'static str)` placeholder that subsequent waves replace.

Purpose: ROADMAP constraint — `ratatui::init()` + SIGTERM handler must be the first code written, before any app logic. This plan establishes that foundation. CORE-01 (two-option main menu with keyboard navigation) is the first user-observable behavior. Every subsequent wave adds Screen variants, key match arms, and render functions to this shell.

Output: Crate builds and runs; binary launched in a git repo shows the main menu; arrow keys navigate; Enter transitions to a "not yet implemented" placeholder; q/Esc/Ctrl-C exits cleanly; SIGTERM restores the terminal before exit.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-tui-integration/03-RESEARCH.md
@.planning/phases/03-tui-integration/03-PATTERNS.md
@.planning/phases/03-tui-integration/03-01-PLAN.md
@src/main.rs
@src/lib.rs
@src/error.rs
@src/tui/mod.rs
@Cargo.toml

<interfaces>
<!-- Contracts this plan establishes for Wave 3+ to extend -->

From src/error.rs (existing; this plan ADDS one variant):
```rust
pub enum AppError {
    NotARepo(String),
    StashDetected,
    WorktreesDetected(String),
    Git(#[from] git2::Error),
    NonUtf8Message(git2::Oid),
    // ADDED in this plan:
    Io(#[from] std::io::Error),  // ratatui terminal.draw() returns io::Error
}
```

App + Screen surface this plan defines (Wave 3+ extends Screen with more variants):
```rust
// src/tui/app.rs
pub struct App {
    pub repo: git2::Repository,
    pub screen: Screen,
    pub should_exit: bool,
}

pub enum Screen {
    MainMenu { selected: usize },                 // CORE-01
    NotImplemented(&'static str),                 // placeholder for Wave 3+
}

pub enum MenuChoice { Rename, Drop }
```

handle_key signature (Wave 3+ adds match arms):
```rust
// src/tui/event.rs
pub fn handle_key(app: &mut App, key: crossterm::event::KeyCode);
```

render signature (Wave 3+ adds dispatch cases):
```rust
// src/tui/render.rs
pub fn render(frame: &mut ratatui::Frame, app: &App);
```

tui::run signature (already declared empty in Plan 03-01; this plan implements):
```rust
// src/tui/mod.rs
pub fn run(repo: git2::Repository) -> Result<(), crate::error::AppError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Io variant to AppError + replace main.rs with SIGTERM-aware ratatui shell</name>
  <files>src/error.rs, src/main.rs</files>
  <read_first>
    - src/error.rs (full file — existing AppError variants)
    - src/main.rs (full file — existing run/main shell that must be preserved structurally)
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Pattern 1: ratatui Init + SIGTERM + Panic Hook", lines 207–260)
    - .planning/phases/03-tui-integration/03-PATTERNS.md (§"src/main.rs (modified, entry point)", lines 130–161)
  </read_first>
  <action>
    Modify src/error.rs: append a new variant to the `AppError` enum, using the same `thiserror` derive pattern as existing variants:
      #[error("Terminal I/O error: {0}")]
      Io(#[from] std::io::Error),
    This converts `ratatui::DefaultTerminal::draw` and `crossterm::event::poll/read` io::Error values into AppError via `?`. No other AppError changes.

    Replace src/main.rs entirely (the existing 22-line file). Preserve the run() → main() split and the eprintln+exit(1) pattern from the existing code. The new file does:

      use clap::Parser;
      use git_author_reformer::{error, git, tui};
      use std::sync::Arc;
      use std::sync::atomic::AtomicBool;

      #[derive(Parser)]
      #[command(name = "git-author-reformer", version)]
      struct Cli {}

      fn run() -> Result&lt;(), error::AppError&gt; {
          // 1. SIGTERM flag — registered BEFORE ratatui::init() (RESEARCH §Pattern 1).
          let term_flag = Arc::new(AtomicBool::new(false));
          signal_hook::flag::register(signal_hook::consts::SIGTERM, Arc::clone(&term_flag))?;

          // 2. Pre-flight (existing Phase 1 gates).
          let repo = git::open_repo()?;
          git::preflight::check_stash(&repo)?;
          git::preflight::check_worktrees(&repo)?;

          // 3. ratatui::init() installs panic hook + raw mode + alternate screen.
          let mut terminal = ratatui::init();

          // 4. Run the TUI; capture result so we can ALWAYS call restore().
          let result = tui::run_with_terminal(&mut terminal, repo, term_flag);

          // 5. Restore on EVERY exit path — happy and error.
          ratatui::restore();

          // 6. Propagate after restore so terminal is clean before printing.
          result
      }

      fn main() {
          let _cli = Cli::parse();
          if let Err(e) = run() {
              eprintln!("error: {e}");
              std::process::exit(1);
          }
      }

    `signal_hook::flag::register` returns `io::Result<SigId>`; the `?` operator with the new `AppError::Io(#[from] std::io::Error)` variant converts it. Discard the `SigId` (the handler is process-lifetime).

    `tui::run_with_terminal` is the NEW signature this plan introduces — it takes an already-initialized terminal plus the term_flag. The empty stub `pub fn run` from Plan 03-01 is REPLACED in Task 2 below. Why this split: keeping `ratatui::init()` and `ratatui::restore()` in main.rs (not in tui::run) makes the restore-on-all-paths invariant locally checkable in one file.
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build2.txt; test ! -s /tmp/build2.txt &amp;&amp; grep -c 'Io(#\[from\] std::io::Error)' src/error.rs &amp;&amp; grep -c 'signal_hook::flag::register' src/main.rs &amp;&amp; grep -c 'ratatui::init()' src/main.rs &amp;&amp; grep -c 'ratatui::restore()' src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 with no warnings
    - src/error.rs contains `Io(#[from] std::io::Error)` variant
    - src/main.rs contains exactly one `signal_hook::flag::register` call, and it appears at an EARLIER line number than the first `ratatui::init()` call — verify: `awk '/signal_hook::flag::register/{s=NR} /ratatui::init/{i=NR} END{exit !(s &amp;&amp; i &amp;&amp; s &lt; i)}' src/main.rs`
    - src/main.rs contains exactly one `ratatui::restore()` call, and it appears AFTER the call to `tui::run_with_terminal` — verify analogously
    - src/main.rs preserves the `run() -> Result<(), error::AppError>` + `fn main()` split with `eprintln!("error: {e}"); std::process::exit(1)` on the error branch
  </acceptance_criteria>
  <done>main.rs is the SIGTERM-aware ratatui shell; AppError handles io::Error; no TUI logic in main.rs beyond init/restore — that lives in src/tui/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement App state machine + Screen enum + handle_key (with unit tests for state transitions)</name>
  <files>src/tui/app.rs, src/tui/event.rs, src/tui/mod.rs</files>
  <read_first>
    - src/tui/mod.rs (the empty stub from Plan 03-01)
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Pattern 2: App State Machine", lines 263–303; §"Pattern 1" event loop, lines 239–257)
    - src/git/scan.rs (already exists from Plan 03-01 — Wave 3+ will call it; not used here yet)
  </read_first>
  <behavior>
    Unit tests for handle_key (state transitions are pure mutations and TDD-able per RESEARCH §"TDD Strategy" point 2):

    - test_main_menu_down_increments_selected_mod_2: Screen::MainMenu { selected: 0 } + KeyCode::Down → selected: 1. Then again → 0 (wraps). Message: "CORE-01: keyboard navigation must cycle the two options."
    - test_main_menu_up_decrements_with_wrap: selected: 0 + Up → 1; selected: 1 + Up → 0.
    - test_main_menu_j_k_same_as_down_up: 'j' behaves like Down, 'k' behaves like Up (vim binding affordance — explicit per CORE-01 "responds to keyboard navigation"; if you decide j/k are out of scope, drop the test AND don't implement them. Default: include both.)
    - test_main_menu_enter_with_selected_0_transitions_to_rename_placeholder: Screen::MainMenu { selected: 0 } + Enter → Screen::NotImplemented("rename") (or however you tag the rename branch). Message: "CORE-01 + RENAME-01: selecting Rename advances state."
    - test_main_menu_enter_with_selected_1_transitions_to_drop_placeholder: Screen::MainMenu { selected: 1 } + Enter → Screen::NotImplemented("drop").
    - test_main_menu_q_sets_should_exit: 'q' → app.should_exit = true.
    - test_main_menu_esc_sets_should_exit: Esc → app.should_exit = true.
    - test_not_implemented_esc_returns_to_main_menu: Screen::NotImplemented(_) + Esc → Screen::MainMenu { selected: 0 }.

    Tests live in `#[cfg(test)] mod tests { ... }` inside src/tui/app.rs (or src/tui/event.rs — pick one location, recommendation: event.rs since it tests handle_key). The App struct must be constructible in tests without a real git2::Repository — implement a test constructor `#[cfg(test)] pub fn new_for_test() -> App` that creates an in-memory bare repo via `git2::Repository::init_bare(tempfile::TempDir::new()?.path())`, OR refactor so handle_key takes `&mut Screen` and `&mut bool should_exit` rather than `&mut App` so tests don't need a repo at all. Recommendation: refactor handle_key to take `&mut App` BUT add a `#[cfg(test)] fn make_test_app() -> App` helper using a temp bare repo (matches the test pattern from tests/common/mod.rs). Add `tempfile` to dev-dependencies if not already there (check Cargo.toml — it already is, line 13).
  </behavior>
  <action>
    Implement src/tui/app.rs:

      pub struct App {
          pub repo: git2::Repository,
          pub screen: Screen,
          pub should_exit: bool,
      }

      pub enum Screen {
          MainMenu { selected: usize },
          NotImplemented(&'static str),
          // Wave 3+ adds: AuthorList, CoAuthorList, RenameForm, Preview, Executing, Success, Err
      }

      pub enum MenuChoice { Rename, Drop }
      impl MenuChoice {
          pub fn from_index(i: usize) -> Self { if i == 0 { Self::Rename } else { Self::Drop } }
          pub fn label(self) -> &'static str { match self { Self::Rename => "Rename an author", Self::Drop => "Drop a co-author" } }
          pub fn all() -> [Self; 2] { [Self::Rename, Self::Drop] }
      }

      impl App {
          pub fn new(repo: git2::Repository) -> Self {
              Self { repo, screen: Screen::MainMenu { selected: 0 }, should_exit: false }
          }
      }

    Implement src/tui/event.rs:

      use crate::tui::app::{App, Screen};
      use crossterm::event::KeyCode;

      pub fn handle_key(app: &mut App, key: KeyCode) {
          match &mut app.screen {
              Screen::MainMenu { selected } => match key {
                  KeyCode::Down | KeyCode::Char('j') => *selected = (*selected + 1) % 2,
                  KeyCode::Up   | KeyCode::Char('k') => *selected = (*selected + 1) % 2, // 2-item wrap — both directions equivalent
                  KeyCode::Enter => {
                      let choice = if *selected == 0 { "rename" } else { "drop" };
                      app.screen = Screen::NotImplemented(choice);
                  }
                  KeyCode::Char('q') | KeyCode::Esc => app.should_exit = true,
                  _ => {}
              },
              Screen::NotImplemented(_) => match key {
                  KeyCode::Esc | KeyCode::Char('q') => app.screen = Screen::MainMenu { selected: 0 },
                  _ => {}
              },
          }
      }

      #[cfg(test)]
      mod tests {
          use super::*;
          use tempfile::TempDir;

          fn make_test_app() -> (TempDir, App) {
              let dir = TempDir::new().unwrap();
              let repo = git2::Repository::init_bare(dir.path()).unwrap();
              (dir, App::new(repo))
          }
          // ... per-behavior tests from the <behavior> block
      }

    Modify src/tui/mod.rs: replace the Plan 03-01 stub with:

      pub mod app;
      pub mod event;
      pub mod render;

      use std::sync::Arc;
      use std::sync::atomic::{AtomicBool, Ordering};
      use std::time::Duration;
      use crossterm::event::{Event, KeyEventKind};
      use ratatui::DefaultTerminal;

      pub fn run_with_terminal(
          terminal: &mut DefaultTerminal,
          repo: git2::Repository,
          term_flag: Arc&lt;AtomicBool&gt;,
      ) -> Result&lt;(), crate::error::AppError&gt; {
          let mut app = app::App::new(repo);
          loop {
              if term_flag.load(Ordering::Relaxed) { break; }
              terminal.draw(|f| render::render(f, &app))?;
              if crossterm::event::poll(Duration::from_millis(16))? {
                  if let Event::Key(key) = crossterm::event::read()? {
                      if key.kind == KeyEventKind::Press {
                          event::handle_key(&mut app, key.code);
                      }
                  }
              }
              if app.should_exit { break; }
          }
          Ok(())
      }

      // Keep the original `pub fn run` removed — main.rs now calls run_with_terminal directly.

    Filter `KeyEventKind::Press` only — Pitfall 4 (double-fire on Windows).
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build3.txt; test ! -s /tmp/build3.txt &amp;&amp; cargo test --lib tui 2>&amp;1 | tee /tmp/tui_unit.txt; grep -E "test result: ok\\." /tmp/tui_unit.txt &amp;&amp; grep -c 'KeyEventKind::Press' src/tui/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 with no warnings
    - `cargo test --lib tui` reports `test result: ok.` with at least 7 passing tests (one per <behavior> bullet)
    - src/tui/mod.rs contains `KeyEventKind::Press` filter (grep ≥ 1)
    - src/tui/mod.rs's event loop uses `crossterm::event::poll(Duration::from_millis(16))` (grep -c 'poll(Duration' ≥ 1)
    - src/tui/app.rs defines `pub enum Screen` with at least `MainMenu` and `NotImplemented` variants
    - src/tui/event.rs `handle_key` matches on `Screen::MainMenu` AND `Screen::NotImplemented`
  </acceptance_criteria>
  <done>App + event dispatcher built and unit-tested for state transitions; ratatui event loop assembled in tui::run_with_terminal.</done>
</task>

<task type="auto">
  <name>Task 3: Implement render dispatcher + render_main_menu + NotImplemented screen</name>
  <files>src/tui/render.rs</files>
  <read_first>
    - src/tui/app.rs (Screen enum from Task 2)
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Code Examples" — List widget lines 527–541, Layout lines 545–555)
  </read_first>
  <action>
    Create src/tui/render.rs with:

      use crate::tui::app::{App, MenuChoice, Screen};
      use ratatui::Frame;
      use ratatui::layout::{Constraint, Layout, Rect};
      use ratatui::style::{Modifier, Style};
      use ratatui::widgets::{Block, List, ListItem, ListState, Paragraph};

      pub fn render(frame: &mut Frame, app: &App) {
          match &app.screen {
              Screen::MainMenu { selected } =&gt; render_main_menu(frame, frame.area(), *selected),
              Screen::NotImplemented(tag) =&gt; render_not_implemented(frame, frame.area(), tag),
          }
      }

      fn render_main_menu(frame: &mut Frame, area: Rect, selected: usize) {
          let [header, body, footer] = Layout::vertical([
              Constraint::Length(2),
              Constraint::Fill(1),
              Constraint::Length(1),
          ]).areas(area);

          frame.render_widget(Paragraph::new("git-author-reformer"), header);

          let items: Vec&lt;ListItem&gt; = MenuChoice::all().iter()
              .map(|c| ListItem::new(c.label()))
              .collect();
          let list = List::new(items)
              .block(Block::bordered().title("Main Menu"))
              .highlight_style(Style::default().add_modifier(Modifier::REVERSED))
              .highlight_symbol("&gt; ");
          let mut state = ListState::default();
          state.select(Some(selected));
          frame.render_stateful_widget(list, body, &mut state);

          frame.render_widget(
              Paragraph::new("↑/↓ or j/k: move   Enter: select   q/Esc: quit"),
              footer,
          );
      }

      fn render_not_implemented(frame: &mut Frame, area: Rect, tag: &str) {
          let msg = format!("'{tag}' flow not implemented yet — press Esc/q to return");
          frame.render_widget(Paragraph::new(msg).block(Block::bordered().title("TODO")), area);
      }

    Use `frame.area()` (ratatui 0.30 API) not the deprecated `frame.size()`. The MenuChoice labels are sourced from the enum's `label()` method — DO NOT hardcode the strings here a second time (single source of truth lives in app.rs).
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build4.txt; test ! -s /tmp/build4.txt &amp;&amp; cargo test 2>&amp;1 | grep -E "test result: ok\\." &amp;&amp; grep -c 'MenuChoice::all' src/tui/render.rs &amp;&amp; grep -c 'frame.area()' src/tui/render.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` and full `cargo test` pass
    - src/tui/render.rs uses `MenuChoice::all()` (no hardcoded "Rename an author" / "Drop a co-author" strings — single source of truth in app.rs)
    - src/tui/render.rs uses `frame.area()` (not the deprecated `frame.size()`)
    - src/tui/render.rs implements `render`, `render_main_menu`, `render_not_implemented` matching the dispatcher contract
    - Footer line shows keybindings (q/Esc/Enter) so the help is discoverable
  </acceptance_criteria>
  <done>render.rs renders both implemented screens; dispatcher pattern established for Wave 3+ to extend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human smoke-test — launch the binary and verify main menu + SIGTERM</name>
  <what-built>
    A working TUI shell. Running `cargo run` inside a git repository should:
    - Enter the ratatui alternate screen
    - Display "git-author-reformer" header, a bordered "Main Menu" listing two options with the first highlighted, and a footer with keybindings
    - Respond to ↑/↓ and j/k to move the highlight
    - Transition to the "TODO" placeholder screen on Enter; return to main menu on Esc
    - Exit cleanly on q or Esc from the main menu — terminal returns to normal prompt
    - On SIGTERM (`kill -TERM &lt;pid&gt;`), exit and leave the terminal in normal mode (not stuck in raw mode)
  </what-built>
  <how-to-verify>
    1. From the project root, run `cargo run` inside this repo (which is itself a git repo — Phase 1 preflight will pass).
    2. Verify the screen shows: top line "git-author-reformer"; a bordered "Main Menu" block with "&gt; Rename an author" and "Drop a co-author"; a bottom line showing the keybindings.
    3. Press ↓ once — selection should move to "Drop a co-author".
    4. Press ↑ once — selection returns to "Rename an author".
    5. Press 'j' — selection moves to "Drop a co-author".
    6. Press Enter — screen changes to the "TODO" panel with message about 'drop' not being implemented.
    7. Press Esc — return to main menu.
    8. Press 'q' — process exits cleanly; your shell prompt appears normally with no cursor weirdness or echo issues.
    9. SIGTERM check: in one terminal run `cargo run`, in another find the PID with `pgrep git-author-reformer`, then run `kill -TERM &lt;pid&gt;`. The first terminal should exit cleanly with the shell prompt rendered normally. (Optional but recommended — documents that the SIGTERM handler actually fires.)
  </how-to-verify>
  <read_first>
    - src/main.rs (the new shell)
    - src/tui/mod.rs (the event loop)
    - src/tui/render.rs (what's drawn)
  </read_first>
  <acceptance_criteria>
    - User confirms the binary launched without errors
    - User confirms keyboard navigation works (↑↓, j/k, Enter, Esc, q)
    - User confirms terminal is left in a clean state after every exit path tested
    - If SIGTERM check was performed: user confirms terminal was not stuck in raw mode
  </acceptance_criteria>
  <resume-signal>Type "approved" after verifying, or describe any UI/cleanup issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal state ↔ process exit | If the process exits without calling ratatui::restore(), the user's shell is corrupted |
| OS signal (SIGTERM) → process | SIGTERM bypasses panic hooks; explicit handler required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of service | Terminal raw mode leak | mitigate | signal_hook::flag::register(SIGTERM) registered BEFORE ratatui::init(); ratatui::restore() called on EVERY exit path in main.rs::run (happy and error); ratatui::init() also installs panic hook that calls restore before panic message prints. Verified by acceptance_criteria grep + manual SIGTERM smoke test. |
| T-03-03 | Tampering | KeyEventKind double-fire on Windows | mitigate | Filter `if key.kind == KeyEventKind::Press` in tui::run_with_terminal — Pitfall 4. |
| T-03-04 | Denial of service | Hostile crossterm event flooding | accept | Event loop runs synchronously; no unbounded buffering. crossterm itself is trusted (Task 1 supply-chain checkpoint already covered in Plan 03-01). |
</threat_model>

<verification>
- `cargo build` passes with no warnings
- `cargo test` passes including new tui unit tests
- Manual smoke test (Task 4) confirms CORE-01 behavior
- src/main.rs SIGTERM register precedes ratatui::init() (line-order grep)
- src/main.rs ratatui::restore() follows tui::run_with_terminal call (line-order grep)
</verification>

<success_criteria>
1. CORE-01 satisfied: two-option main menu visible on launch, keyboard navigation responsive
2. SIGTERM during the event loop exits cleanly with terminal restored
3. Panic safety: ratatui::init()'s panic hook ensures restore on panic
4. App / Screen / handle_key / render scaffolding ready for Wave 3 to extend with AuthorList, RenameForm, Preview, etc.
</success_criteria>

<output>
Create `.planning/phases/03-tui-integration/03-02-SUMMARY.md` summarizing the App + Screen + event + render contracts that Wave 3+ extends.
</output>
