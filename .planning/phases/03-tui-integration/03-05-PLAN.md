---
phase: 03-tui-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/tui/app.rs
  - src/tui/event.rs
  - src/tui/render.rs
autonomous: false
requirements:
  - RENAME-05
  - DROP-04
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - OUT-01
must_haves:
  truths:
    - "On entering Preview, scan_rename or scan_drop is called exactly once and the result is stored in the Screen variant"
    - "Preview screen displays the exact affected commit count from RewritePreview.affected_count (RENAME-05, DROP-04)"
    - "Preview screen displays non-blocking warnings when signed_commit_count > 0 (SAFE-03), annotated_tags_affected non-empty (SAFE-04), or has_notes_ref (SAFE-05)"
    - "User can still proceed past warnings — they are advisory, not blocking"
    - "Y or Enter on Preview triggers rewrite_author/drop_coauthor; N/Esc returns to MainMenu"
    - "On Y/Enter at Preview, rewrite_author or drop_coauthor runs synchronously and Success screen displays count + force-push reminder using detected remote name (OUT-01)"
    - "Errors during rewrite route to Screen::Err with the error message; Esc/q from Err returns to MainMenu"
    - "scan and rewrite are called ONLY in handle_key (state transitions), never in render"
  artifacts:
    - path: "src/tui/app.rs"
      provides: "Final Screen variants: Preview { op, scan }, Success { rewritten, remote_name }, Err(String)"
      contains: "Screen::Success"
    - path: "src/tui/event.rs"
      provides: "scan-on-transition into Preview; rewrite execution on Y/Enter; Success/Err routing"
      contains: "scan_rename"
    - path: "src/tui/render.rs"
      provides: "render_preview (with warnings), render_executing, render_success, render_err"
      contains: "render_preview"
  key_links:
    - from: "src/tui/event.rs"
      to: "git::scan::scan_rename / scan_drop"
      via: "called on Form Enter (rename) or CoAuthorList Enter (drop) — once, transition-time"
      pattern: "scan_rename|scan_drop"
    - from: "src/tui/event.rs"
      to: "git::rewrite::rewrite_author / drop_coauthor"
      via: "called on Preview Y/Enter — blocks the event loop briefly"
      pattern: "rewrite_author|drop_coauthor"
    - from: "src/tui/render.rs"
      to: "RewritePreview.remote_name"
      via: "force-push reminder in Success screen substitutes &lt;remote&gt; placeholder when None"
      pattern: "force-with-lease"
---

<objective>
Close the loop: scan on transition into Preview, render warnings + confirmation count, execute the rewrite on user confirmation, show success with force-push reminder, route errors. This plan completes Phase 3 — every requirement (CORE-01, RENAME-01, RENAME-02, RENAME-05, DROP-01, DROP-04, SAFE-03, SAFE-04, SAFE-05, OUT-01) is satisfied end-to-end after this lands.

Purpose: Plans 03-01 through 03-04 built the data layer (scan.rs) and the UI skeleton (main menu + author list + rename form + coauthor list + preview placeholder). This plan replaces the placeholder with the real preview that calls scan, displays warnings, runs the rewrite, and shows the result.

Output: A user can launch the binary, pick rename or drop, navigate the list, fill the form (for rename), see the affected count + warnings, confirm, watch the rewrite execute, and see a success screen with the exact `git push --force-with-lease --all` command using their actual remote name.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tui-integration/03-RESEARCH.md
@.planning/phases/03-tui-integration/03-PATTERNS.md
@.planning/phases/03-tui-integration/03-01-PLAN.md
@.planning/phases/03-tui-integration/03-04-PLAN.md
@src/git/scan.rs
@src/git/rewrite.rs
@src/tui/app.rs
@src/tui/event.rs
@src/tui/render.rs

<interfaces>
<!-- Final shape of Screen + PendingOp after this plan -->

From src/git/scan.rs (Plan 03-01):
```rust
pub struct RewritePreview {
    pub affected_count: usize,
    pub signed_commit_count: usize,
    pub annotated_tags_affected: Vec<String>,
    pub has_notes_ref: bool,
    pub remote_name: Option<String>,
}
pub fn scan_rename(repo, old_name, old_email) -> Result<RewritePreview, AppError>;
pub fn scan_drop(repo, target_email) -> Result<RewritePreview, AppError>;
```

From src/git/rewrite.rs (Phase 2):
```rust
pub fn rewrite_author(repo, old_name, old_email, new_name, new_email) -> Result<usize, AppError>;
pub fn drop_coauthor(repo, target_email) -> Result<usize, AppError>;
```

Final Screen enum after this plan:
```rust
pub enum Screen {
    MainMenu { selected: usize },
    AuthorList { items, filter, matched, nucleo, selected },
    CoAuthorList { items, filter, matched, nucleo, selected },
    RenameForm { source, draft },
    Preview { op: PendingOp, scan: RewritePreview },    // CHANGED from Plan 03-03's placeholder
    Success { rewritten: usize, remote_name: Option<String> },  // NEW
    Err(String),                                          // NEW (replaces NotImplemented entirely — deleted in Task 1)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape Screen::Preview to carry RewritePreview + add Success/Err variants + delete NotImplemented</name>
  <files>src/tui/app.rs</files>
  <read_first>
    - src/tui/app.rs (existing Screen from Plan 03-04 — Preview currently is `Preview(PendingOp)`)
    - src/git/scan.rs (RewritePreview definition + derives)
  </read_first>
  <behavior>
    Unit tests in app.rs:
    - test_screen_preview_holds_op_and_scan: construct Screen::Preview { op: PendingOp::Rename { ... }, scan: RewritePreview { affected_count: 5, ... } } and verify field access. (Trivial test, just proves the variant compiles with the new shape.)
    - test_screen_err_holds_message: Screen::Err("boom".into()) — verify a message-bearing variant exists.
    - test_screen_success_remote_name_optional: Screen::Success { rewritten: 3, remote_name: None } compiles; same with Some("origin").
  </behavior>
  <action>
    Modify src/tui/app.rs:

    1. Change `Screen::Preview(PendingOp)` to `Screen::Preview { op: PendingOp, scan: crate::git::scan::RewritePreview }`. All call sites in event.rs and render.rs from Plans 03-03 and 03-04 currently destructure as `Screen::Preview(op)` or `Screen::Preview(_)` — those are this plan's Tasks 2 and 3 to update. The compile error is the planned signal.

    2. Add two new variants:
         Success { rewritten: usize, remote_name: Option&lt;String&gt; },
         Err(String),

    3. Delete `Screen::NotImplemented(&'static str)`. By the end of Plan 03-05 Task 2 the only remaining callers (the `Err(_) =&gt; app.screen = Screen::NotImplemented("error")` placeholders in event.rs from Plans 03-03 and 03-04) are rewired to `Screen::Err(e.to_string())`. The variant has no callers after that rewire. Drop it now to keep the Screen enum minimal (Karpathy Rule 2). Note: this will introduce additional compile errors in event.rs/render.rs at the NotImplemented arms — Tasks 2 and 3 must remove those arms.

    Strategy A (synchronous rewrite, no intermediate Executing screen) is committed for v1 — see Task 2's behavior block. Do NOT add a `Screen::Executing` variant; if a future version wants a deferred-execution UI, that's a separate change.

    4. RewritePreview needs `Clone` (already derives Clone + Debug per Plan 03-01 spec — verify and add if missing). PendingOp needs Clone too (already added in Plan 03-03/04).

    5. Update the test module to use the new Preview shape in any test that constructed Screen::Preview.
  </action>
  <verify>
    <automated>cargo check --quiet 2>&amp;1 | tee /tmp/check1.txt; grep -E "expected.*found" /tmp/check1.txt &gt;/dev/null &amp;&amp; echo "EXPECTED: pattern errors in event.rs/render.rs from Preview shape change" || echo "WARNING: no expected compile errors — Task 2/3 may have nothing to fix"; grep -c 'Screen::Success' src/tui/app.rs &amp;&amp; grep -c 'Screen::Err' src/tui/app.rs &amp;&amp; grep -c 'Screen::Preview { op' src/tui/app.rs &amp;&amp; (! grep -q 'Screen::Executing\|Screen::NotImplemented' src/tui/app.rs)</automated>
  </verify>
  <acceptance_criteria>
    - src/tui/app.rs contains `Screen::Preview { op: PendingOp, scan: crate::git::scan::RewritePreview }` (struct-form variant, not tuple)
    - src/tui/app.rs contains `Screen::Success { rewritten: usize, remote_name: Option<String> }`
    - src/tui/app.rs contains `Screen::Err(String)`
    - src/tui/app.rs does NOT contain `Screen::Executing` or `Screen::NotImplemented` (deleted — Strategy A commits synchronously; NotImplemented has no callers)
    - `cargo check` reports compile errors ONLY in src/tui/event.rs and src/tui/render.rs (the destructuring sites for the old Preview shape) — this is expected and Tasks 2/3 resolve them
    - app.rs's own `#[cfg(test)] mod tests` block compiles (i.e., any Screen::Preview constructions in tests are already updated to the new shape)
  </acceptance_criteria>
  <done>Type layer reshaped; deliberate compile errors in event.rs/render.rs await Tasks 2 and 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Call scan on transition into Preview + run rewrite on Y/Enter + route Success/Err + remove NotImplemented call sites</name>
  <files>src/tui/event.rs</files>
  <read_first>
    - src/tui/event.rs (current handle_key — Preview placeholder transitions need updating)
    - src/git/scan.rs (scan_rename, scan_drop signatures)
    - src/git/rewrite.rs (rewrite_author, drop_coauthor signatures)
  </read_first>
  <behavior>
    Unit tests:
    - test_rename_form_enter_calls_scan_and_transitions_to_preview_with_data: complete RenameForm + Enter → handle_key calls scan_rename with the source identity → Screen::Preview { op: Rename {...}, scan: RewritePreview { affected_count: &gt;= 1, .. } }. Use the make_test_app_with_commits helper from Plan 03-03 so enumerate_authors AND scan_rename both have real data.
    - test_coauthor_list_enter_calls_scan_drop_and_transitions_to_preview_with_data: same shape for drop.
    - test_scan_error_routes_to_err_screen: trigger an error path (e.g., poison the repo by removing HEAD — or use a less destructive path: empty bare repo where revwalk produces zero items but no error; revisit. Realistic path: cannot easily trigger from a test. SKIP this test with a `#[ignore]` and a TODO if no clean trigger exists. Acceptable.).
    - test_preview_y_transitions_directly_to_success: handle_key('y') on Preview runs the rewrite inline (Strategy A — synchronous) and transitions DIRECTLY to Screen::Success in the same handler call. There is no intermediate Executing state in v1. Rationale: rewrite_author / drop_coauthor for typical repo sizes (&lt;1000 commits) complete in well under a second; the freeze is imperceptible. If a future version needs an "Executing..." progress screen for very large repos, that's a separate change with its own state-machine work.
    - test_preview_y_calls_rewrite_author_for_rename_op: complete fixture, Preview holding PendingOp::Rename, handle_key('y') → Screen::Success { rewritten: &gt;= 1, remote_name: detected }. Verify the post-condition by reading authors from the repo and confirming the old identity is gone (mirror tests/rewrite_test.rs pattern).
    - test_preview_y_calls_drop_coauthor_for_drop_op: same but for drop.
    - test_preview_n_returns_to_main_menu: 'n' or Esc → MainMenu.
    - test_preview_other_keys_ignored: arbitrary other keys do nothing.
    - test_rewrite_error_routes_to_err_screen: harder to trigger reliably; mark `#[ignore]` with a TODO if no clean fixture exists, OR construct an in-memory bare repo with no commits so rewrite trivially returns 0 with no error (in which case this test moves to verifying Success on zero count). Use judgment.
  </behavior>
  <action>
    Modify src/tui/event.rs. Two places change:

    1. Where Plan 03-03's RenameForm Enter previously set `app.screen = Screen::Preview(op)`: now call scan_rename and build the new Preview shape:

         KeyCode::Enter =&gt; {
             if draft.is_complete() {
                 let old_name = source_clone.name.clone();
                 let old_email = source_clone.email.clone();
                 match crate::git::scan::scan_rename(&app.repo, &old_name, &old_email) {
                     Ok(scan) =&gt; {
                         let op = PendingOp::Rename {
                             source: source_clone,
                             new_name: draft.new_name.trim().to_string(),
                             new_email: draft.new_email.trim().to_string(),
                         };
                         app.screen = Screen::Preview { op, scan };
                     }
                     Err(e) =&gt; app.screen = Screen::Err(e.to_string()),
                 }
             }
         }

    2. Where Plan 03-04's CoAuthorList Enter previously set `app.screen = Screen::Preview(PendingOp::Drop { target })`:

         KeyCode::Enter =&gt; {
             if let Some(target) = matched.get(*selected).cloned() {
                 match crate::git::scan::scan_drop(&app.repo, &target.email) {
                     Ok(scan) =&gt; {
                         let op = PendingOp::Drop { target };
                         app.screen = Screen::Preview { op, scan };
                     }
                     Err(e) =&gt; app.screen = Screen::Err(e.to_string()),
                 }
             }
         }

    3. Replace the existing Screen::Preview arm (which currently routes any key to MainMenu via the catch-all) with the real confirmation logic:

         Screen::Preview { op, scan } =&gt; match key {
             KeyCode::Char('y') | KeyCode::Char('Y') | KeyCode::Enter =&gt; {
                 let remote_name = scan.remote_name.clone();
                 let result = match op {
                     PendingOp::Rename { source, new_name, new_email } =&gt;
                         crate::git::rewrite::rewrite_author(&app.repo, &source.name, &source.email, new_name, new_email),
                     PendingOp::Drop { target } =&gt;
                         crate::git::rewrite::drop_coauthor(&app.repo, &target.email),
                 };
                 match result {
                     Ok(rewritten) =&gt; app.screen = Screen::Success { rewritten, remote_name },
                     Err(e) =&gt; app.screen = Screen::Err(e.to_string()),
                 }
             }
             KeyCode::Char('n') | KeyCode::Char('N') | KeyCode::Esc =&gt; app.screen = Screen::MainMenu { selected: 0 },
             _ =&gt; {}
         }

    4. Add a Screen::Success and Screen::Err arm:

         Screen::Success { .. } | Screen::Err(_) =&gt; match key {
             KeyCode::Esc | KeyCode::Char('q') | KeyCode::Enter =&gt; app.should_exit = true,  // Success → exit; Err → exit too
             _ =&gt; {}
         }

       Why Success exits rather than returns to MainMenu: the rewrite has happened; staying in the TUI invites confusion ("can I do another?"). v1 keeps it simple — exit after success. If you prefer "any key returns to MainMenu", that's also valid; document your choice and update the test accordingly. Recommendation: exit on any key. The user can re-run the binary to do another operation.

    Borrow-checker note: the `Screen::Preview { op, scan }` destructure borrows app.screen mutably. To reassign `app.screen` inside that arm, you need to clone `op` (PendingOp must derive Clone — already added in Plan 03-03). Pattern: `let op_copy = op.clone(); let remote_name = scan.remote_name.clone();` then drop the original borrow before reassigning. Adapt the snippet above to compile.

    Additionally, rewire all `Err(_) =&gt; app.screen = Screen::NotImplemented("error")` arms in handle_key (left behind as TODOs by Plans 03-03 and 03-04 on the MainMenu Enter branches calling enumerate_authors and enumerate_coauthors) to `Err(e) =&gt; app.screen = Screen::Err(e.to_string())`. After this rewire there must be ZERO references to `Screen::NotImplemented` anywhere in event.rs — verified by negative grep in the acceptance criteria below.

    Verify ALL existing tests still pass (rename/drop flows up to Preview should now produce REAL preview data, not the placeholder).
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build11.txt; test ! -s /tmp/build11.txt &amp;&amp; cargo test --lib tui 2>&amp;1 | grep -E "test result: ok\\." &amp;&amp; grep -c 'scan_rename' src/tui/event.rs &amp;&amp; grep -c 'scan_drop' src/tui/event.rs &amp;&amp; grep -c 'rewrite_author' src/tui/event.rs &amp;&amp; grep -c 'drop_coauthor' src/tui/event.rs &amp;&amp; grep -c 'Screen::Success' src/tui/event.rs &amp;&amp; grep -c 'Screen::Err' src/tui/event.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` + `cargo test --lib tui` pass
    - handle_key calls `scan_rename` (rename flow) and `scan_drop` (drop flow) exactly once each, at the transition INTO Screen::Preview — verify by grep -c &gt;= 1 for each, AND a code review confirming they are in the Enter arms of RenameForm and CoAuthorList respectively
    - handle_key calls `rewrite_author` and `drop_coauthor` inside the Screen::Preview Y/Enter arm
    - Errors from scan_* and rewrite_* route to Screen::Err
    - At least 5 new tests covering the new transitions
    - The earlier `Screen::Preview(PendingOp)` shape no longer appears anywhere in event.rs (negative grep for `Screen::Preview(P`)
    - `Screen::NotImplemented` no longer appears anywhere in event.rs (negative grep — Plans 03-03/04 had TODO placeholders, Task 2 rewires them)
  </acceptance_criteria>
  <done>Scan-on-transition, rewrite-on-confirm, and error routing all wired.</done>
</task>

<task type="auto">
  <name>Task 3: Render Preview (warnings + count + Y/N), render Success (count + force-push), render Err</name>
  <files>src/tui/render.rs</files>
  <read_first>
    - src/tui/render.rs (existing render dispatcher + render_preview_placeholder which is now broken by Plan 03-05 Task 1's variant reshape)
    - src/git/scan.rs (RewritePreview field shapes)
    - .planning/REQUIREMENTS.md (OUT-01 wording — "git push --force-with-lease --all")
  </read_first>
  <action>
    Modify src/tui/render.rs:

    1. Delete `render_preview_placeholder` and replace with the real `fn render_preview(frame: &mut Frame, area: Rect, op: &PendingOp, scan: &crate::git::scan::RewritePreview)`:

       Layout: vertical with [header (Length 3) showing operation summary, body (Fill) showing the affected count + warnings, footer (Length 2) showing Y/N keybindings].

       Header line: format the operation as one line:
         - For PendingOp::Rename: `format!("Rename: {} &lt;{}&gt; → {} &lt;{}&gt;", source.name, source.email, new_name, new_email)`
         - For PendingOp::Drop: `format!("Drop co-author: {} &lt;{}&gt;", target.name, target.email)`

       Body: a Paragraph with multi-line text. Compose the lines:
         - Line 1: `format!("This will rewrite {} commit(s).", scan.affected_count)` — this is RENAME-05 / DROP-04 (exact count).
         - Blank line.
         - If scan.signed_commit_count &gt; 0: push line `format!("⚠ {} commit(s) in the affected set are GPG/SSH-signed — signatures will be invalidated.", scan.signed_commit_count)` (SAFE-03).
         - If !scan.annotated_tags_affected.is_empty(): push line `format!("⚠ Annotated tag(s) will be recreated: {}", scan.annotated_tags_affected.join(", "))` (SAFE-04).
         - If scan.has_notes_ref: push line `"⚠ refs/notes/commits exists — notes reference old SHAs and will be orphaned by the rewrite."` (SAFE-05).
         - Blank line.
         - Final line: `"Proceed? (Y/N)"`

       Note: ratatui Paragraph handles multi-line text via Text or by `Paragraph::new(joined_string).wrap(Wrap { trim: false })`. Use the simple String-join approach.

       Footer: `Paragraph::new("Y / Enter: confirm   N / Esc: cancel")`.

    2. Add `fn render_success(frame: &mut Frame, area: Rect, rewritten: usize, remote_name: &Option&lt;String&gt;)`:
       - Body shows: `format!("✔ Rewrote {} commit(s).\n\nRun the following to update the remote:\n\n  git push --force-with-lease --all {}\n\nPress any key to exit.", rewritten, remote_name.as_deref().unwrap_or("&lt;remote&gt;"))`
       - The placeholder `&lt;remote&gt;` is used when `remote_name == None` (OUT-01 says "detected remote name" — when there is none, the placeholder communicates "fill this in yourself").
       - Use a single bordered Paragraph filling the area; pre-render with `.wrap(Wrap { trim: false })`.

    3. Add `fn render_err(frame: &mut Frame, area: Rect, msg: &str)`:
       - `Paragraph::new(format!("✗ Error\n\n{msg}\n\nPress any key to exit.")).block(Block::bordered().title("Error"))`.

    4. Update the `render` dispatcher's exhaustive match: route `Screen::Preview { op, scan }` → render_preview, `Screen::Success { rewritten, remote_name }` → render_success, `Screen::Err(msg)` → render_err. Remove the `Screen::NotImplemented(_)` arm from render.rs — the variant was deleted in Task 1 and rewired in Task 2, so the render dispatcher must also drop its arm. The match must remain exhaustive (no `_ =&gt;` wildcard).

    5. Re-verify the negative grep: render.rs MUST NOT call scan_*, rewrite_*, enumerate_*. The data shown comes entirely from the Screen variant fields.
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build12.txt; test ! -s /tmp/build12.txt &amp;&amp; cargo test 2>&amp;1 | grep -E "test result: ok\\." &amp;&amp; grep -c 'render_preview' src/tui/render.rs &amp;&amp; grep -c 'render_success' src/tui/render.rs &amp;&amp; grep -c 'render_err' src/tui/render.rs &amp;&amp; grep -c 'force-with-lease' src/tui/render.rs &amp;&amp; (! grep -E 'scan_rename|scan_drop|rewrite_author|drop_coauthor|enumerate_authors|enumerate_coauthors' src/tui/render.rs)</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` + full `cargo test` pass
    - src/tui/render.rs defines `render_preview`, `render_success`, `render_err`
    - The literal string `force-with-lease` appears in render.rs (OUT-01 force-push command shown verbatim in success screen)
    - render.rs has NO calls to scan_rename, scan_drop, rewrite_author, drop_coauthor, enumerate_authors, enumerate_coauthors (negative grep gate)
    - render_preview shows: operation summary header, exact affected_count line, conditional warning lines for signed/tags/notes, "Proceed? (Y/N)" prompt
    - render_success substitutes the detected remote name OR the `<remote>` placeholder when remote_name is None
    - The render dispatcher is exhaustive (no `_ =>` wildcard)
  </acceptance_criteria>
  <done>All five required screens render correctly; warnings displayed before confirmation per SAFE-03/04/05; success screen carries OUT-01 force-push reminder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human smoke-test — full end-to-end rename and drop on a throwaway repo</name>
  <what-built>
    Complete Phase 3 TUI: both flows end-to-end, with scan-based exact counts, non-blocking warnings, real rewrite, success screen with force-push reminder.

    ⚠ This test MUTATES the test repo's history. Use a THROWAWAY repo, never the project repo or anything pushed.
  </what-built>
  <how-to-verify>
    Build first: `cargo build --release`. The release binary path is `target/release/git-author-reformer`.

    Create a throwaway test fixture:
      ```
      cd /tmp
      mkdir gar-smoke && cd gar-smoke && git init
      git config user.name "Alice" && git config user.email "alice@example.com"
      echo a > f.txt && git add . && git commit -m "first"
      echo b >> f.txt && git commit -am "second"
      git remote add origin git@example.invalid:fake/repo.git
      git config user.name "Bob" && git config user.email "bob@example.com"
      echo c >> f.txt && git commit -am "co-authored

Co-authored-by: Carol <carol@example.com>"
      ```

    Rename flow:
      1. Run the binary: `/path/to/target/release/git-author-reformer`
      2. Main Menu → Enter on "Rename an author" → see Alice + Bob in the list, with commit counts (Alice: 2, Bob: 1).
      3. Filter to "ali" → only Alice shown. Enter on Alice.
      4. Form opens. Type new name "Alice Renamed", Tab, type "alice2@example.com", Enter.
      5. Preview shows: "Rename: Alice <alice@example.com> → Alice Renamed <alice2@example.com>" header, "This will rewrite 3 commit(s)." (cascade: Alice's two + Bob's one descendant), no warnings (no GPG, no tags, no notes), "Proceed? (Y/N)". Verify count is 3 (NOT 2 — the cascade is the load-bearing RENAME-05 acceptance).
      6. Press Y. Screen changes to Success: "✔ Rewrote 3 commit(s)." and "git push --force-with-lease --all origin".
      7. Press any key. Binary exits cleanly.
      8. Verify post-condition: `git log --all --format='%an <%ae>'` shows zero `Alice <alice@example.com>` lines.

    Drop flow (on a fresh fixture):
      9. `cd /tmp && rm -rf gar-smoke2 && mkdir gar-smoke2 && cd gar-smoke2 && git init && git config user.name X && git config user.email x@x.x`
      10. `echo a > f && git add . && git commit -m "msg

Co-authored-by: Carol <carol@example.com>"`
      11. Run the binary, navigate to Drop a co-author, filter "carol", Enter on Carol.
      12. Preview shows "Drop co-author: Carol <carol@example.com>" + "This will rewrite 1 commit(s)." + "Proceed? (Y/N)".
      13. Press Y. Success: "✔ Rewrote 1 commit(s)." + force-push reminder using `<remote>` placeholder (no remote configured).
      14. Verify: `git log --format=%B HEAD` no longer contains the Co-authored-by line.

    Warning paths (optional but recommended):
      15. Create a tag: `git tag -a v1.0 HEAD~1 -m "tag msg"` then run the rename flow. Preview should show "⚠ Annotated tag(s) will be recreated: v1.0".
      16. Add a note: `git notes add -m "note" HEAD`. Run drop flow. Preview should show "⚠ refs/notes/commits exists ...".

    Cancel path:
      17. In Preview, press N or Esc. Should return to Main Menu without writing.

    Error path:
      18. Optional: run the binary in a dirty stash state (`git stash push`). Should fail at preflight (Phase 1) before even reaching the TUI. Not strictly a Phase 3 test.
  </how-to-verify>
  <read_first>
    - All Phase 3 plan SUMMARYs (03-01 through 03-04 if they exist by now)
    - src/tui/render.rs (final form)
    - src/tui/event.rs (final form)
  </read_first>
  <acceptance_criteria>
    - User confirms rename flow shows correct cascade count (3, not 2, in the fixture above)
    - User confirms drop flow shows correct count (1)
    - User confirms force-push reminder shows actual remote name when present (`origin`) and `<remote>` placeholder when absent
    - User confirms post-rewrite `git log` no longer shows the renamed identity / dropped co-author
    - User confirms warning lines appear when applicable (signed commits, tags, notes)
    - User confirms N/Esc cancels without writing
    - User confirms terminal is left clean after every exit path
  </acceptance_criteria>
  <resume-signal>Type "approved" after running the full smoke test, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Confirmation prompt → rewrite | User input gates a destructive operation; must require explicit Y |
| RewritePreview data → Preview screen | Cascade count must match what the rewrite actually does, or the user is misled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Repudiation / accidental destruction | Preview Y/Enter → rewrite | mitigate | Confirmation requires explicit Y or Enter (no auto-confirm); N/Esc cancels. Preview shows EXACT cascade count (RENAME-05, DROP-04) — scan.rs cascade-tracking guarantees count matches rewrite. Warnings (SAFE-03/04/05) surface destructive consequences before confirmation. |
| T-03-10 | Tampering | rewrite_author / drop_coauthor invocation | accept | These functions are already vetted in Phase 2 (full test suite). This plan does not modify them — only invokes them with user-validated inputs. |
| T-03-11 | Information disclosure | Error message in Screen::Err | accept | `e.to_string()` on AppError surfaces git2 error text. git2 errors are not sensitive (paths, refs, OIDs — already user-visible). |
</threat_model>

<verification>
- `cargo build` + `cargo test` pass
- Manual smoke test (Task 4) verifies RENAME-05, DROP-04, SAFE-03, SAFE-04, SAFE-05, OUT-01 in actual UI
- render.rs negative grep clean (no scan_*, rewrite_*, enumerate_* calls)
- Cascade count in UI matches Phase 2 rewrite count for same fixture
- Success screen shows `git push --force-with-lease --all <remote-name>` literal
</verification>

<success_criteria>
1. RENAME-05: rename flow shows exact affected commit count + Y/N confirmation before write
2. DROP-04: drop flow shows exact affected commit count + Y/N confirmation before write
3. SAFE-03: GPG/SSH-signed commit count surfaces as non-blocking warning when > 0
4. SAFE-04: annotated tag names surface as non-blocking warning when affected set intersects tags
5. SAFE-05: refs/notes/commits presence surfaces as non-blocking warning
6. OUT-01: success screen shows rewritten count + `git push --force-with-lease --all <remote>` using detected name (or `<remote>` placeholder if no remote)
7. All five Phase 3 success criteria from ROADMAP are met
8. Phase 3 complete: every requirement ID (CORE-01, RENAME-01, RENAME-02, RENAME-05, DROP-01, DROP-04, SAFE-03, SAFE-04, SAFE-05, OUT-01) demonstrably satisfied
</success_criteria>

<output>
Create `.planning/phases/03-tui-integration/03-05-SUMMARY.md` summarizing the final Screen shape, the scan-on-transition pattern, and the synchronous-rewrite (Strategy A) decision so Phase 4 inherits a stable understanding of the TUI lifecycle.
</output>
