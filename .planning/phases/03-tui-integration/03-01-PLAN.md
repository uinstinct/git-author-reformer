---
phase: 03-tui-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/git/mod.rs
  - src/git/scan.rs
  - src/lib.rs
  - src/tui/mod.rs
  - tests/scan_test.rs
autonomous: false
requirements:
  - RENAME-05
  - DROP-04
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - OUT-01
must_haves:
  truths:
    - "scan_rename returns the same affected commit count as rewrite_author would produce for the same identity (cascade equivalence)"
    - "scan_drop returns the same affected commit count as drop_coauthor would produce for the same email"
    - "RewritePreview.signed_commit_count counts only commits in the cascade set, not all signed commits in the repo"
    - "RewritePreview.annotated_tags_affected contains only tag names pointing at commits in the cascade set"
    - "RewritePreview.has_notes_ref is true if and only if refs/notes/commits or the configured note_default_ref() exists"
    - "RewritePreview.remote_name is Some(\"origin\") when origin remote exists, else first remote name, else None"
    - "src/tui/ module compiles as an empty namespace (no TUI yet); src/lib.rs exposes pub mod tui"
  artifacts:
    - path: "src/git/scan.rs"
      provides: "RewritePreview struct + scan_rename + scan_drop"
      contains: "pub struct RewritePreview"
    - path: "tests/scan_test.rs"
      provides: "scan cascade equivalence + warning detection tests"
      contains: "fn test_scan_rename_count_matches_rewrite_author"
    - path: "src/tui/mod.rs"
      provides: "empty tui namespace ready for Wave 2"
      contains: "pub fn run"
    - path: "Cargo.toml"
      provides: "ratatui 0.30, crossterm 0.29, nucleo 0.5, signal-hook 0.4 dependencies"
      contains: "ratatui"
  key_links:
    - from: "src/git/scan.rs"
      to: "src/git/rewrite.rs"
      via: "shared cascade logic — identity_matches || any_parent_remapped"
      pattern: "any_parent_remapped"
    - from: "src/git/mod.rs"
      to: "src/git/scan.rs"
      via: "pub mod scan;"
      pattern: "pub mod scan"
    - from: "src/lib.rs"
      to: "src/tui/mod.rs"
      via: "pub mod tui;"
      pattern: "pub mod tui"
---

<objective>
Add the read-only `git::scan` module that produces `RewritePreview` (affected count + warnings + remote name) before any rewrite is attempted. Add the four TUI-stack dependencies to Cargo.toml behind a blocking legitimacy checkpoint. Add an empty `src/tui/` module skeleton (`pub mod tui` + `tui::run` stub) so Wave 2 can drop screens into a compiling crate.

Purpose: RENAME-05 and DROP-04 require showing the **exact** affected count BEFORE rewriting. SAFE-03/04/05 require showing non-blocking warnings. OUT-01 needs the remote name for the force-push reminder. The existing `rewrite_author` and `drop_coauthor` functions produce a count only AFTER they execute — too late for confirmation. This plan adds the read-only preview API that calls in waves 3–5 will use.

Output: scan.rs with two pub functions returning `RewritePreview`; Cargo deps added; tui module skeleton committed; scan_test.rs verifies cascade equivalence against rewrite functions.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tui-integration/03-CONTEXT.md
@.planning/phases/03-tui-integration/03-RESEARCH.md
@.planning/phases/03-tui-integration/03-PATTERNS.md
@src/git/rewrite.rs
@src/git/reader.rs
@src/git/mod.rs
@src/error.rs
@src/lib.rs
@Cargo.toml
@tests/common/mod.rs
@tests/rewrite_test.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing src/. -->

From src/error.rs:
```rust
pub enum AppError {
    NotARepo(String),
    StashDetected,
    WorktreesDetected(String),
    Git(#[from] git2::Error),  // covers all git2::Error via ?
    NonUtf8Message(git2::Oid),
}
```
No new AppError variant needed for scan.rs — all errors flow through `AppError::Git`.

From src/git/rewrite.rs (cascade logic that scan MUST replicate, lines 19–44):
```rust
let mut revwalk = repo.revwalk()?;
revwalk.push_glob("refs/heads/*")?;
revwalk.push_glob("refs/tags/*")?;
revwalk.set_sorting(Sort::TOPOLOGICAL | Sort::REVERSE)?;

for oid_result in revwalk {
    let old_oid = oid_result?;
    let commit = repo.find_commit(old_oid)?;
    let identity_matches = commit.author().name().unwrap_or("") == old_name
        && commit.author().email().unwrap_or("") == old_email;
    let any_parent_remapped =
        (0..commit.parent_count()).any(|i| oid_map.contains_key(&commit.parent_id(i).unwrap()));
    let needs_rewrite = identity_matches || any_parent_remapped;
    // scan: would_remap.insert(old_oid) instead of writing a commit
}
```

From src/git/reader.rs (pub(crate) helpers scan_drop reuses):
```rust
pub(crate) fn strip_coauthor_prefix(line: &str) -> Option<&str>
pub(crate) fn parse_coauthor_value(value: &str) -> Option<(String, String)>
```

From src/git/types.rs (existing — DO NOT modify, just for awareness):
```rust
pub struct AuthorIdentity { pub name: String, pub email: String, pub commit_count: usize }
pub struct CoAuthorEntry  { pub name: String, pub email: String, pub commit_count: usize }
```

From tests/common/mod.rs (helpers — DO NOT modify; reuse):
```rust
pub fn create_fixture_repo() -> (TempDir, Repository);
pub fn add_commit_with_message(repo: &Repository, name: &str, email: &str, message: &str);
pub fn create_branch(repo: &Repository, name: &str, target: &git2::Commit);
pub fn add_merge_commit(repo, name, email, message, parent0, parent1);
pub fn create_annotated_tag(repo: &Repository, name: &str, target: &git2::Commit, message: &str);
```

New public API this plan defines:
```rust
// src/git/scan.rs
pub struct RewritePreview {
    pub affected_count: usize,
    pub signed_commit_count: usize,
    pub annotated_tags_affected: Vec<String>,
    pub has_notes_ref: bool,
    pub remote_name: Option<String>,
}

pub fn scan_rename(
    repo: &git2::Repository,
    old_name: &str,
    old_email: &str,
) -> Result<RewritePreview, crate::error::AppError>;

pub fn scan_drop(
    repo: &git2::Repository,
    target_email: &str,
) -> Result<RewritePreview, crate::error::AppError>;
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Cargo dependency legitimacy (BLOCKING — supply chain)</name>
  <what-built>Four new Cargo dependencies will be added to Cargo.toml in Task 2: ratatui 0.30, crossterm 0.29, nucleo 0.5, signal-hook 0.4. All four are tagged `[ASSUMED]` in RESEARCH.md §"Package Legitimacy Audit" because slopcheck was unavailable at research time.</what-built>
  <how-to-verify>
    Verify each package on crates.io BEFORE proceeding. For each crate, open the listed URL and confirm: (a) source repository link present; (b) recent maintenance activity; (c) download count consistent with RESEARCH.md claims; (d) no abandonment notice.

    1. ratatui — https://crates.io/crates/ratatui — expect github.com/ratatui/ratatui, very-high downloads, recent commits
    2. crossterm — https://crates.io/crates/crossterm — expect github.com/crossterm-rs/crossterm, very-high downloads
    3. nucleo — https://crates.io/crates/nucleo — expect github.com/helix-editor/helix (or helix-editor/nucleo), high downloads
    4. signal-hook — https://crates.io/crates/signal-hook — expect github.com/vorner/signal-hook, ~151M downloads
  </how-to-verify>
  <read_first>
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Package Legitimacy Audit", lines 82–96)
  </read_first>
  <acceptance_criteria>
    - User responds "approved" after confirming all four crates are legitimate per the criteria above
    - If any crate fails verification, user provides explicit substitute or aborts the phase — DO NOT silently downgrade or skip
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed, or list problematic crates with substitute proposals</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add Cargo dependencies + tui module skeleton + scan.rs module declaration</name>
  <files>Cargo.toml, src/git/mod.rs, src/git/scan.rs, src/lib.rs, src/tui/mod.rs</files>
  <read_first>
    - Cargo.toml (existing dependency block, lines 7–10)
    - src/git/mod.rs (existing module declarations, lines 1–8)
    - src/lib.rs (lines 1–2)
  </read_first>
  <action>
    Modify Cargo.toml: under `[dependencies]` add exactly these four lines (no feature flags, no = pinning):
      ratatui = "0.30"
      crossterm = "0.29"
      nucleo = "0.5"
      signal-hook = "0.4"
    Do not touch the existing git2/thiserror/clap lines, the [dev-dependencies] block, or the [profile.release] block.

    Modify src/git/mod.rs: add `pub mod scan;` to the existing module declarations (alphabetical order is acceptable but not required — keep the file as a flat list of `pub mod` declarations followed by the `open_repo` function).

    Modify src/lib.rs: add `pub mod tui;` as a third line below `pub mod git;`.

    Create src/tui/mod.rs containing exactly this stub:
      - One doc-comment line explaining "TUI shell — populated in Phase 3 Wave 2+"
      - `pub fn run(_repo: git2::Repository) -> Result<(), crate::error::AppError> { Ok(()) }`
      The function takes ownership of the repo (move) but does nothing yet — Wave 2 fills in the body. The leading underscore on `_repo` suppresses the unused-variable warning until Wave 2.

    Create src/git/scan.rs as an EMPTY file containing only a `// placeholder — implementation in Task 3 (RED) and Task 4 (GREEN)` comment. The file must exist so `pub mod scan;` resolves; it gets real content in Task 4. (TDD discipline: don't write the impl before the test.)
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tee /tmp/build1.txt; test ! -s /tmp/build1.txt &amp;&amp; grep -c "^pub mod tui;" src/lib.rs &amp;&amp; grep -c "^pub mod scan;" src/git/mod.rs &amp;&amp; grep -c '^ratatui = "0.30"$' Cargo.toml &amp;&amp; grep -c '^crossterm = "0.29"$' Cargo.toml &amp;&amp; grep -c '^nucleo = "0.5"$' Cargo.toml &amp;&amp; grep -c '^signal-hook = "0.4"$' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 with no warnings (the `_repo` underscore prevents unused-variable warning)
    - Cargo.toml contains exactly the four new lines: `ratatui = "0.30"`, `crossterm = "0.29"`, `nucleo = "0.5"`, `signal-hook = "0.4"`
    - src/lib.rs contains a line matching `^pub mod tui;$`
    - src/git/mod.rs contains a line matching `^pub mod scan;$`
    - src/tui/mod.rs exports `pub fn run` returning `Result<(), crate::error::AppError>`
    - src/git/scan.rs file exists (contents may be a placeholder comment only at this point)
  </acceptance_criteria>
  <done>Crate compiles cleanly with all four new deps resolved; tui namespace exists empty; scan.rs declared but unimplemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED — write scan_test.rs covering cascade equivalence, signatures, tags, notes, remote</name>
  <files>tests/scan_test.rs</files>
  <read_first>
    - tests/rewrite_test.rs (full file — analog test patterns + cascade behavior expectations)
    - tests/common/mod.rs (full file — fixture helpers)
    - src/git/rewrite.rs (lines 19–95: cascade logic that scan must match; lines 215–295: drop cascade logic)
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Pattern 4–6" — signature/notes/remote detection contracts)
  </read_first>
  <behavior>
    Tests MUST cover the following observable contracts. Each test name carries the requirement ID it encodes; each assert message states WHY the assertion exists (the business rule), not just WHAT it checks.

    Cascade equivalence (RENAME-05 / DROP-04 — the load-bearing tests):
      - test_scan_rename_count_matches_rewrite_author: build fixture with Alice-authored commits + a merge commit + a downstream commit by Bob whose parent is Alice's. Call scan_rename(repo, "Alice", "alice@example.com"). Then on a CLONE of the fixture state call rewrite_author with the same args. Assert scan.affected_count == rewrite_count, with a message: "RENAME-05 requires the count shown at confirmation to equal the count actually rewritten — cascade tracking is mandatory."
      - test_scan_drop_count_matches_drop_coauthor: same shape but with co-authored commits and target_email.
      - test_scan_rename_counts_cascade_descendants: fixture where Alice authors commit A, Bob authors commit B with A as parent. Scanning for Alice must report affected_count == 2 (A + B's cascade). Assert message cites Pitfall 2 from RESEARCH.md.
      - test_scan_rename_zero_when_no_match: scanning for a name that doesn't exist returns affected_count == 0 and empty warning vectors.

    Signature detection (SAFE-03):
      - test_scan_rename_counts_signed_commits_in_cascade_only: create two commits by Alice, one with a fake `gpgsig` header (use git2's commit creation; if direct header injection is impractical, simulate via repo.commit_signed). Also create one signed commit by Bob (NOT in cascade). Assert signed_commit_count == 1 (only the in-cascade signed one). Message: "SAFE-03 warning must reflect only the cascade set, not the whole repo."
      - Note: if git2 0.21 does not expose `commit_signed`, write the test to construct a commit object via `repo.odb()` with a synthetic gpgsig header and verify `commit.header_field_bytes("gpgsig").is_ok()`. If this proves intractable, mark that single sub-case `#[ignore]` with a TODO referencing this plan — but the in-cascade counting logic itself must still be tested using a commit that DOES have a gpgsig header by some constructable means. Document the chosen approach in a code comment.

    Annotated tag detection (SAFE-04):
      - test_scan_rename_lists_annotated_tags_pointing_at_cascade: fixture creates Alice-authored commit, creates annotated tag "v1.0" pointing at it via common::create_annotated_tag. scan_rename returns annotated_tags_affected containing "refs/tags/v1.0" (or just "v1.0" — pick one and document; recommendation: refs/tags/-stripped short name). Lightweight tags pointing at cascade commits should NOT appear (only annotated). Assert message cites RENAME-04 + SAFE-04.

    Notes ref detection (SAFE-05):
      - test_scan_drop_detects_notes_ref_when_present: create note via `repo.note(&sig, &sig, None, commit_oid, "note body", false)`. Assert has_notes_ref == true.
      - test_scan_drop_no_notes_ref_when_absent: fixture with no notes. Assert has_notes_ref == false.

    Remote detection (OUT-01):
      - test_scan_rename_prefers_origin_remote: fixture adds remotes "upstream" and "origin"; assert remote_name == Some("origin").
      - test_scan_rename_first_remote_when_no_origin: only "upstream" exists; assert remote_name == Some("upstream").
      - test_scan_rename_none_when_no_remote: fresh fixture has no remotes; assert remote_name == None.

    All tests must use the `mod common;` + use-import pattern from tests/rewrite_test.rs.
  </behavior>
  <action>
    Create tests/scan_test.rs from scratch using the structure pattern from tests/rewrite_test.rs (lines 1–22):
      mod common;
      use git2::{Repository, Signature, Time};
      use git_author_reformer::git::scan::{scan_rename, scan_drop, RewritePreview};
      use git_author_reformer::git::rewrite::{rewrite_author, drop_coauthor};

    Write every test listed in <behavior> above. Each test:
      1. Creates a tempdir+repo via common::create_fixture_repo()
      2. Builds the precondition graph using common::add_commit_with_message / common::create_branch / common::add_merge_commit / common::create_annotated_tag (do NOT add new helpers in this task — Task 4 can add a signed-commit helper IF needed, but prefer in-test construction)
      3. Calls scan_rename or scan_drop
      4. Asserts on RewritePreview fields with a message that names the requirement ID

    For cascade-equivalence tests: construct the fixture, snapshot it by reading affected_count from scan, then create a SECOND fixture with identical setup (re-run the same builder sequence on a new tempdir) and call rewrite_author / drop_coauthor on the second. Compare counts. (Cannot re-use the same repo because rewrite mutates it.)

    For the signed-commit subcase: try `repo.commit_signed(commit_content, signature_str, signature_field)` first (git2 0.21 API). If that path is unavailable on this git2 version, fall back to constructing the commit object directly via `repo.odb().write(ObjectType::Commit, &raw_bytes)` where raw_bytes embeds a `gpgsig` header. The single assertion that must work is `commit.header_field_bytes("gpgsig").is_ok()` on the resulting commit.

    Do NOT modify rewrite.rs, reader.rs, scan.rs, or tests/common/mod.rs in this task — this is the RED step, the test file is the only new artifact.
  </action>
  <verify>
    <automated>cargo test --test scan_test 2>&amp;1 | tee /tmp/red.txt; grep -E "error\\[E0432\\]|cannot find" /tmp/red.txt &gt;/dev/null &amp;&amp; echo "RED: tests fail to compile due to missing scan_rename/scan_drop (expected)" || (grep -E "FAILED|test result: FAILED" /tmp/red.txt &gt;/dev/null &amp;&amp; echo "RED: tests compiled but failed (also acceptable)" || (echo "UNEXPECTED: tests passed before implementation — RED step invalid" &amp;&amp; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - tests/scan_test.rs exists
    - `cargo test --test scan_test` fails — either compile-fails on missing `scan_rename`/`scan_drop`/`RewritePreview` symbols (preferred, proves the API surface is exercised) OR compiles and fails at runtime
    - At least 9 test functions exist (count via `grep -c '^fn test_' tests/scan_test.rs` — wait, tests use `#[test]\nfn`; use `grep -c '#\[test\]' tests/scan_test.rs` ≥ 9)
    - Every `assert!` / `assert_eq!` message text contains the substring `RENAME-05`, `DROP-04`, `SAFE-03`, `SAFE-04`, `SAFE-05`, or `OUT-01` (at least one per requirement covered above)
  </acceptance_criteria>
  <done>RED step proven: failing test suite committed, no implementation yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — implement scan.rs to make all scan_test.rs tests pass</name>
  <files>src/git/scan.rs</files>
  <read_first>
    - src/git/scan.rs (the placeholder from Task 2)
    - src/git/rewrite.rs (cascade logic — must be replicated, not approximated)
    - src/git/reader.rs (pub(crate) strip_coauthor_prefix + parse_coauthor_value — reused for scan_drop)
    - tests/scan_test.rs (the contract this implementation must satisfy)
    - .planning/phases/03-tui-integration/03-RESEARCH.md (§"Pattern 4" signatures, §"Pattern 5" notes, §"Pattern 6" remote)
  </read_first>
  <behavior>
    The implementation MUST:
    - Use `Sort::TOPOLOGICAL | Sort::REVERSE` revwalk pushing both `refs/heads/*` and `refs/tags/*` (same as rewrite.rs lines 19–25).
    - Track cascade via `let mut would_remap: HashSet&lt;Oid&gt; = HashSet::new();` — insert any commit where `identity_matches || any_parent_remapped` for scan_rename, or `message_would_change || any_parent_remapped` for scan_drop.
    - For scan_drop's `message_would_change` check: call `commit.message_raw().unwrap_or("")` (reader.rs style — silent skip on non-UTF-8 since this is read-only), parse trailers with `strip_coauthor_prefix` + `parse_coauthor_value`, return true if any matching trailer exists (case-insensitive email compare via `eq_ignore_ascii_case`). Replicate the message-change semantics of `drop_coauthor_from_message` (rewrite.rs lines 181–208): also normalize \r\n -> \n equivalence when computing "would change" so CRLF commits don't falsely register.
    - signed_commit_count: iterate over the would_remap set; for each Oid call `repo.find_commit(oid)?` and check `commit.header_field_bytes("gpgsig").is_ok() || commit.header_field_bytes("sshsig").is_ok()`. Increment.
    - annotated_tags_affected: walk `repo.references_glob("refs/tags/*")?`; for each ref whose object is `ObjectType::Tag`, peel to the underlying commit via `tag.target_id()`; if that commit Oid is in would_remap, push the tag short name (strip the "refs/tags/" prefix) into the result Vec. Skip lightweight tags (ObjectType::Commit) — they are silent ref updates; SAFE-04 specifically warns about annotated tag OBJECTS being recreated.
    - has_notes_ref: try `repo.note_default_ref().ok()` first, then check `repo.find_reference(&default_ref).is_ok() || repo.find_reference("refs/notes/commits").is_ok()`. Return the OR.
    - remote_name: `repo.remotes()?.iter().flatten().collect::&lt;Vec&lt;&amp;str&gt;&gt;()`. If "origin" is in the list, return `Some("origin".to_string())`; else return the first name (mapped to owned String); else None. If `repo.remotes()` itself errors, return None (don't propagate — remote enumeration is best-effort warning data, not safety-critical).
    - All errors that DO matter (revwalk failures, find_commit failures) propagate via `?` and convert to `AppError::Git` via the existing `#[from]`.

    Define `RewritePreview` exactly as specified in the <interfaces> block. Derive `Debug, Clone` so callers in Wave 3+ can pass it to render functions.
  </behavior>
  <action>
    Replace the placeholder content of src/git/scan.rs with the full implementation per the <behavior> contract. Mirror rewrite.rs's organization: imports at top, `pub struct RewritePreview` next, then `pub fn scan_rename`, then `pub fn scan_drop`, then private helpers (`commit_is_signed`, `detect_remote_name`, `has_notes_ref`, `collect_affected_annotated_tags`, `message_would_drop_coauthor`).

    Imports needed (analog: rewrite.rs lines 1–3):
      use crate::git::reader::{parse_coauthor_value, strip_coauthor_prefix};
      use git2::{ObjectType, Oid, Sort};
      use std::collections::HashSet;

    The two pub functions share enough revwalk + warning-collection logic that you SHOULD extract a private helper `fn collect_warnings(repo, would_remap) -> (signed_count, tags, has_notes, remote)` to avoid duplication. Do not over-abstract — a single private helper is enough; do not introduce a trait.

    Run `cargo test --test scan_test` after each substantive change. When ALL tests pass, the GREEN step is done. Do NOT add features beyond what tests require (Karpathy Rule 2).

    Do NOT modify rewrite.rs, reader.rs, mod.rs, or tests in this task. If you find that scan needs a helper from reader.rs that is currently `pub(crate)`, you may use it directly — same crate. If you discover you need a helper from rewrite.rs that is currently private, prefer copying the logic (3 lines of `any_parent_remapped`) rather than widening visibility.
  </action>
  <verify>
    <automated>cargo test --test scan_test 2>&amp;1 | tee /tmp/green.txt; grep -E "test result: ok\\." /tmp/green.txt &amp;&amp; cargo clippy --tests --quiet -- -D warnings 2>&amp;1 | tee /tmp/clippy.txt; test ! -s /tmp/clippy.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test scan_test` reports `test result: ok.` with non-zero passed count and zero failures
    - `cargo test` (all targets) still passes — no regressions in phase 1 / phase 2 test suites
    - `cargo clippy --tests -- -D warnings` exits 0
    - src/git/scan.rs exports `pub struct RewritePreview` with fields exactly: affected_count: usize, signed_commit_count: usize, annotated_tags_affected: Vec&lt;String&gt;, has_notes_ref: bool, remote_name: Option&lt;String&gt;
    - src/git/scan.rs contains `let mut would_remap: HashSet&lt;Oid&gt; = HashSet::new();` (verify with grep -F 'HashSet&lt;Oid&gt;' src/git/scan.rs)
    - src/git/scan.rs uses `header_field_bytes("gpgsig")` and `header_field_bytes("sshsig")` (verify with grep -c 'header_field_bytes' src/git/scan.rs ≥ 2)
  </acceptance_criteria>
  <done>GREEN step proven: every scan_test passes; cascade equivalence with rewrite functions verified by test; all warnings/remote detection working.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: REFACTOR — extract shared revwalk seed helper if duplication justifies; otherwise note pass-through</name>
  <files>src/git/scan.rs</files>
  <read_first>
    - src/git/scan.rs (the GREEN implementation)
    - src/git/rewrite.rs (compare revwalk setup at lines 19–25 and 219–222)
    - src/git/reader.rs (lines 75–80 — a different revwalk pattern with heads-only)
  </read_first>
  <action>
    Review src/git/scan.rs for the kind of duplication TDD's REFACTOR step actually fixes — not speculative abstraction. Specifically:

    1. If `scan_rename` and `scan_drop` share an identical revwalk-construction block (push_glob heads, push_glob tags, set_sorting), extract into `fn build_full_revwalk(repo: &git2::Repository) -> Result&lt;git2::Revwalk&lt;'_&gt;, git2::Error&gt;` private to scan.rs. (Do not promote this to a crate-wide helper — rewrite.rs has its own copy and changing rewrite.rs is out of scope here. Karpathy Rule 3.)

    2. If the `collect_warnings` helper is large enough that the two scan_* functions read awkwardly, leave it — already extracted in Task 4. Do NOT split further.

    3. If, after review, no meaningful duplication exists and the implementation is already minimal, write a single-line comment at top of scan.rs: `// Refactor pass: no duplication justifying extraction; revwalk inlined intentionally per Karpathy Rule 2.` and move on. This is acceptable — REFACTOR is a check, not a mandate to invent abstractions.

    4. Run `cargo fmt` and ensure `cargo clippy --tests -- -D warnings` still passes.

    Do NOT introduce new public APIs. Do NOT widen visibility on private helpers in rewrite.rs or reader.rs. Do NOT add new dependencies.
  </action>
  <verify>
    <automated>cargo fmt --check 2>&amp;1 | tee /tmp/fmt.txt; test ! -s /tmp/fmt.txt &amp;&amp; cargo test 2>&amp;1 | grep -E "test result: ok\\." &amp;&amp; cargo clippy --tests --quiet -- -D warnings 2>&amp;1 | tee /tmp/clippy2.txt; test ! -s /tmp/clippy2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0 (formatting clean)
    - Full `cargo test` reports `test result: ok.` for every test target (no regressions)
    - `cargo clippy --tests -- -D warnings` exits 0
    - No new pub or pub(crate) symbol added in scan.rs beyond `RewritePreview`, `scan_rename`, `scan_drop`
  </acceptance_criteria>
  <done>REFACTOR pass complete; scan.rs is final form for Phase 3; all tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo registry → local build | Four new dependencies pulled from crates.io into the build; supply-chain attack surface |
| Local repository (filesystem) → scan logic | git2 reads commit objects; malformed/hostile objects could trigger git2 panics — out of scope, libgit2 is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | Cargo deps (ratatui, crossterm, nucleo, signal-hook) | mitigate | Task 1 blocking-human checkpoint: human verifies crates.io entry, source repo, download counts before Task 2 modifies Cargo.toml. Versions are minor-range pinned ("0.30", "0.29", "0.5", "0.4") — Cargo.lock will pin the exact patch. |
| T-03-01 | Information disclosure | RewritePreview.signed_commit_count | accept | Signature content is never read or surfaced — only presence/absence of the header. Treating Ok(_) as "signed" leaks no secret material. |
| T-03-02 | Denial of service | revwalk over hostile repo | accept | Same revwalk logic is already in rewrite.rs (Phase 2) running in production code paths. No new attack surface introduced. |
</threat_model>

<verification>
- `cargo build` succeeds with no warnings
- `cargo test` reports all tests passing across every target (phase 1, phase 2, new scan_test)
- `cargo clippy --tests -- -D warnings` exits 0
- `cargo fmt --check` exits 0
- `src/tui/mod.rs` exposes `pub fn run` (the empty stub Wave 2 will replace)
- `src/git/scan.rs` exports `pub struct RewritePreview` + `scan_rename` + `scan_drop`
- Cascade equivalence is provable: scan.affected_count == rewrite count on shared fixtures
</verification>

<success_criteria>
1. `git::scan::scan_rename` and `git::scan::scan_drop` produce `RewritePreview` with `affected_count` matching what `rewrite_author`/`drop_coauthor` would actually rewrite — verified by test
2. Warning data (`signed_commit_count`, `annotated_tags_affected`, `has_notes_ref`) is computed from the cascade set, not the whole repo
3. `remote_name` resolves "origin" preferentially, falls back to first remote, returns None on local-only repos
4. Cargo manifest contains the four new dependencies; `cargo build` succeeds
5. `src/tui/` module exists as empty namespace ready for Wave 2 to populate
</success_criteria>

<output>
Create `.planning/phases/03-tui-integration/03-01-SUMMARY.md` when done — summarize what scan.rs exposes, what Cargo.toml gained, and the empty tui module skeleton ready for Wave 2.
</output>
