---
phase: 06-hook-tui-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/tui/app.rs
  - src/tui/event.rs
  - src/tui/render.rs
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
must_haves:
  truths:
    - "Main menu renders four options: Rename, Drop, Add co-author auto-strip hook, Manage auto-strip hook"
    - "Down arrow from index 3 wraps to 0; Down from index 1 goes to index 2 (not 0)"
    - "Codebase compiles with all four new Screen variants declared"
    - "Existing test suite passes; mod_2 tests renamed to reflect 4-item semantics"
  artifacts:
    - path: "src/tui/app.rs"
      provides: "Extended MenuChoice (4 variants), 4 new Screen variants, build_strip_nucleo, apply_strip_filter"
      contains: "AddHook"
    - path: "src/tui/event.rs"
      provides: "% 4 modulus, match MenuChoice::from_index dispatch, placeholder arms for 4 new screens"
      contains: "% 4"
    - path: "src/tui/render.rs"
      provides: "Placeholder render functions for HookAddList, HookManageList, HookSuccess, HookAlreadyStripped"
      contains: "render_hook_add_list"
  key_links:
    - from: "src/tui/event.rs"
      to: "src/tui/app.rs"
      via: "MenuChoice::from_index(*selected)"
      pattern: "from_index"
    - from: "src/tui/render.rs"
      to: "src/tui/app.rs"
      via: "match &app.screen arms for new variants"
      pattern: "HookAddList"
---

<objective>
Extend the App state machine with four new Screen variants, extend MenuChoice from 2 to 4 options,
wire the navigation modulus changes, and add placeholder match arms so the codebase compiles cleanly.
Also add build_strip_nucleo and apply_strip_filter to app.rs (Manage flow fuzzy helpers).

This plan scaffolds all structural changes; 06-03 and 06-04 fill in the real logic.

Purpose: Every downstream plan (06-03 Add, 06-04 Manage) can compile and build against these types.
Output: Extended enums, stub render functions, corrected navigation modulus.
Requirements addressed: HOOK-01, HOOK-02.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/instinct/Desktop/working/git-author-reformer/.planning/PROJECT.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/ROADMAP.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/phases/06-hook-tui-integration/06-01-SUMMARY.md

<!-- HOOKNAMING: use Absent / Managed { emails } / NotToolManaged(PathBuf) — NOT NoHook/Foreign -->

<!-- CURRENT MenuChoice (app.rs:96-119) — to be extended:
pub enum MenuChoice { Rename, Drop }
impl MenuChoice {
    pub fn from_index(i: usize) -> Self { if i == 0 { Rename } else { Drop } }
    pub fn label(&self) -> &'static str { ... }
    pub fn all() -> [Self; 2] { [Self::Rename, Self::Drop] }
}
-->

<!-- CURRENT Screen variants (app.rs:13-45):
MainMenu, AuthorList, RenameForm, Preview, CoAuthorList, Success, Err
Four new variants must be added: HookAddList, HookManageList, HookSuccess, HookAlreadyStripped
-->

<!-- CURRENT MODULUS (event.rs:40-41):
*selected = (*selected + 1) % 2
*selected = (*selected + 2 - 1) % 2
Must become % 4 and + 4 - 1 (same wrap pattern, 4 items)
-->

<!-- EXISTING FUZZY HELPERS (app.rs:154-177) to mirror for strip list:
build_coauthor_nucleo(items: &[CoAuthorEntry]) -> Nucleo<CoAuthorEntry>
apply_coauthor_filter(nucleo: &mut Nucleo<CoAuthorEntry>, query: &str) -> Vec<CoAuthorEntry>
New helpers follow identical pattern but for Vec<String> (email strings).
-->

<!-- PITFALL 6 (RESEARCH.md): existing test names encode % 2 semantics.
test_main_menu_down_increments_selected_mod_2 → rename to test_main_menu_down_increments_selected
test_main_menu_up_decrements_with_wrap       → keep name, update assertions for 4-item wrap
The wrap assertion for Down must change: Down from index 3 wraps to 0 (not Down from 1 to 0).
-->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MenuChoice, Screen enum, and fuzzy helpers in app.rs</name>
  <files>src/tui/app.rs</files>
  <behavior>
    - MenuChoice::all() returns [Self; 4] with Rename, Drop, AddHook, ManageHook
    - MenuChoice::from_index: 0=Rename, 1=Drop, 2=AddHook, _=ManageHook
    - MenuChoice::label() covers all four variants with the exact strings from CONTEXT.md success criteria:
      "Rename an author", "Drop a co-author", "Add co-author auto-strip hook", "Manage auto-strip hook"
    - Screen has four new variants:
        HookAddList { current_strip: Vec<String>, items: Vec<CoAuthorEntry>, filter: String, matched: Vec<CoAuthorEntry>, nucleo: Nucleo<CoAuthorEntry>, selected: usize }
        HookManageList { items: Vec<String>, filter: String, matched: Vec<String>, nucleo: Nucleo<String>, selected: usize }
        HookSuccess { state: crate::hook::HookState }
        HookAlreadyStripped { email: String }
    - build_strip_nucleo(items: &[String]) -> Nucleo<String> — mirrors build_coauthor_nucleo
    - apply_strip_filter(nucleo: &mut Nucleo<String>, query: &str) -> Vec<String> — mirrors apply_coauthor_filter
    - All existing tests in app.rs continue to pass
    - New test: test_menu_choice_all_has_four_items() asserts MenuChoice::all().len() == 4
    - New test: test_menu_choice_labels() asserts each label string matches the spec
  </behavior>
  <action>
    In src/tui/app.rs:

    1. Add two new variants to MenuChoice: AddHook and ManageHook.

    2. Update MenuChoice::from_index to use a match expression:
       match i { 0 => Self::Rename, 1 => Self::Drop, 2 => Self::AddHook, _ => Self::ManageHook }

    3. Update MenuChoice::label to cover all four variants. Labels must match EXACTLY:
       Rename => "Rename an author"
       Drop => "Drop a co-author"
       AddHook => "Add co-author auto-strip hook"
       ManageHook => "Manage auto-strip hook"

    4. Update MenuChoice::all return type from [Self; 2] to [Self; 4] and include all four variants.

    5. Add four new variants to Screen enum. The HookAddList and HookManageList fields mirror
       CoAuthorList and AuthorList field structure respectively. HookSuccess carries a
       crate::hook::HookState (use the real type — not a string alias). HookAlreadyStripped carries
       a single email: String field.

    6. After the existing apply_coauthor_filter function, add build_strip_nucleo and apply_strip_filter.
       Follow the exact same structure as build_coauthor_nucleo / apply_coauthor_filter but generic over
       String items. The injector push closure: for String, push item.clone() and use item.clone() as
       the column value (no name/email formatting needed).

    7. In the #[cfg(test)] block, add:
       test_menu_choice_all_has_four_items: assert_eq!(MenuChoice::all().len(), 4)
       test_menu_choice_labels: assert all four label strings match the spec strings exactly.

    cargo check must pass. cargo test --lib tui::app must pass.
  </action>
  <verify>
    <automated>cargo test --lib tui::app</automated>
  </verify>
  <done>
    cargo test --lib tui::app passes.
    MenuChoice::all().len() == 4.
    Screen has HookAddList, HookManageList, HookSuccess, HookAlreadyStripped variants.
    build_strip_nucleo and apply_strip_filter are pub functions in app.rs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update event.rs modulus, dispatch, and add placeholder arms; fix test names</name>
  <files>src/tui/event.rs, src/tui/render.rs</files>
  <behavior>
    - Down from index 3 wraps to 0; Down from index 1 goes to 2 (not 0 as before)
    - Up from index 0 wraps to 3
    - Enter handler dispatches on MenuChoice::from_index(*selected) with a match expression
    - Rename and Drop branches keep existing behavior (preflight + load, from 06-01)
    - AddHook and ManageHook branches are stubs: app.screen = Screen::Err("not yet implemented".into()); (placeholder — replaced in 06-03 and 06-04)
    - handle_key has exhaustive match arms for all four new Screen variants: each arm does nothing for now (empty body or return) — ensures compilation
    - render::render() has arms for all four new Screen variants, calling stub render functions
    - Stub render functions (render_hook_add_list, render_hook_manage_list, render_hook_success, render_hook_already_stripped) each render a Paragraph with a "todo" string — enough to compile and display something
    - Existing tests: test_main_menu_down_increments_selected_mod_2 is RENAMED to test_main_menu_down_increments_selected; its assertion changes from Down-from-1-wraps-to-0 to Down-from-3-wraps-to-0
    - test_main_menu_up_decrements_with_wrap: assertion changes to Up-from-0-wraps-to-3 (four items)
    - test_main_menu_j_k_same_as_down_up: update if it asserts specific index after second keypress
    - All other existing tests continue to pass
    - New test: test_main_menu_shows_four_options asserts MenuChoice::all().len() == 4 (simple guard)
  </behavior>
  <action>
    In src/tui/event.rs:

    1. Update the Down handler in Screen::MainMenu: change % 2 to % 4.

    2. Update the Up handler: change (*selected + 2 - 1) % 2 to (*selected + 4 - 1) % 4.

    3. Replace the if *selected == 0 { ... } else { ... } Enter handler with:
       Extract sel = *selected before the match key block ends or inside the Enter arm.
       Use match MenuChoice::from_index(sel) { Rename => {...}, Drop => {...}, AddHook => { app.screen = Screen::Err("not yet implemented".into()); }, ManageHook => { app.screen = Screen::Err("not yet implemented".into()); } }
       The Rename and Drop branches keep the exact code from 06-01 (preflight + enumerate logic).

    4. Add placeholder arms for the four new Screen variants in handle_key. Each arm: just return or do nothing for all key inputs. Example:
       Screen::HookAddList { .. } => {}
       Screen::HookManageList { .. } => {}
       Screen::HookSuccess { .. } => {}
       Screen::HookAlreadyStripped { .. } => {}

    5. Update the existing test functions per the behavior block:
       - Rename test_main_menu_down_increments_selected_mod_2 to test_main_menu_down_increments_selected
       - Change its wrap assertion to: Down from index 3 wraps back to 0
       - Update test_main_menu_up_decrements_with_wrap to assert Up from 0 wraps to 3
       - Update test_main_menu_j_k_same_as_down_up assertions if they check index > 1

    6. Add test_main_menu_shows_four_options.

    In src/tui/render.rs:

    7. Add four arms to the match in render():
       Screen::HookAddList { filter, matched, selected, current_strip, .. } =>
           render_hook_add_list(frame, frame.area(), current_strip, filter, matched, *selected),
       Screen::HookManageList { filter, matched, selected, .. } =>
           render_hook_manage_list(frame, frame.area(), filter, matched, *selected),
       Screen::HookSuccess { state } =>
           render_hook_success(frame, frame.area(), state),
       Screen::HookAlreadyStripped { email } =>
           render_hook_already_stripped(frame, frame.area(), email),

    8. Add stub private render functions:
       fn render_hook_add_list(frame, area, current_strip, filter, matched, selected): render a Paragraph "Add hook (todo)".
       fn render_hook_manage_list(frame, area, filter, matched, selected): render a Paragraph "Manage hook (todo)".
       fn render_hook_success(frame, area, state): render a Paragraph "Hook success (todo)".
       fn render_hook_already_stripped(frame, area, email): render a Paragraph "Already stripped (todo)".

    These stubs will be replaced in 06-03 and 06-04. They only need to compile.

    Add crate::hook::HookState to render.rs imports (or use the full path in the stub function signature).
    Add the new Screen variants to the imports from crate::tui::app in render.rs (they come from app.rs).

    cargo check must pass. cargo test must pass.
  </action>
  <verify>
    <automated>cargo test</automated>
  </verify>
  <done>
    cargo test (full suite) passes.
    No % 2 in event.rs MainMenu Down/Up handlers.
    render() in render.rs has arms for all 11 Screen variants (7 original + 4 new).
    Old test_main_menu_down_increments_selected_mod_2 function name no longer exists.
    test_main_menu_down_increments_selected exists and asserts wrap at 4 items.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MenuChoice dispatch | User keyboard input routes to one of four branches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | AddHook/ManageHook stub branches | accept | Stubs show Screen::Err("not yet implemented") — no mutation, no data access |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cargo test — full suite green.
grep -c "% 2" src/tui/event.rs — must be 0 (MainMenu Down/Up no longer use % 2).
grep -c "HookAddList\|HookManageList\|HookSuccess\|HookAlreadyStripped" src/tui/render.rs — must be >= 4.
grep "mod_2" src/tui/event.rs — must return nothing (old test name removed).
</verification>

<success_criteria>
- All four new Screen variants compile (HookAddList, HookManageList, HookSuccess, HookAlreadyStripped)
- MenuChoice::all() returns 4 items; label strings match spec exactly
- Navigation modulus is 4; Down from index 3 wraps to 0
- Placeholder event arms and stub render functions keep everything compiling
- Full cargo test passes
- HOOK-01 and HOOK-02 structurally satisfied (menu shows 4 items with correct labels)
</success_criteria>

<output>
Create .planning/phases/06-hook-tui-integration/06-02-SUMMARY.md when done.
</output>
