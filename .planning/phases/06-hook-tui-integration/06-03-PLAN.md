---
phase: 06-hook-tui-integration
plan: 03
type: tdd
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/tui/event.rs
  - src/tui/render.rs
autonomous: true
requirements:
  - HOOK-01
  - HOOK-03
  - HOOK-11
must_haves:
  truths:
    - "Selecting 'Add co-author auto-strip hook' (index 2) reads the current strip list and opens HookAddList"
    - "HookAddList shows current strip emails in a header panel (or 'no entries yet') and a fuzzy co-author list below"
    - "Selecting a co-author calls install_strip; success lands on HookSuccess with strip state re-read from hook engine"
    - "Selecting a duplicate co-author lands on HookAlreadyStripped screen"
    - "HookAlreadyStripped: any key returns to MainMenu at index 2"
    - "HookSuccess: any key sets should_exit = true"
    - "Foreign hook (NotToolManaged) at Add entry -> Screen::Err"
  artifacts:
    - path: "src/tui/event.rs"
      provides: "Real AddHook branch replacing the 06-02 stub; HookAddList arm; HookAlreadyStripped arm; HookSuccess arm"
      contains: "install_strip"
    - path: "src/tui/render.rs"
      provides: "render_hook_add_list with three-zone layout; render_hook_success; render_hook_already_stripped"
      contains: "current_strip"
  key_links:
    - from: "src/tui/event.rs"
      to: "src/hook/mod.rs"
      via: "crate::hook::install_strip, crate::hook::read_strip_list"
      pattern: "install_strip"
    - from: "src/tui/event.rs"
      to: "src/git/reader.rs"
      via: "crate::git::reader::enumerate_coauthors — HOOK-03 mandates reuse, not reimplementation"
      pattern: "enumerate_coauthors"
---

<objective>
Wire the real Add flow: replace the 06-02 placeholder AddHook branch with the full implementation.
Also implement the HookAddList, HookAlreadyStripped, and HookSuccess event arms and their render functions.

The Add flow:
  MainMenu(2) --(Enter)--> read_strip_list --> HookAddList (show strip header + co-author fuzzy list)
  HookAddList --(Enter on item)--> install_strip -->
      Installed: re-read via read_strip_list --> HookSuccess
      AlreadyStripped --> HookAlreadyStripped { email }
      Err --> Screen::Err
  HookAlreadyStripped --(any key)--> MainMenu { selected: 2 }
  HookSuccess --(any key)--> should_exit = true

Purpose: Deliver the complete Add co-author auto-strip hook user flow (HOOK-01, HOOK-03, HOOK-11).
Output: Fully functional Add flow with real render, real event handling, tested.
Requirements addressed: HOOK-01, HOOK-03, HOOK-11.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/instinct/Desktop/working/git-author-reformer/.planning/PROJECT.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/ROADMAP.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/phases/06-hook-tui-integration/06-02-SUMMARY.md

<!-- HOOK ENGINE API (src/hook/mod.rs) — do not re-implement these:
pub fn install_strip(repo: &git2::Repository, email: &str) -> Result<AddResult, AppError>
    AddResult::Installed { count: usize }
    AddResult::AlreadyStripped
pub fn read_strip_list(repo: &git2::Repository) -> Result<HookState, AppError>
    HookState::Absent
    HookState::Managed { emails: Vec<String> }
    HookState::NotToolManaged(PathBuf)   <-- NOT "Foreign"; NOT "NoHook"
-->

<!-- FUZZY REUSE (app.rs) — use existing functions, no new ones:
build_coauthor_nucleo(items: &[CoAuthorEntry]) -> Nucleo<CoAuthorEntry>
apply_coauthor_filter(nucleo: &mut Nucleo<CoAuthorEntry>, query: &str) -> Vec<CoAuthorEntry>
HookAddList carries a Nucleo<CoAuthorEntry> using these exact functions.
-->

<!-- NLL BORROW PATTERN (event.rs Screen::Preview arm, lines 158-196):
Clone data out of the variant BEFORE the `match key` block.
Then call app.repo functions using the clone — the borrow on app.screen has ended.
Pattern for HookAddList Enter arm:
  if let Some(target) = matched.get(*selected).cloned() {
      let email = target.email.clone();
      // email is a String, no longer borrowing app.screen
      match crate::hook::install_strip(&app.repo, &email) { ... }
  }
-->

<!-- HOOK-11 RE-READ MANDATE:
After install_strip returns Ok(AddResult::Installed), always call read_strip_list(&app.repo)
to populate HookSuccess.state. Do NOT construct state from count or from a cached vec.
Exception: AlreadyStripped -> HookAlreadyStripped { email } directly (no re-read needed).
-->

<!-- PATTERN 1 from RESEARCH.md — AddHook entry point:
read_strip_list -> match state:
  Managed { emails } -> current_strip = emails
  Absent -> current_strip = vec![]
  NotToolManaged(_) -> current_strip = vec![]  (install_strip will refuse at write time)
enumerate_coauthors -> build_coauthor_nucleo -> apply_coauthor_filter("") -> HookAddList
-->

<!-- RENDER LAYOUT — HookAddList (three zones per RESEARCH.md §Rendering Patterns):
Zone 1 (top, Length(4)): Paragraph block titled "Current strip list" showing current_strip emails,
  one per line, or "no entries yet" if empty.
Zone 2 (middle, Fill(1)): filter input + fuzzy co-author list (mirror render_coauthor_list layout).
Zone 3 (bottom, Length(1)): hint "type: filter  up/down: move  Enter: select  Esc: back"
-->

<!-- RENDER — HookSuccess:
HookState::Absent -> "No hook installed — no emails configured.\n\nAny key to exit."
HookState::Managed { emails } -> "Hook active — stripping N email(s):\n  a@x.com\n  ...\n\nAny key to exit."
HookState::NotToolManaged -> should never reach HookSuccess; treat as unreachable or Err in event.rs.
-->

<!-- RENDER — HookAlreadyStripped:
"Already stripped: <email>\n\nThis email is already in the strip list.\n\nAny key to return to menu."
Any key -> MainMenu { selected: 2 }
-->
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for Add flow state transitions</name>
  <files>src/tui/event.rs</files>
  <behavior>
    - test_main_menu_routes_add_hook: From MainMenu selected:2, press Enter on a repo with no hook.
      Result should be Screen::HookAddList (not Screen::Err("not yet implemented")).
      FAILS currently because 06-02 left a stub that produces Screen::Err.
    - test_add_hook_happy_path: Set up HookAddList screen with one co-author (bob@example.com).
      Press Enter. Result should be Screen::HookSuccess with a Managed state containing that email.
      FAILS because HookAddList arm is empty (placeholder from 06-02).
    - test_add_hook_already_stripped: Same setup but first install bob@example.com via
      crate::hook::install_strip directly, then set up HookAddList and press Enter.
      Result should be Screen::HookAlreadyStripped { email } where email == "bob@example.com".
      FAILS because HookAddList arm is empty.
    - test_hook_already_stripped_any_key_returns_to_menu: Set screen to HookAlreadyStripped { email: "x@x".into() }, press Enter.
      Result should be Screen::MainMenu { selected: 2 }.
      FAILS because HookAlreadyStripped arm does nothing.
    - test_hook_success_any_key_exits: Set screen to HookSuccess { state: HookState::Absent }, press Enter.
      Result should be should_exit = true.
      FAILS because HookSuccess arm does nothing.
  </behavior>
  <action>
    Add the five test functions to #[cfg(test)] mod tests in src/tui/event.rs.

    For tests that need a real repo with a hook installed, use make_test_app_with_commits() (gives
    a non-bare repo). The hook engine functions (install_strip, read_strip_list) work on any
    non-bare repo's .git/hooks/ directory.

    For make_hook_add_list_screen helper: build a HookAddList screen directly by constructing the
    variant with a Vec<CoAuthorEntry> item, calling build_coauthor_nucleo and apply_coauthor_filter,
    and setting current_strip to vec![]. This helper is analogous to make_coauthor_list_screen.

    Import crate::hook::{HookState, AddResult} in the test module as needed.

    Run cargo test --lib tui::event — the five new tests MUST FAIL. All previously passing tests
    MUST PASS. Commit: test(06-03): add failing Add flow tests.
  </action>
  <verify>
    <automated>cargo test --lib tui::event 2>&1 | grep -E "FAILED|test result"</automated>
  </verify>
  <done>
    The five new tests fail with assertion mismatches (not compile errors).
    All previously passing tests pass.
    git commit: test(06-03): add failing Add flow tests
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement Add flow event logic and render functions</name>
  <files>src/tui/event.rs, src/tui/render.rs</files>
  <behavior>
    - After this task all five tests from Task 1 pass.
    - AddHook branch: reads strip list, enumerates co-authors, builds HookAddList screen.
    - HookAddList Enter arm: NLL-clones email, calls install_strip, re-reads on Installed, HookAlreadyStripped on AlreadyStripped.
    - HookAddList filter/nav keys: mirror CoAuthorList pattern (Char appends to filter, Backspace pops, Down/Up move selection, Esc returns to MainMenu { selected: 2 }).
    - HookAlreadyStripped: any key -> MainMenu { selected: 2 }.
    - HookSuccess: any key -> should_exit = true. (Note: HookSuccess covers both Add and Manage outcomes — this arm handles both.)
    - render_hook_add_list: three-zone layout with current strip list header, filter input, co-author list, hint.
    - render_hook_success: renders based on HookState variant (Absent vs Managed).
    - render_hook_already_stripped: renders email and return hint.
  </behavior>
  <action>
    In src/tui/event.rs:

    1. Replace the AddHook stub with the real implementation (per RESEARCH.md Pattern 1):
       - Call crate::hook::read_strip_list(&app.repo) to get current_strip.
       - On NotToolManaged: app.screen = Screen::Err("Foreign hook at <path> — remove or rename it first.".into()); return.
       - On Absent: current_strip = vec![].
       - On Managed { emails }: current_strip = emails.
       - Call crate::git::reader::enumerate_coauthors(&app.repo). On Err: Screen::Err.
       - Build nucleo: let mut nucleo = build_coauthor_nucleo(&items); let matched = apply_coauthor_filter(&mut nucleo, "");
       - app.screen = Screen::HookAddList { current_strip, items, filter: String::new(), matched, nucleo, selected: 0 }.
       The NLL issue does not apply here because we're in the Enter arm of MainMenu before any mutable borrow of app.screen variants that carry the repo — just read app.repo fields freely.

    2. Replace the HookAddList placeholder arm with a real arm:
       Screen::HookAddList { filter, matched, nucleo, selected, .. } => match key { ... }

       Enter key (NLL pattern from RESEARCH.md):
         let email = matched.get(*selected).map(|t| t.email.clone());
         // After extracting email, drop borrow by falling through to the reassignment
         if let Some(email) = email {
             match crate::hook::install_strip(&app.repo, &email) {
                 Ok(AddResult::Installed { .. }) => {
                     match crate::hook::read_strip_list(&app.repo) {
                         Ok(state) => app.screen = Screen::HookSuccess { state },
                         Err(e)    => app.screen = Screen::Err(e.to_string()),
                     }
                 }
                 Ok(AddResult::AlreadyStripped) => {
                     app.screen = Screen::HookAlreadyStripped { email };
                 }
                 Err(e) => app.screen = Screen::Err(e.to_string()),
             }
         }

       Char(c): filter.push(c); *matched = apply_coauthor_filter(nucleo, filter); *selected = 0;
       Backspace: filter.pop(); *matched = apply_coauthor_filter(nucleo, filter); *selected = 0;
       Down: if !matched.is_empty() { *selected = (*selected + 1) % matched.len(); }
       Up: if !matched.is_empty() { *selected = (*selected + matched.len() - 1) % matched.len(); }
       Esc: app.screen = Screen::MainMenu { selected: 2 };
       _: {}

    3. Replace HookAlreadyStripped placeholder:
       Screen::HookAlreadyStripped { .. } => { app.screen = Screen::MainMenu { selected: 2 }; }
       (Any key returns to menu — the match on key can be a wildcard.)

    4. Replace HookSuccess placeholder:
       Screen::HookSuccess { .. } => { app.should_exit = true; }
       (Any key exits — same pattern as Screen::Err.)

    In src/tui/render.rs:

    5. Replace render_hook_add_list stub with three-zone implementation:
       Zone 1 (Length(4)): Paragraph block titled "Current strip list". Body: if current_strip.is_empty(), "no entries yet", else join emails with "\n".
       Zone 2 (Fill(1)): filter row (Length(3)) + list (Fill(1)) mirroring render_coauthor_list. Title: "Co-authors ({N} match)".
       Zone 3 (Length(1)): hint paragraph "type: filter  up/down: move  Enter: select  Esc: back".
       The overall layout needs a vertical split of 4 zones: Length(4), Length(3), Fill(1), Length(1).

    6. Replace render_hook_success stub:
       use crate::hook::HookState in the function signature.
       match state {
           HookState::Absent => render "No hook installed — no emails configured.\n\nAny key to exit."
           HookState::Managed { emails } => render "Hook active — stripping N email(s):\n  {email list}\n\nAny key to exit."
           HookState::NotToolManaged(_) => render "Error: foreign hook (should not reach this screen)."
       }
       Wrap the Paragraph in a Block::bordered().title("Hook Status").

    7. Replace render_hook_already_stripped stub:
       Render: "Already stripped: {email}\n\nThis email is already in the strip list.\n\nAny key to return to menu."
       Block::bordered().title("No change").

    Run cargo test — all tests including the five from Task 1 must pass.
    Commit: feat(06-03): implement Add flow (HookAddList, HookSuccess, HookAlreadyStripped).
  </action>
  <verify>
    <automated>cargo test --lib tui::event</automated>
  </verify>
  <done>
    All five Task 1 tests now pass.
    Full cargo test passes.
    AddHook branch calls enumerate_coauthors (not a parallel implementation).
    install_strip result is followed by read_strip_list re-read (HOOK-11).
    HookAlreadyStripped any-key returns to MainMenu { selected: 2 }.
    HookSuccess any-key sets should_exit = true.
    git commit: feat(06-03): implement Add flow (HookAddList, HookSuccess, HookAlreadyStripped)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI selection → hook engine | Selected email string passed verbatim to install_strip for validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Injection | Email string passed to install_strip | accept | validate_email_for_embedding is called inside install_strip (Phase 5 delivery); TUI passes verbatim |
| T-06-03-02 | Tampering | Foreign hook at Add entry | mitigate | NotToolManaged -> Screen::Err with clear message; install_strip also refuses via HookExists |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cargo test — full suite green.
grep -n "install_strip" src/tui/event.rs — must match (Add flow calls it).
grep -n "enumerate_coauthors" src/tui/event.rs — must have exactly 2 matches (Drop branch from 06-01 + AddHook branch from this plan). No new enumeration function.
grep -n "read_strip_list" src/tui/event.rs — must appear after install_strip call (HOOK-11 re-read).
</verification>

<success_criteria>
- Add flow is fully operational end-to-end
- Co-author enumeration reuses enumerate_coauthors (HOOK-03)
- HookSuccess populated from read_strip_list re-read, not from AddResult (HOOK-11)
- HookAlreadyStripped shown for duplicates; any-key returns to menu at index 2
- Five new TDD tests pass
- Full cargo test green
</success_criteria>

<output>
Create .planning/phases/06-hook-tui-integration/06-03-SUMMARY.md when done.
</output>
