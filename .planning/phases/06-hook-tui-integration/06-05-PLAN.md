---
phase: 06-hook-tui-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - src/tui/event.rs
autonomous: true
requirements:
  - HOOK-14
must_haves:
  truths:
    - "All 12 HOOK-14 test cases exist and pass in tui::event tests"
    - "make_test_app_with_stash() helper is present in the test module"
    - "Add on a stash repo reaches HookAddList (not Screen::Err from preflight)"
    - "Manage on a stash repo reaches HookSuccess or HookManageList (not Screen::Err from preflight)"
    - "Rename on a stash repo still reaches Screen::Err (preflight still fires)"
    - "Drop on a stash repo still reaches Screen::Err (preflight still fires)"
    - "All previously passing tests continue to pass"
  artifacts:
    - path: "src/tui/event.rs"
      provides: "Complete HOOK-14 test suite in #[cfg(test)] block"
      contains: "test_add_hook_no_preflight_with_stash"
  key_links:
    - from: "src/tui/event.rs tests"
      to: "src/tui/event.rs handle_key"
      via: "handle_key called with KeyCode events on test App instances"
      pattern: "handle_key"
---

<objective>
Complete the HOOK-14 test suite. Plans 06-01 through 06-04 each added tests for their specific
behaviors. This plan adds the remaining HOOK-14 tests that require all four flows to exist
simultaneously: particularly the preflight regression tests on stash repos for Add and Manage
(which could not be written until MenuChoice::AddHook/ManageHook existed from 06-02).

Purpose: Guarantee full automated coverage of every user path as required by HOOK-14.
Output: All 12 HOOK-14 tests passing; make_test_app_with_stash() helper in place.
Requirements addressed: HOOK-14.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/instinct/Desktop/working/git-author-reformer/.planning/PROJECT.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/ROADMAP.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/phases/06-hook-tui-integration/06-04-SUMMARY.md

<!-- HOOK-14 TEST TABLE (from RESEARCH.md §TUI Test Harness):
test_main_menu_shows_four_options             -- added in 06-02
test_main_menu_routes_add_hook                -- added in 06-03
test_main_menu_routes_manage_hook             -- added in 06-04
test_add_hook_happy_path                      -- added in 06-03
test_add_hook_already_stripped                -- added in 06-03
test_manage_empty_state                       -- added in 06-04
test_manage_remove_single_entry               -- added in 06-04
test_manage_remove_last_entry                 -- added in 06-04
test_add_hook_no_preflight_with_stash         -- MISSING — this plan
test_manage_no_preflight_with_stash           -- MISSING — this plan
test_rename_still_hits_preflight_with_stash   -- added in 06-01 (as test_rename_with_stash_repo_hits_preflight_err)
test_drop_still_hits_preflight_with_stash     -- added in 06-01 (as test_drop_with_stash_repo_hits_preflight_err)
-->

<!-- make_test_app_with_stash() (RESEARCH.md §New helper needed):
fn make_test_app_with_stash() -> (TempDir, App) {
    Create non-bare repo in TempDir.
    Create one empty-tree commit with Alice sig.
    Call repo.reference("refs/stash", head_oid, false, "stash") to fake the stash ref.
    Return (dir, App::new(repo)).
}
check_stash checks only repo.find_reference("refs/stash").is_ok() — the OID content is irrelevant.
This helper is NOT the same as make_test_app_with_commits (which has no stash ref).
-->

<!-- EXISTING TESTS TO AUDIT:
test_rename_with_stash_repo_hits_preflight_err  -- added 06-01; ensure name is present, don't add duplicate
test_drop_with_stash_repo_hits_preflight_err    -- added 06-01; same
test_main_menu_shows_four_options               -- added 06-02
test_main_menu_routes_add_hook                  -- added 06-03
test_main_menu_routes_manage_hook_empty         -- added 06-04 (matches test_manage_empty_state intent)
test_add_hook_happy_path                        -- added 06-03
test_add_hook_already_stripped                  -- added 06-03
test_manage_remove_single_entry / test_manage_remove_last_entry -- added 06-04
-->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit existing HOOK-14 coverage and add missing stash-bypass tests</name>
  <files>src/tui/event.rs</files>
  <behavior>
    - make_test_app_with_stash() helper exists in the test module (if not already added by 06-01 or earlier plans; add if absent).
    - test_add_hook_no_preflight_with_stash: construct App with stash repo (make_test_app_with_stash). Navigate to MainMenu selected:2 (Down twice from 0). Press Enter. Result must be Screen::HookAddList (or Screen::HookSuccess if no co-authors exist in a bare-ish repo — acceptable; the key invariant is NOT Screen::Err with a StashDetected message).
    - test_manage_no_preflight_with_stash: construct App with stash repo. Navigate to MainMenu selected:3 (Down three times). Press Enter. Result must be Screen::HookSuccess { state: Absent } (no hook installed on fresh repo) — NOT Screen::Err with StashDetected.
    - All 12 HOOK-14 test names either exist (from prior plans) or are added here. Do a quick audit: grep the test module for each of the 8 prior-plan tests from the context above. If any are missing (unlikely, but audit confirms), add them in this task too.
    - Full cargo test passes.
  </behavior>
  <action>
    First, audit existing test coverage. Scan the #[cfg(test)] mod in src/tui/event.rs for the
    following test names (or their 06-01/06-02/06-03/06-04 equivalents from those plans):

    From 06-01: test_rename_with_stash_repo_hits_preflight_err, test_drop_with_stash_repo_hits_preflight_err
    From 06-02: test_main_menu_shows_four_options, test_main_menu_down_increments_selected
    From 06-03: test_main_menu_routes_add_hook, test_add_hook_happy_path, test_add_hook_already_stripped
    From 06-04: test_main_menu_routes_manage_hook_empty, test_manage_remove_single_entry (or test_manage_remove_last_entry)

    For any gap found: add the missing test now.

    Then add the two new stash-bypass tests:

    test_add_hook_no_preflight_with_stash:
      let (_dir, mut app) = make_test_app_with_stash();
      // Navigate to Add (index 2): two Down presses
      handle_key(&mut app, KeyCode::Down); // 0 -> 1
      handle_key(&mut app, KeyCode::Down); // 1 -> 2
      handle_key(&mut app, KeyCode::Enter);
      // Must NOT be Screen::Err with StashDetected text
      match &app.screen {
          Screen::Err(msg) => {
              assert!(!msg.contains("stash") && !msg.contains("Stash"),
                  "Add flow must not trigger stash preflight; got Err: {}", msg);
          }
          Screen::HookAddList { .. } | Screen::HookSuccess { .. } => {} // both acceptable
          other => panic!("unexpected screen variant after Add on stash repo"),
      }

    test_manage_no_preflight_with_stash:
      let (_dir, mut app) = make_test_app_with_stash();
      // Navigate to Manage (index 3): three Down presses
      handle_key(&mut app, KeyCode::Down); // 0->1
      handle_key(&mut app, KeyCode::Down); // 1->2
      handle_key(&mut app, KeyCode::Down); // 2->3
      handle_key(&mut app, KeyCode::Enter);
      match &app.screen {
          Screen::Err(msg) => {
              assert!(!msg.contains("stash") && !msg.contains("Stash"),
                  "Manage flow must not trigger stash preflight; got Err: {}", msg);
          }
          Screen::HookSuccess { .. } | Screen::HookManageList { .. } => {} // both acceptable
          other => panic!("unexpected screen variant after Manage on stash repo"),
      }

    If make_test_app_with_stash was already added in 06-01, do not duplicate it; reuse it.
    If it was NOT added (06-01 may have used a different helper name), add it here.

    Run cargo test --lib tui::event to confirm the new tests pass (they should, given 06-01 moved
    the preflight correctly). Run full cargo test to confirm no regressions.

    Commit: test(06-05): add HOOK-14 stash-bypass regression tests for Add and Manage flows.
  </action>
  <verify>
    <automated>cargo test --lib tui::event</automated>
  </verify>
  <done>
    test_add_hook_no_preflight_with_stash passes.
    test_manage_no_preflight_with_stash passes.
    Full cargo test passes.
    All 12 HOOK-14 test behaviors are covered (either by prior plans or this task).
    git commit: test(06-05): add HOOK-14 stash-bypass regression tests for Add and Manage flows
  </done>
</task>

<task type="auto">
  <name>Task 2: Final phase gate — full suite clean</name>
  <files></files>
  <action>
    Run the full test suite and cargo check to confirm the entire phase's work compiles and passes:

    1. cargo test — all tests pass (no failures, no panics).
    2. cargo clippy -- -D warnings — no warnings in touched files.
    3. cargo fmt --check — no formatting drift in touched files.

    If clippy or fmt flags issues in files this phase touched (src/main.rs, src/tui/app.rs,
    src/tui/event.rs, src/tui/render.rs), fix them inline. Do NOT touch files this phase did not
    modify just to clean them up.

    If all three commands pass without fixes, this task is a no-op (still run them to confirm).

    Commit only if fixes were needed: fix(06-05): clippy/fmt cleanup in phase 06 touched files.
  </action>
  <verify>
    <automated>cargo test && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>
    cargo test passes (all tests green).
    cargo clippy -- -D warnings exits 0.
    cargo fmt --check exits 0.
    Phase 06 is ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only | All changes are test code; no production logic added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Test repos on disk in TempDir | accept | TempDir auto-cleaned on drop; no sensitive data in test repos |
| T-06-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cargo test --lib tui::event 2>&1 | grep -c "^test .* ok" — count must be >= 12 more than before phase 06 (12 new HOOK-14 tests).
grep -n "test_add_hook_no_preflight_with_stash\|test_manage_no_preflight_with_stash" src/tui/event.rs — must match.
grep -n "make_test_app_with_stash" src/tui/event.rs — must match.
cargo clippy -- -D warnings — exits 0.
cargo fmt --check — exits 0.
</verification>

<success_criteria>
- test_add_hook_no_preflight_with_stash and test_manage_no_preflight_with_stash both pass
- All 12 HOOK-14 behaviors are covered across the phase's test additions
- Full cargo test green
- cargo clippy and cargo fmt clean for all phase-touched files
- HOOK-14 requirement fully satisfied
- Phase 06 ready for verification
</success_criteria>

<output>
Create .planning/phases/06-hook-tui-integration/06-05-SUMMARY.md when done.
</output>
