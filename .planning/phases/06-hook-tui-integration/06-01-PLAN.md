---
phase: 06-hook-tui-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/main.rs
  - src/tui/event.rs
autonomous: true
requirements:
  - HOOK-12
must_haves:
  truths:
    - "Launching the TUI on a repo with stash entries does NOT terminate before the menu appears"
    - "Rename still triggers stash/worktree preflight and shows Screen::Err on a stash repo"
    - "Drop still triggers stash/worktree preflight and shows Screen::Err on a stash repo"
    - "AddHook and ManageHook paths (added in 06-02) will bypass preflight by design — no call site exists in the matched branches"
  artifacts:
    - path: "src/main.rs"
      provides: "Only git::open_repo(); no preflight calls"
      contains: "open_repo"
    - path: "src/tui/event.rs"
      provides: "Preflight calls inside Rename and Drop branches of MainMenu Enter handler"
      contains: "check_stash"
  key_links:
    - from: "src/tui/event.rs"
      to: "src/git/preflight.rs"
      via: "crate::git::preflight::check_stash / check_worktrees"
      pattern: "check_stash"
---

<objective>
Move the SAFE-01/SAFE-02 preflight calls (check_stash, check_worktrees) from src/main.rs into the
handle_key MainMenu Enter handler in src/tui/event.rs, gated to Rename (index 0) and Drop (index 1)
only. Without this change, any repo with stash entries exits before the TUI menu is rendered,
making Add and Manage unreachable (HOOK-12).

Purpose: Unblock Add and Manage flows from the preflight that only guards history-rewriting operations.
Output: src/main.rs without preflight calls; src/tui/event.rs with preflight inside Rename/Drop branches.
Requirements addressed: HOOK-12.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/instinct/Desktop/working/git-author-reformer/.planning/PROJECT.md
@/Users/instinct/Desktop/working/git-author-reformer/.planning/ROADMAP.md

<!-- CRITICAL FINDING (from RESEARCH.md §Critical Refactor) -->
<!-- src/main.rs lines 20-21 call check_stash and check_worktrees BEFORE ratatui::init(). -->
<!-- Any stash entry causes the binary to exit before the TUI starts. -->
<!-- The fix: remove those two lines from main.rs; add them inside the Rename and Drop branches -->
<!-- of the Screen::MainMenu Enter handler in event.rs. -->

<!-- NLL BORROW PATTERN (event.rs lines 158-196, Screen::Preview arm) -->
<!-- When reading from app.repo while app.screen is borrowed, clone needed values FIRST, -->
<!-- then call the repo function using the clone. This is already used in the Preview arm. -->

<!-- EXISTING PREFLIGHT FUNCTIONS -->
<!-- src/git/preflight.rs: check_stash(&repo) -> Result<(), AppError> -->
<!-- src/git/preflight.rs: check_worktrees(&repo) -> Result<(), AppError> -->
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing regression tests for preflight move</name>
  <files>src/tui/event.rs</files>
  <behavior>
    - make_test_app_with_stash() creates a non-bare repo with one commit and a fake refs/stash reference pointing at HEAD. check_stash checks only repo.find_reference("refs/stash").is_ok() — the OID does not matter.
    - test_rename_with_stash_repo_hits_preflight_err: construct App with stash repo, dispatch Enter on selected:0 (Rename), assert screen is Screen::Err. This test will FAIL before the preflight is moved into event.rs, because currently the preflight runs in main.rs (not visible to the TUI at all) — the test's App::new() succeeds and handle_key dispatches Rename to enumerate_authors without any preflight check, landing on Screen::AuthorList instead of Screen::Err.
    - test_drop_with_stash_repo_hits_preflight_err: same but selected:1 (Down then Enter), assert Screen::Err.
    - test_rename_without_stash_repo_loads_author_list: bare (no stash) repo, Enter on selected:0, assert Screen::AuthorList. This already passes and must keep passing after the refactor.
  </behavior>
  <action>
    Add make_test_app_with_stash() helper function inside the #[cfg(test)] mod tests block in src/tui/event.rs.
    The helper: init a non-bare repo in TempDir, create one empty-tree commit with Alice sig, then call repo.reference("refs/stash", head_oid, false, "stash") to create the fake stash ref. Return (dir, App::new(repo)).
    Add the three test functions listed in the behavior block. Run cargo test --lib tui::event — the two stash tests MUST FAIL (Screen::AuthorList != Screen::Err). The no-stash test must pass. Commit: test(06-01): add failing preflight regression tests.
    Do NOT modify any production code in this task.
  </action>
  <verify>
    <automated>cargo test --lib tui::event 2>&1 | grep -E "FAILED|ok|error"</automated>
  </verify>
  <done>
    test_rename_with_stash_repo_hits_preflight_err FAILS (expected Screen::Err, got Screen::AuthorList).
    test_drop_with_stash_repo_hits_preflight_err FAILS (expected Screen::Err, got Screen::CoAuthorList or similar).
    test_rename_without_stash_repo_loads_author_list PASSES.
    All previously passing tests still pass.
    git commit: test(06-01): add failing preflight regression tests
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Move preflight from main.rs into event.rs Rename/Drop branches</name>
  <files>src/main.rs, src/tui/event.rs</files>
  <behavior>
    - After this task: test_rename_with_stash_repo_hits_preflight_err passes (Screen::Err).
    - After this task: test_drop_with_stash_repo_hits_preflight_err passes (Screen::Err).
    - After this task: all previously passing tests still pass.
    - src/main.rs no longer calls check_stash or check_worktrees.
  </behavior>
  <action>
    In src/main.rs: remove lines 20-21 (the check_stash and check_worktrees calls). Remove any imports
    that become unused as a result — do not remove other lines.

    In src/tui/event.rs, inside the Screen::MainMenu Enter handler, replace the current if/else dispatch:

    The current code:
      if *selected == 0 { // Rename branch } else { // Drop branch }

    Replace with a match on the selected value (index 0 = Rename, index 1 = Drop — MenuChoice does not
    yet have AddHook/ManageHook; those come in 06-02). Add preflight calls at the top of the Rename and
    Drop branches BEFORE any other work:

    Rename branch: call crate::git::preflight::check_stash(&app.repo); if Err -> app.screen = Screen::Err(e.to_string()); return. Same for check_worktrees. If both pass, continue with the existing enumerate_authors logic.

    Drop branch: same two preflight calls in the same order before enumerate_coauthors.

    The NLL pattern in the Preview arm (event.rs:158) shows how to read app.repo while inside a match
    on app.screen. For the MainMenu arm: the selected value is a &mut usize; clone it as let sel = *selected; before the match key block, then use sel in match arms where you need to call app functions. However, the preflight calls can be done with app.repo before reassigning app.screen — the borrow on selected ends when you reassign app.screen, so the pattern is: extract what you need, call preflight, then assign screen.

    After the change, run cargo test --lib tui::event — all tests including the two new stash tests must PASS.
    Run cargo test (full suite) — must pass.
    Commit: feat(06-01): move preflight into event.rs Rename/Drop branches
  </action>
  <verify>
    <automated>cargo test --lib tui::event</automated>
  </verify>
  <done>
    All tui::event tests pass including the two new stash regression tests.
    Full cargo test passes.
    src/main.rs does not contain check_stash or check_worktrees.
    src/tui/event.rs contains check_stash inside both Rename and Drop branches.
    git commit: feat(06-01): move preflight into event.rs Rename/Drop branches
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process startup → TUI init | Preflight now runs inside TUI; still before any mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Preflight bypass via AddHook/ManageHook branches | accept | By design — hook install does not rewrite history; SAFE-01/SAFE-02 guards are history-rewrite-only |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
cargo test — full suite green after Task 2.
grep -n "check_stash\|check_worktrees" src/main.rs — must return 0 matches.
grep -n "check_stash" src/tui/event.rs — must return 2 matches (one per Rename and Drop branch).
</verification>

<success_criteria>
- src/main.rs has no check_stash or check_worktrees calls
- src/tui/event.rs has preflight calls inside Rename and Drop Enter-handler branches only
- Two new regression tests pass: stash repo + Rename = Screen::Err, stash repo + Drop = Screen::Err
- All previously passing tests continue to pass
- HOOK-12 precondition satisfied: the structural blocker is removed
</success_criteria>

<output>
Create .planning/phases/06-hook-tui-integration/06-01-SUMMARY.md when done.
</output>
