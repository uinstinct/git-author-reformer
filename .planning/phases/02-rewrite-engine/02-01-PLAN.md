---
phase: 02-rewrite-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/git/reader.rs
  - src/git/mod.rs
  - tests/common/mod.rs
autonomous: true
requirements: [RENAME-03, RENAME-04, DROP-02, DROP-03]

must_haves:
  truths:
    - "strip_coauthor_prefix and parse_coauthor_value are callable from src/git/rewrite.rs without duplication"
    - "src/git/mod.rs declares the rewrite module so rewrite.rs is part of the crate"
    - "Test fixtures can construct a repo containing a merge commit, multiple local branches, and an annotated tag"
    - "Existing Phase 1 tests still pass after the visibility change (no logic touched)"
  artifacts:
    - path: "src/git/reader.rs"
      provides: "pub(crate) visibility on strip_coauthor_prefix and parse_coauthor_value"
      contains: "pub(crate) fn strip_coauthor_prefix"
    - path: "src/git/mod.rs"
      provides: "pub mod rewrite; declaration"
      contains: "pub mod rewrite;"
    - path: "tests/common/mod.rs"
      provides: "create_branch, add_merge_commit, create_annotated_tag helpers"
      contains: "pub fn add_merge_commit"
    - path: "src/git/rewrite.rs"
      provides: "Empty/stub module file so the crate compiles after mod.rs declaration"
      contains: ""
  key_links:
    - from: "src/git/rewrite.rs"
      to: "src/git/reader.rs"
      via: "use crate::git::reader::{strip_coauthor_prefix, parse_coauthor_value};"
      pattern: "pub\\(crate\\) fn (strip_coauthor_prefix|parse_coauthor_value)"
    - from: "src/git/mod.rs"
      to: "src/git/rewrite.rs"
      via: "module declaration"
      pattern: "pub mod rewrite"
    - from: "tests/common/mod.rs"
      to: "tests/rewrite_test.rs (future)"
      via: "shared fixture helpers"
      pattern: "pub fn (create_branch|add_merge_commit|create_annotated_tag)"
---

<objective>
Establish the scaffolding for Phase 2 (rewrite engine) without writing any rewrite logic. Three changes: (1) expose two trailer-parsing helpers in reader.rs as `pub(crate)` so rewrite.rs can call them; (2) declare the new `rewrite` module in `src/git/mod.rs` and create an empty `src/git/rewrite.rs` stub so the crate compiles; (3) extend `tests/common/mod.rs` with three new fixture helpers (`create_branch`, `add_merge_commit`, `create_annotated_tag`) that the rewrite engine's tests will compose into multi-branch / merge / annotated-tag scenarios.

Purpose: enables the next two plans (RENAME, DROP) to be written purely as engine code + tests without any boilerplate detour. The visibility change unblocks DROP-02/03 (which reuses trailer parsing). The fixture helpers unblock RENAME-04 (annotated tag tests) and the merge-parent-order success criterion.

Output: a compiling crate with no behavior change, plus three new test helpers ready to be composed by the next plan's RED tests.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rewrite-engine/02-CONTEXT.md
@.planning/phases/02-rewrite-engine/02-RESEARCH.md
@.planning/phases/02-rewrite-engine/02-PATTERNS.md
@.planning/phases/01-foundation-read-layer/01-04-SUMMARY.md
@src/git/reader.rs
@src/git/mod.rs
@tests/common/mod.rs
@CLAUDE.md

<interfaces>
<!-- Current state of helpers being changed. Visibility is the only delta. -->

From src/git/reader.rs (lines 84, 95) — current private signatures:

  fn strip_coauthor_prefix(line: &str) -> Option<&str>
  fn parse_coauthor_value(value: &str) -> Option<(String, String)>

These two must become `pub(crate)` so `src/git/rewrite.rs` can call them via
`use crate::git::reader::{strip_coauthor_prefix, parse_coauthor_value};`.
No logic changes. No signature changes. Only the `pub(crate)` prefix.

From src/git/mod.rs (lines 1-3) — current module list:

  pub mod preflight;
  pub mod reader;
  pub mod types;

Add one line: `pub mod rewrite;`. Module order: alphabetical (per existing convention).

From tests/common/mod.rs (lines 6-30) — existing helper shape to mirror:

  pub fn create_fixture_repo() -> (TempDir, Repository)
  pub fn add_commit_with_message(repo: &Repository, name: &str, email: &str, message: &str)

New helpers follow the same style: no Result, panic via .unwrap() on failure
(test helpers, not production code), fixed Time::new(seconds, 0) timestamps to
keep commit SHAs deterministic across runs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make trailer-parsing helpers crate-visible and create empty rewrite module</name>
  <read_first>
    - src/git/reader.rs (read all 107 lines — confirm `fn strip_coauthor_prefix` at line 84 and `fn parse_coauthor_value` at line 95 are still private)
    - src/git/mod.rs (read all 7 lines — confirm existing module list and `open_repo` fn)
  </read_first>
  <files>src/git/reader.rs, src/git/mod.rs, src/git/rewrite.rs</files>
  <action>
    In `src/git/reader.rs`: change the keyword `fn strip_coauthor_prefix` to `pub(crate) fn strip_coauthor_prefix` and the keyword `fn parse_coauthor_value` to `pub(crate) fn parse_coauthor_value`. Touch nothing else in this file — do not modify the docstrings, the bodies, the return types, or the helpers' callers in `enumerate_coauthors`. Karpathy Rule 3 (surgical changes) applies — every changed character must be either `pub(crate) ` insertion or whitespace untouched.

    In `src/git/mod.rs`: add the line `pub mod rewrite;` to the module list. Keep the existing alphabetical convention — the resulting order is `preflight`, `reader`, `rewrite`, `types`. Do not touch `pub fn open_repo`.

    Create `src/git/rewrite.rs` as a new file containing exactly one line: the comment `// Phase 2 rewrite engine — implemented in plan 02-02 and 02-03.` This empty stub is required so that `pub mod rewrite;` in mod.rs resolves and the crate compiles. Do not add any `use` statements, function declarations, or type definitions — those belong in the next plans.
  </action>
  <verify>
    <automated>cd /Users/instinct/Desktop/working/git-author-reformer &amp;&amp; cargo build --lib 2>&amp;1 | tail -20 &amp;&amp; cargo test --tests 2>&amp;1 | tail -10 &amp;&amp; grep -c 'pub(crate) fn strip_coauthor_prefix' src/git/reader.rs &amp;&amp; grep -c 'pub(crate) fn parse_coauthor_value' src/git/reader.rs &amp;&amp; grep -c 'pub mod rewrite' src/git/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pub(crate) fn strip_coauthor_prefix' src/git/reader.rs` returns `1`
    - Source assertion: `grep -c 'pub(crate) fn parse_coauthor_value' src/git/reader.rs` returns `1`
    - Source assertion: `grep -c '^fn strip_coauthor_prefix' src/git/reader.rs` returns `0` (no remaining private declaration)
    - Source assertion: `grep -c '^fn parse_coauthor_value' src/git/reader.rs` returns `0`
    - Source assertion: `grep -c '^pub mod rewrite;' src/git/mod.rs` returns `1`
    - Source assertion: `test -f src/git/rewrite.rs` exits 0
    - Behavior assertion: `cargo build --lib` succeeds with no errors
    - Behavior assertion: `cargo test --tests` shows all 15 Phase 1 tests still pass (4 preflight + 7 reader + 4 main_integration)
    - Behavior assertion: `cargo fmt -- --check` exits 0 (no formatting drift introduced)
  </acceptance_criteria>
  <done>Crate compiles with empty rewrite module stub, both trailer helpers are pub(crate), all 15 prior tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add three new fixture helpers to tests/common/mod.rs</name>
  <read_first>
    - tests/common/mod.rs (read all 30 lines — confirm existing helpers `create_fixture_repo` at line 6 and `add_commit_with_message` at line 24, note the `#![allow(dead_code)]` at line 1 and `use git2::{Repository, Signature, Time};` at line 3)
  </read_first>
  <files>tests/common/mod.rs</files>
  <action>
    Append three new public helper functions to `tests/common/mod.rs`, after the existing `add_commit_with_message` function (line 30). Use the same style as the existing helpers: no `Result` return type, panic via `.unwrap()` on every git2 call (these are test helpers; failure surfaces as a test panic).

    Helper 1: `create_branch(repo: &Repository, name: &str, target: &git2::Commit)` — calls `repo.branch(name, target, false).unwrap()`. The `false` is the `force` flag (don't overwrite an existing branch with the same name). Returns nothing.

    Helper 2: `add_merge_commit(repo: &Repository, name: &str, email: &str, message: &str, parent0: &git2::Commit, parent1: &git2::Commit)` — creates a merge commit with two parents in index order. Use `Signature::new(name, email, &git2::Time::new(1_000_002, 0)).unwrap()` for both the author and committer signature (consistent with the existing `add_commit_with_message` style — fixed deterministic timestamp). Use `parent0.tree().unwrap()` as the merge commit's tree (no tree merging — fixture commits do not change content). Call `repo.commit(Some("HEAD"), &sig, &sig, message, &tree, &[parent0, parent1]).unwrap()`. Critical: the parents slice MUST be `[parent0, parent1]` in this exact index order — Phase 2's merge parent ordering test depends on this.

    Helper 3: `create_annotated_tag(repo: &Repository, name: &str, target: &git2::Commit, message: &str)` — creates a TAG OBJECT (not a lightweight tag), using a fixed tagger identity. Build `Signature::new("Tagger", "tagger@example.com", &git2::Time::new(2_000_000, 0)).unwrap()`. Call `repo.tag(name, target.as_object(), &tagger, message, false).unwrap()`. The trailing `false` is the `force` flag (don't overwrite an existing tag). The fact that `repo.tag()` is called with a tagger and message — as opposed to `repo.tag_lightweight()` — is what makes this an annotated tag; the test for Plan 02-02's RENAME-04 verification depends on this being an annotated (not lightweight) tag.

    Do NOT touch the existing `create_fixture_repo` or `add_commit_with_message`. Do NOT add `pub use` re-exports. Do NOT add new imports beyond what's already at the top — `Repository`, `Signature`, and `Time` are already in scope; new helpers reference `git2::Time::new` directly which already resolves.
  </action>
  <verify>
    <automated>cd /Users/instinct/Desktop/working/git-author-reformer &amp;&amp; cargo build --tests 2>&amp;1 | tail -10 &amp;&amp; cargo test --tests 2>&amp;1 | tail -10 &amp;&amp; grep -c 'pub fn create_branch' tests/common/mod.rs &amp;&amp; grep -c 'pub fn add_merge_commit' tests/common/mod.rs &amp;&amp; grep -c 'pub fn create_annotated_tag' tests/common/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'pub fn create_branch' tests/common/mod.rs` returns `1`
    - Source assertion: `grep -c 'pub fn add_merge_commit' tests/common/mod.rs` returns `1`
    - Source assertion: `grep -c 'pub fn create_annotated_tag' tests/common/mod.rs` returns `1`
    - Source assertion: `grep -c 'repo.branch(name, target, false)' tests/common/mod.rs` returns `1`
    - Source assertion: `grep -c '\[parent0, parent1\]' tests/common/mod.rs` returns `1` (index-ordered parent slice)
    - Source assertion: `grep -c 'repo.tag(name, target.as_object()' tests/common/mod.rs` returns `1` (annotated tag, not lightweight)
    - Behavior assertion: `cargo build --tests` succeeds with no errors
    - Behavior assertion: `cargo test --tests` shows all 15 Phase 1 tests still pass — adding helpers must not break existing tests
    - Behavior assertion: `cargo fmt -- --check` exits 0
  </acceptance_criteria>
  <done>Three new helpers compile, follow the existing style (no Result, .unwrap() on failure, fixed deterministic timestamps), all prior tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced in this plan) | Plan 02-01 is pure scaffolding (visibility change + stub file + test helpers). No new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-V | Information Disclosure | `pub(crate)` widening of `strip_coauthor_prefix` / `parse_coauthor_value` | accept | Visibility scope is `pub(crate)` (not `pub`) — these helpers remain inaccessible to downstream consumers of the library; only modules inside `git-author-reformer` can call them. No information disclosure beyond the crate boundary. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in Plan 02-01. The existing `git2 = "0.21.0"` was audited in Phase 1; `tempfile = "3"` dev-dep was audited in Phase 1. No additions in this plan trigger the package legitimacy gate. |
</threat_model>

<verification>
- `cargo build --lib` and `cargo build --tests` both succeed
- `cargo test --tests` shows 15 passed, 0 failed (Phase 1 baseline preserved)
- `cargo fmt -- --check` exits 0
- `src/git/rewrite.rs` exists as a one-line stub (verified by `test -f` and `wc -l`)
- `pub(crate)` visibility is present on exactly the two named helpers; no other visibility shifts in reader.rs
</verification>

<success_criteria>
1. Crate compiles cleanly with the new empty `rewrite` module declared
2. The two trailer-parsing helpers in `reader.rs` are callable from `src/git/rewrite.rs` via `use crate::git::reader::{strip_coauthor_prefix, parse_coauthor_value};` — verified by visibility grep
3. All 15 Phase 1 tests continue to pass (4 preflight + 7 reader + 4 main_integration)
4. The three new fixture helpers exist and compile; their signatures match the spec exactly so Plans 02-02 and 02-03 can call them without modification
</success_criteria>

<output>
Create `.planning/phases/02-rewrite-engine/02-01-SUMMARY.md` when done.
</output>
