---
phase: 01-foundation-read-layer
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/main.rs
  - tests/main_integration_test.rs
autonomous: true
requirements:
  - CORE-02

must_haves:
  truths:
    - "Running the binary outside a git repository prints a descriptive error to stderr and exits with code 1"
    - "Running the binary inside a repo with `refs/stash` prints a descriptive error to stderr and exits with code 1"
    - "Running the binary inside a repo with linked worktrees prints a descriptive error to stderr and exits with code 1"
    - "Running the binary inside a clean repo passes preflight without error (exits successfully with placeholder output)"
    - "`--help` and `--version` continue to work and exit 0"
  artifacts:
    - path: "src/main.rs"
      provides: "Wires open_repo → check_stash → check_worktrees with stderr errors and exit codes"
      contains: "process::exit"
    - path: "tests/main_integration_test.rs"
      provides: "End-to-end CLI tests via std::process::Command"
      contains: "fn test_binary_exits_with_error_outside_git_repo"
  key_links:
    - from: "src/main.rs"
      to: "src/git/mod.rs"
      via: "git::open_repo()"
      pattern: "open_repo"
    - from: "src/main.rs"
      to: "src/git/preflight.rs"
      via: "git::preflight::check_stash / check_worktrees"
      pattern: "check_stash|check_worktrees"
---

<objective>
Wire the CLI entry point so that the binary, when launched, performs the open-repo + preflight sequence and exits with code 1 on any failure with the descriptive error printed to stderr. Add end-to-end CLI integration tests that invoke the compiled binary as a subprocess to verify the exit-code contract.

Purpose: Closes the CORE-02 loop (auto-detect repo from CWD; clear error and exit if not inside a git repo) and exercises SAFE-01/SAFE-02 from main.rs all the way to the user-visible exit code. Phase 1 success criterion 1 (binary outside repo exits 1 with descriptive error) and criterion 2 (stash/worktrees block at startup) are verified here as integration tests.

Output: Updated `src/main.rs` that calls open_repo → check_stash → check_worktrees, prints any AppError Display message to stderr, and uses `std::process::exit(1)` on error. Plus `tests/main_integration_test.rs` with four assertions invoking the binary via `cargo run -- ` or `std::process::Command` on the cargo-built binary.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-read-layer/01-RESEARCH.md
@.planning/phases/01-foundation-read-layer/01-PATTERNS.md
@.planning/phases/01-foundation-read-layer/01-01-SUMMARY.md
@.planning/phases/01-foundation-read-layer/01-02-SUMMARY.md
@src/main.rs
@src/git/mod.rs
@src/git/preflight.rs
@src/error.rs
@tests/common/mod.rs
@CLAUDE.md

<interfaces>
<!-- From src/git/mod.rs (Plan 01) -->
- pub fn open_repo() -> Result<git2::Repository, crate::error::AppError>

<!-- From src/git/preflight.rs (Plan 02) -->
- pub fn check_stash(repo: &git2::Repository) -> Result<(), crate::error::AppError>
- pub fn check_worktrees(repo: &git2::Repository) -> Result<(), crate::error::AppError>

<!-- From src/error.rs (Plan 01) -->
- AppError implements Display via thiserror; printing `{e}` produces the user-facing message
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire main.rs preflight chain with stderr + exit(1)</name>
  <files>src/main.rs</files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-RESEARCH.md (Architecture diagram lines 121-148)
    - src/main.rs (current scaffolding from Plan 01)
    - src/git/mod.rs (open_repo signature)
    - src/git/preflight.rs (check_stash, check_worktrees signatures)
    - src/error.rs (AppError Display messages)
  </read_first>
  <action>
    Update `src/main.rs` to add a helper `fn run() -> Result<(), error::AppError>` that calls `let repo = git::open_repo()?;`, then `git::preflight::check_stash(&repo)?;`, then `git::preflight::check_worktrees(&repo)?;`, then returns `Ok(())` with a placeholder print (e.g., `println!("git-author-reformer: preflight passed");`) indicating success without doing further work — the TUI is Phase 3. The `fn main()` body: parse the clap CLI struct, then `if let Err(e) = run() { eprintln!("error: {e}"); std::process::exit(1); }`. Keep the clap derive struct (`--version` / `--help`) untouched from Plan 01. Do NOT add subcommands or flags beyond what Plan 01 created. The error is printed via Display (`{e}`) — thiserror gives a human-readable message per AppError variant.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5; cargo run -- --help 2>&1 | head -3; cargo run -- --version 2>&1 | head -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo run -- --help` exits 0
    - `cargo run -- --version` exits 0
    - `grep -c 'process::exit(1)' src/main.rs` outputs at least `1`
    - `grep -c 'open_repo' src/main.rs` outputs at least `1`
    - `grep -c 'check_stash' src/main.rs` outputs at least `1`
    - `grep -c 'check_worktrees' src/main.rs` outputs at least `1`
    - `grep -c 'eprintln!' src/main.rs` outputs at least `1` (errors go to stderr, not stdout)
    - `cargo clippy --bin git-author-reformer -- -D warnings` exits 0
  </acceptance_criteria>
  <done>main.rs wires open_repo → check_stash → check_worktrees with stderr errors and exit(1) on failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end CLI integration tests</name>
  <files>tests/main_integration_test.rs</files>
  <read_first>
    - tests/common/mod.rs (fixture helpers)
    - src/main.rs (the wiring from Task 1)
    - src/error.rs (Display messages — tests assert on parts of them)
  </read_first>
  <behavior>
    Four `#[test]` functions, each spawns the compiled binary as a subprocess via `std::process::Command::new(env!("CARGO_BIN_EXE_git-author-reformer"))` with `.current_dir(...)` pointing at a fixture directory, and inspects exit status + stderr.

    1. `test_binary_exits_with_error_outside_git_repo`: temp dir that is NOT a git repo → exit code is NOT 0 (assert `!status.success()`); stderr contains the literal substring `Not inside a git repository`.
    2. `test_binary_blocks_when_stash_ref_exists`: fixture repo (via `common::create_fixture_repo`) with a synthetic `refs/stash` reference pointing at HEAD → `!status.success()`; stderr contains the literal substring `Stash entries detected`.
    3. `test_binary_blocks_when_linked_worktree_exists`: fixture repo + one linked worktree (via `repo.worktree("test-wt", &sibling_temp_path, None)`) → `!status.success()`; stderr contains the literal substring `Linked worktrees detected`.
    4. `test_binary_passes_preflight_on_clean_repo`: fixture repo, no stash, no worktree → `status.success()`; exit code 0.
  </behavior>
  <action>
    Create `tests/main_integration_test.rs`. Top: `mod common;`. Use `std::process::Command::new(env!("CARGO_BIN_EXE_git-author-reformer"))` — this Cargo env var is set automatically for integration tests and points to the compiled binary in `target/`. For each test, create the appropriate fixture state in a temp directory, then set `.current_dir(<repo_path>)` on the Command and call `.output()`. Assert on `output.status.success()` and on `String::from_utf8_lossy(&output.stderr).contains("<exact substring>")`. Use the EXACT Display strings from `src/error.rs`: `Not inside a git repository`, `Stash entries detected`, `Linked worktrees detected`. For test 1, the temp dir must be writable but contain no `.git` (just `TempDir::new()` is enough — must NOT be inside the project's own .git, hence the temp dir). For test 3, the linked worktree path must be a separate `TempDir` (sibling, not subdirectory of the main repo). Note: these are integration tests — they invoke the binary as a subprocess, so they exercise the full main → run() → preflight chain.
  </action>
  <verify>
    <automated>cargo test --test main_integration_test 2>&1 | tail -10; cargo test --test main_integration_test 2>&1 | grep 'test result:'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test main_integration_test` exits 0
    - `cargo test --test main_integration_test 2>&1 | grep 'test result:'` shows a line containing `4 passed; 0 failed`
    - `cargo test 2>&1 | grep 'test result:'` shows ALL test binaries passing (preflight + reader + main_integration)
    - `grep -c 'CARGO_BIN_EXE_git-author-reformer' tests/main_integration_test.rs` outputs at least `4` (one per test that spawns the binary)
    - `grep -c 'Not inside a git repository' tests/main_integration_test.rs` outputs at least `1`
    - `grep -c 'Stash entries detected' tests/main_integration_test.rs` outputs at least `1`
    - `grep -c 'Linked worktrees detected' tests/main_integration_test.rs` outputs at least `1`
    - `cargo clippy --tests -- -D warnings` exits 0
  </acceptance_criteria>
  <done>End-to-end CLI tests pass. Phase 1 success criteria 1 and 2 verified at the binary level (not just library level).</done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification — full test suite + clippy + build</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-01-SUMMARY.md
    - .planning/phases/01-foundation-read-layer/01-02-SUMMARY.md
    - .planning/phases/01-foundation-read-layer/01-03-SUMMARY.md
    - .planning/ROADMAP.md (Phase 1 success criteria)
  </read_first>
  <action>
    Run the complete phase verification commands in sequence. If any fails, STOP and surface the failure — do not attempt to "fix" earlier plans from here. Expected commands:
    1. `cargo build --release` (verifies release profile builds — vendored libgit2 + lto + strip)
    2. `cargo test` (all test binaries: preflight, reader, main_integration — total 15 tests across the three binaries: 4 + 7 + 4)
    3. `cargo clippy --all-targets -- -D warnings`
    4. `cargo fmt -- --check` (warns only if formatting drifted from rustfmt defaults)
    Confirm the Phase 1 success criteria are observable:
    - Criterion 1: `test_binary_exits_with_error_outside_git_repo` passes
    - Criterion 2: `test_binary_blocks_when_stash_ref_exists` and `test_binary_blocks_when_linked_worktree_exists` pass
    - Criterion 3: `test_enumerate_authors_counts_and_sorts_descending` passes
    - Criterion 4: `test_enumerate_coauthors_case_insensitive_dedup` passes
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -3 && cargo test 2>&1 | tail -15 && cargo test 2>&1 | grep 'test result:' && cargo clippy --all-targets -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` exits 0
    - `cargo test` exits 0 — output contains `test result:` lines totaling 15 passed across the three test binaries (4 preflight + 7 reader + 4 main_integration)
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo fmt -- --check` exits 0 OR the executor runs `cargo fmt` and notes formatting was applied
    - The compiled binary at `target/release/git-author-reformer` exists (`test -f target/release/git-author-reformer`)
  </acceptance_criteria>
  <done>Phase 1 success criteria 1-4 fully verified end-to-end. Project builds in release mode. No clippy warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell environment → binary launch | User invokes binary from arbitrary CWD with arbitrary env vars |
| Binary stderr → terminal | Error messages are surfaced to the user via stderr; must be safe to print |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-IN | Information disclosure | Error messages on stderr | accept | AppError Display strings are static templates with controlled placeholders (path string, worktree names) — no secrets in scope for Phase 1 |
| T-01-EX | Elevation of privilege | Binary auto-detects repo via `open_from_env` (honors `$GIT_DIR`) | accept | Standard git behavior; user runs the binary in their own shell, no privilege boundary crossed |
| T-01-RX | Tampering | Exit code expectation by shell scripts wrapping the binary | mitigate | `process::exit(1)` is used uniformly on any AppError; exit-code contract documented and tested in main_integration_test |
</threat_model>

<verification>
- Binary outside git repo exits 1 with `error: Not inside a git repository: ...` on stderr
- Binary with stash ref exits 1 with `error: Stash entries detected. ...` on stderr
- Binary with linked worktree exits 1 with `error: Linked worktrees detected: ...` on stderr
- Binary in clean repo exits 0 with placeholder output (TUI is Phase 3)
- `cargo test` reports all 15 tests passing (4 + 7 + 4) across three test binaries
- `cargo build --release` produces `target/release/git-author-reformer` with vendored libgit2 statically linked
- `cargo clippy --all-targets -- -D warnings` is clean
</verification>

<success_criteria>
- Phase 1 success criterion 1 verified: binary outside a git repo exits 1 with descriptive stderr
- Phase 1 success criterion 2 verified: binary blocks on stash + linked worktrees
- Phase 1 success criteria 3 and 4 covered by Plan 03's reader tests, regression-checked here by full `cargo test`
- CORE-02 fully implemented (open_repo + exit code wiring complete)
- CORE-03 satisfied (only git2 used; no git binary invoked at runtime — verified by absence of `Command::new("git")` anywhere in src/)
- SAFE-01, SAFE-02 wired through main and verified at the binary level
- Release build succeeds; binary artifact exists
</success_criteria>

<output>
Create `.planning/phases/01-foundation-read-layer/01-04-SUMMARY.md` documenting: main.rs wiring chain, end-to-end test assertions, the exact stderr strings used, and a confirmation table mapping each Phase 1 success criterion to the test that verifies it.
</output>
