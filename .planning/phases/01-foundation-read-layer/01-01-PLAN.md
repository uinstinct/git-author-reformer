---
phase: 01-foundation-read-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/error.rs
  - src/main.rs
  - src/git/mod.rs
  - src/git/types.rs
  - src/git/reader.rs
  - src/git/preflight.rs
  - tests/common/mod.rs
autonomous: false
requirements:
  - CORE-02
  - CORE-03

must_haves:
  truths:
    - "Project compiles with `cargo build` on a clean checkout"
    - "Project compiles tests with `cargo test --no-run` without errors"
    - "Binary defines `--version` and `--help` via clap derive"
    - "`AppError` enum is defined with four variants: NotARepo(String), StashDetected, WorktreesDetected(String), Git(#[from] git2::Error)"
    - "`AuthorIdentity` and `CoAuthorEntry` structs are defined with name: String, email: String, commit_count: usize fields"
    - "`open_repo()` calls `Repository::open_from_env()` and maps errors to `AppError::NotARepo`"
    - "`tests/common/mod.rs` provides `create_fixture_repo()` and `add_commit_with_message()` helpers using git2 (no git binary calls)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Crate manifest with vendored libgit2 deps"
      contains: 'git2 = { version = "0.21", default-features = false, features = ["vendored-libgit2"] }'
    - path: "src/error.rs"
      provides: "AppError enum"
      contains: "enum AppError"
    - path: "src/git/types.rs"
      provides: "AuthorIdentity, CoAuthorEntry models"
      contains: "AuthorIdentity"
    - path: "src/git/mod.rs"
      provides: "Module facade and open_repo()"
      contains: "open_from_env"
    - path: "src/git/reader.rs"
      provides: "Stub module with enumerate_authors/enumerate_coauthors signatures"
      contains: "pub fn enumerate_authors"
    - path: "src/git/preflight.rs"
      provides: "Stub module with check_stash/check_worktrees signatures"
      contains: "pub fn check_stash"
    - path: "tests/common/mod.rs"
      provides: "Shared test fixtures"
      contains: "create_fixture_repo"
  key_links:
    - from: "src/git/mod.rs"
      to: "src/error.rs"
      via: "use crate::error::AppError"
      pattern: "use crate::error"
    - from: "src/main.rs"
      to: "src/git/mod.rs"
      via: "module include"
      pattern: "mod git"
---

<objective>
Establish the project scaffolding: Cargo manifest with vendored libgit2 dependencies, the `AppError` error type, shared data models (`AuthorIdentity`, `CoAuthorEntry`), the `open_repo()` function, stub modules for `reader` and `preflight` that downstream plans will fill in via TDD, and the shared test fixture helpers under `tests/common/mod.rs`.

Purpose: Create the compilation unit and shared contracts so Plan 02 (preflight TDD) and Plan 03 (reader TDD) can run in parallel against the same baseline. Implements CORE-03 (vendored git2, no SSH/HTTPS features, no git binary) and the open-repo half of CORE-02. The exit-on-non-repo wiring (CORE-02 exit code path) is completed in Plan 04 once preflight is implemented.

Output: A buildable Rust project with stub functions (`todo!()` bodies) for reader and preflight so the crate compiles before TDD plans fill them in. `cargo build` succeeds; `cargo test --no-run` succeeds.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-read-layer/01-CONTEXT.md
@.planning/phases/01-foundation-read-layer/01-RESEARCH.md
@.planning/phases/01-foundation-read-layer/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- All identifiers and signatures downstream plans must implement against.   -->
<!-- Sourced from 01-RESEARCH.md Patterns 1-7. Do NOT alter these signatures —  -->
<!-- Plan 02 and Plan 03 are written against them.                              -->

src/error.rs exposes:
- pub enum AppError with variants:
    NotARepo(String)
    StashDetected
    WorktreesDetected(String)
    Git(#[from] git2::Error)
  Derives: Debug, thiserror::Error
  Display messages per Pattern 7 (01-RESEARCH.md lines 357-370)

src/git/types.rs exposes:
- pub struct AuthorIdentity { pub name: String, pub email: String, pub commit_count: usize }
  Derives: Clone, Debug, PartialEq, Eq
- pub struct CoAuthorEntry { pub name: String, pub email: String, pub commit_count: usize }
  Derives: Clone, Debug, PartialEq, Eq

src/git/mod.rs exposes:
- pub mod types;
- pub mod reader;
- pub mod preflight;
- pub fn open_repo() -> Result<git2::Repository, crate::error::AppError>

src/git/reader.rs exposes (stub signatures, bodies are todo!()):
- pub fn enumerate_authors(repo: &git2::Repository) -> Result<Vec<crate::git::types::AuthorIdentity>, crate::error::AppError>
- pub fn enumerate_coauthors(repo: &git2::Repository) -> Result<Vec<crate::git::types::CoAuthorEntry>, crate::error::AppError>

src/git/preflight.rs exposes (stub signatures, bodies are todo!()):
- pub fn check_stash(repo: &git2::Repository) -> Result<(), crate::error::AppError>
- pub fn check_worktrees(repo: &git2::Repository) -> Result<(), crate::error::AppError>

tests/common/mod.rs exposes (must use Rust integration-test convention — `common` subdirectory is NOT compiled as its own test binary, while a bare `tests/fixtures.rs` would be):
- pub fn create_fixture_repo() -> (tempfile::TempDir, git2::Repository)
- pub fn add_commit_with_message(repo: &git2::Repository, name: &str, email: &str, message: &str)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (cargo crates)</name>
  <files>Cargo.toml</files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-RESEARCH.md (Package Legitimacy Audit section, lines 99-114)
  </read_first>
  <action>
    BLOCKING-HUMAN CHECKPOINT. Per the Package Legitimacy Audit in 01-RESEARCH.md, slopcheck was unavailable at research time, so all four crates this plan installs are tagged [ASSUMED]. Pause execution and prompt the user to verify each crate on crates.io BEFORE Cargo.toml is created in Task 2. Threat: T-01-SC (Tampering / package supply chain). Steps the user must perform: (1) Visit https://crates.io/crates/git2 and confirm owner `rust-lang`, recent updates, downloads in millions. (2) Visit https://crates.io/crates/thiserror and confirm owner `dtolnay`, recent updates, downloads in tens of millions. (3) Visit https://crates.io/crates/clap and confirm owner `clap-rs`/`epage`, recent updates, downloads in millions. (4) Visit https://crates.io/crates/tempfile and confirm recent updates, downloads in tens of millions. (5) Confirm none of the crates are yanked at the pinned major versions (git2 0.21, thiserror 2, clap 4.6, tempfile 3). Resume signal: user types "approved" or describes issues. If any crate fails verification, STOP — do not proceed. Auto-approval is NOT permitted for this gate (`workflow.auto_advance` is ignored for blocking-human checkpoints per the Package Legitimacy Gate protocol).
  </action>
  <verify>
    <human-check>User has typed "approved" (or equivalent) after confirming crates.io verification of all four crates per the action steps above.</human-check>
  </verify>
  <acceptance_criteria>
    - User has explicitly approved by typing "approved" or equivalent
    - No crate showed yanked status at the pinned major version
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with Cargo.toml creation, or describe issues found</resume-signal>
  <done>User approval received; safe to proceed with Cargo.toml.</done>
</task>

<task type="auto">
  <name>Task 2: Create Cargo.toml with vendored libgit2 dependencies</name>
  <files>Cargo.toml</files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-RESEARCH.md (Cargo.toml Dependencies section, lines 75-95)
    - .planning/phases/01-foundation-read-layer/01-PATTERNS.md (Cargo.toml row in Pattern Assignments)
  </read_first>
  <action>
    Create Cargo.toml at the repo root with: package name `git-author-reformer`, version `0.1.0`, edition `2021`, rust-version `1.74`. Under `[dependencies]` add `git2 = { version = "0.21", default-features = false, features = ["vendored-libgit2"] }`, `thiserror = "2"`, and `clap = { version = "4.6", features = ["derive"] }`. Under `[dev-dependencies]` add `tempfile = "3"`. Under `[profile.release]` set `strip = true`, `lto = true`, `codegen-units = 1`. The `default-features = false` on git2 is REQUIRED per CORE-03 — must drop the `ssh` and `https` features which would pull OpenSSL and break the musl static-link target later (per Pitfall 5 reference in RESEARCH.md). Do NOT add `vendored-openssl` — with ssh/https disabled, OpenSSL is not pulled in.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && grep -c 'vendored-libgit2' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 (vendored libgit2 compiles successfully)
    - `grep -c 'vendored-libgit2' Cargo.toml` outputs `1`
    - `grep -c 'default-features = false' Cargo.toml` outputs `1`
    - `grep -E '"ssh"|"https"' Cargo.toml | grep -v '^#'` outputs nothing (no ssh/https features enabled)
    - `Cargo.lock` is generated (`test -f Cargo.lock`)
  </acceptance_criteria>
  <done>Cargo.toml exists with the exact dependency block; `cargo build` succeeds against a minimal `src/main.rs` (created in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Create AppError, types, and module skeleton</name>
  <files>src/error.rs, src/git/mod.rs, src/git/types.rs, src/git/reader.rs, src/git/preflight.rs, src/main.rs</files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-RESEARCH.md (Pattern 1 lines 168-182, Pattern 7 lines 353-371, Architecture diagram lines 121-148)
    - .planning/phases/01-foundation-read-layer/01-PATTERNS.md (rows for src/error.rs, src/git/mod.rs, src/git/types.rs, src/git/reader.rs, src/git/preflight.rs)
  </read_first>
  <action>
    Create six source files matching the `<interfaces>` block in the plan context exactly:
    - `src/error.rs`: `AppError` enum with `#[derive(Debug, thiserror::Error)]` and the four variants (NotARepo(String), StashDetected, WorktreesDetected(String), Git(#[from] git2::Error)) using the Display messages from Pattern 7.
    - `src/git/types.rs`: public `AuthorIdentity` and `CoAuthorEntry` structs, each with `name: String`, `email: String`, `commit_count: usize`, deriving `Clone, Debug, PartialEq, Eq`.
    - `src/git/mod.rs`: `pub mod types; pub mod reader; pub mod preflight;` plus `pub fn open_repo() -> Result<git2::Repository, crate::error::AppError>` calling `git2::Repository::open_from_env()` and mapping the git2 error via `|e| crate::error::AppError::NotARepo(e.to_string())`.
    - `src/git/reader.rs`: stub `pub fn enumerate_authors(repo: &git2::Repository) -> Result<Vec<crate::git::types::AuthorIdentity>, crate::error::AppError>` and `pub fn enumerate_coauthors(repo: &git2::Repository) -> Result<Vec<crate::git::types::CoAuthorEntry>, crate::error::AppError>`. Bodies: `todo!("implemented in Plan 03")`. Mark the `repo` parameter with `_repo` or add `#[allow(dead_code, unused_variables)]` to silence stub warnings. The stub bodies will be replaced in Plan 03 via TDD — do NOT implement here.
    - `src/git/preflight.rs`: stub `pub fn check_stash(repo: &git2::Repository) -> Result<(), crate::error::AppError>` and `pub fn check_worktrees(repo: &git2::Repository) -> Result<(), crate::error::AppError>`. Bodies: `todo!("implemented in Plan 02")`. Same warning-suppression treatment. Stubs replaced in Plan 02 via TDD.
    - `src/main.rs`: declare `mod error;` and `mod git;`. Use `clap` derive macro to define a CLI struct with `#[command(name = "git-author-reformer", version)]` and an empty (no flags or subcommands) struct body so `--version` and `--help` work. `fn main()` parses args (`Cli::parse()`) and returns; the open_repo/preflight wiring is added in Plan 04 — do NOT wire it here.
    Match the existing code style (no `use` clutter; prefer fully-qualified paths matching the `<interfaces>` block to avoid bikeshedding in downstream plans).
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -10 && cargo run -- --help 2>&1 | head -5 && cargo run -- --version 2>&1 | head -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo run -- --help` exits 0 and prints a clap-generated help message
    - `cargo run -- --version` exits 0 and prints `git-author-reformer 0.1.0`
    - `grep -c 'enum AppError' src/error.rs` outputs `1`
    - `grep -c 'NotARepo' src/error.rs` outputs at least `1`
    - `grep -c 'StashDetected' src/error.rs` outputs at least `1`
    - `grep -c 'WorktreesDetected' src/error.rs` outputs at least `1`
    - `grep -c 'pub struct AuthorIdentity' src/git/types.rs` outputs `1`
    - `grep -c 'pub struct CoAuthorEntry' src/git/types.rs` outputs `1`
    - `grep -c 'open_from_env' src/git/mod.rs` outputs `1`
    - `grep -c 'pub fn enumerate_authors' src/git/reader.rs` outputs `1`
    - `grep -c 'pub fn enumerate_coauthors' src/git/reader.rs` outputs `1`
    - `grep -c 'pub fn check_stash' src/git/preflight.rs` outputs `1`
    - `grep -c 'pub fn check_worktrees' src/git/preflight.rs` outputs `1`
    - Running `cargo test --no-run` exits 0 (tests compile against stub signatures)
  </acceptance_criteria>
  <done>All six source files exist, crate builds, --help and --version work, stub signatures match the `<interfaces>` block exactly.</done>
</task>

<task type="auto">
  <name>Task 4: Create shared test fixtures under tests/common/</name>
  <files>tests/common/mod.rs</files>
  <read_first>
    - .planning/phases/01-foundation-read-layer/01-RESEARCH.md ("In-Process Test Fixture" code block, lines 430-460)
    - .planning/phases/01-foundation-read-layer/01-PATTERNS.md (tests/fixtures.rs row — note this plan uses `tests/common/mod.rs` instead, the Rust-idiomatic location for shared integration-test helpers)
  </read_first>
  <action>
    Create `tests/common/mod.rs` (NOT `tests/fixtures.rs` — every bare .rs under tests/ is compiled as its own test binary, and bare files cannot share code; the `common` subdirectory convention is the standard fix). Export two public helpers:
    - `pub fn create_fixture_repo() -> (tempfile::TempDir, git2::Repository)` — initializes a temp dir with `tempfile::TempDir::new()`, calls `git2::Repository::init(dir.path())`, creates an initial commit authored by `Alice <alice@example.com>` at fixed `git2::Time::new(1_000_000, 0)` using an empty tree (`index.write_tree()`), and returns the owned TempDir (so the caller controls cleanup) plus the Repository.
    - `pub fn add_commit_with_message(repo: &git2::Repository, name: &str, email: &str, message: &str)` — peels HEAD to a commit, uses its tree, and creates a new commit with the given signature and message at fixed `git2::Time::new(1_000_001, 0)`, parented on HEAD.
    Downstream test files will declare `mod common;` to access these — the `mod.rs` inside `tests/common/` is the Rust convention that prevents `tests/common/mod.rs` from being treated as a standalone integration test binary. Do not add any `#[test]` functions to this file.
  </action>
  <verify>
    <automated>test -f tests/common/mod.rs && cargo test --no-run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/common/mod.rs` exists
    - `grep -c 'pub fn create_fixture_repo' tests/common/mod.rs` outputs `1`
    - `grep -c 'pub fn add_commit_with_message' tests/common/mod.rs` outputs `1`
    - `grep -c '#\[test\]' tests/common/mod.rs` outputs `0` (no test functions inside the helper module)
    - `cargo test --no-run` exits 0 with no errors
    - `cargo build --tests` exits 0
  </acceptance_criteria>
  <done>Shared fixtures compile and are importable by future test files via `mod common;`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → local Cargo deps | External package supply chain crosses into the build |
| Project source → vendored libgit2 C source | C compilation step ingests source from the git2 crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Cargo dependencies (git2, thiserror, clap, tempfile) | mitigate | Blocking-human checkpoint (Task 1) verifies each crate on crates.io before Cargo.toml install — all four are `[ASSUMED]` per RESEARCH.md Package Legitimacy Audit |
| T-01-CC | Tampering | vendored libgit2 C compilation | accept | git2 crate is owned by `rust-lang` org; vendored source is published with the crate. Risk accepted in exchange for static-linking benefit. |
| T-01-IN | Information disclosure | clap-generated --help / --version | accept | Output is `git-author-reformer <version>` and standard clap help; reveals nothing sensitive |
</threat_model>

<verification>
- `cargo build` exits 0 from a clean state (vendored libgit2 compiles)
- `cargo test --no-run` exits 0 (all stubs compile against integration tests)
- `cargo run -- --help` and `cargo run -- --version` both exit 0
- All six source files contain the exact identifiers declared in the `<interfaces>` block
- `tests/common/mod.rs` exists; no bare `tests/fixtures.rs` was created
- No SSH/HTTPS features enabled on git2 (`grep -E '"ssh"|"https"' Cargo.toml | grep -v '^#'` returns empty)
</verification>

<success_criteria>
- Cargo manifest pins git2 with `vendored-libgit2`, no ssh/https; thiserror 2; clap 4.6 with derive; tempfile 3 dev-dep
- AppError enum has all four required variants with correct Display strings
- AuthorIdentity, CoAuthorEntry structs exist with name/email/commit_count fields and Clone+Debug+PartialEq+Eq derives
- `open_repo()` calls `Repository::open_from_env()` and maps the error
- Stub modules for reader and preflight compile with todo!() bodies — Plan 02/03 will replace them via TDD
- `tests/common/mod.rs` provides create_fixture_repo() and add_commit_with_message() with no #[test] functions
- Binary responds to --help and --version
- CORE-03 satisfied (vendored libgit2, no git binary, no ssh/https); CORE-02 partially satisfied (open_repo returns AppError::NotARepo; exit-code wiring lands in Plan 04)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-read-layer/01-01-SUMMARY.md` when done, documenting: exact Cargo.toml dependency block written, AppError variant list, types created, stub signatures (so Plan 02/03 can read this file and confirm they're implementing the same contract), and the `tests/common/` convention used.
</output>
