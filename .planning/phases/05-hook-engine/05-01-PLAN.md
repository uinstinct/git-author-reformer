---
phase: 05-hook-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/error.rs
  - src/hook/mod.rs
  - src/hook/path.rs
  - src/hook/parse.rs
  - src/hook/render.rs
  - src/hook/write.rs
autonomous: true
requirements: [HOOK-04, HOOK-06, HOOK-12]
must_haves:
  truths:
    - "Crate compiles with new `hook` module declared in `src/lib.rs`"
    - "`crate::hook::install_strip`, `crate::hook::remove_strip`, `crate::hook::read_strip_list` exist with stub bodies and the public signatures defined in RESEARCH.md"
    - "`HookState`, `AddResult`, `RemoveResult` enums are publicly exported from `crate::hook`"
    - "`AppError::HookExists(PathBuf)` exists and round-trips through `?` like the existing `NotARepo` variant"
    - "`crate::hook::path::commit_msg_hook_path(&Repository) -> PathBuf` returns `repo.path().join(\"hooks\").join(\"commit-msg\")`"
  artifacts:
    - path: "src/hook/mod.rs"
      provides: "Module index, public API signatures, public result enums"
      contains: "pub fn install_strip"
    - path: "src/hook/path.rs"
      provides: "commit_msg_hook_path resolver"
      contains: "repo.path().join"
    - path: "src/hook/parse.rs"
      provides: "Stub for marker detection / strip-list extraction (implemented in Plan 02)"
    - path: "src/hook/render.rs"
      provides: "Stub for hook template renderer (implemented in Plan 03)"
    - path: "src/hook/write.rs"
      provides: "Stub for atomic write + delete (implemented in Plan 04)"
    - path: "src/error.rs"
      provides: "Extended AppError"
      contains: "HookExists"
    - path: "src/lib.rs"
      provides: "Hook module wired into crate root"
      contains: "pub mod hook"
  key_links:
    - from: "src/hook/mod.rs"
      to: "src/hook/{path,parse,render,write}.rs"
      via: "sub-module declarations"
      pattern: "pub mod (path|parse|render|write)"
    - from: "src/hook/path.rs"
      to: "git2::Repository::path"
      via: "read-only API call"
      pattern: "repo\\.path\\(\\)"
---

<objective>
Scaffold the hook module skeleton so subsequent TDD plans (parser, renderer, integration) have stable contracts and compile-passing entry points to red-green against.

Purpose: Plans 02–05 implement specific behaviors against fixed signatures. Defining all public types and stub bodies up-front eliminates the "scavenger hunt" anti-pattern and lets the parser and renderer plans run in parallel in Wave 2.

Output: Empty-but-compiling hook engine (`cargo check` passes, `cargo test` passes existing suite). Public API surface frozen for downstream plans.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hook-engine/05-RESEARCH.md
@.planning/phases/05-hook-engine/05-PATTERNS.md
@src/lib.rs
@src/error.rs
@src/git/mod.rs

<interfaces>
<!-- Existing exports the hook module must integrate with — extracted from codebase. -->

From src/error.rs:
```rust
#[derive(Debug, Error)]
pub enum AppError {
    NotARepo(String),
    StashDetected,
    WorktreesDetected(String),
    Git(#[from] git2::Error),
    NonUtf8Message(git2::Oid),
    NotATerminal,
    Io(#[from] std::io::Error),
}
```

From src/git/mod.rs (the module-layout analog):
```rust
pub mod preflight;
pub mod reader;
pub mod rewrite;
pub mod scan;
pub mod types;

pub fn open_repo() -> Result<git2::Repository, crate::error::AppError> { ... }
```

From src/lib.rs (current):
```rust
pub mod error;
pub mod git;
pub mod tui;
```

Hook module public API to declare (RESEARCH §Architecture Patterns):
```rust
pub enum HookState { Absent, Managed { emails: Vec<String> }, NotToolManaged(std::path::PathBuf) }
pub enum AddResult { Installed { count: usize }, AlreadyStripped }
pub enum RemoveResult { Updated { remaining: usize }, HookDeleted, NotFound }
pub fn install_strip(repo: &git2::Repository, email: &str) -> Result<AddResult, crate::error::AppError>;
pub fn remove_strip(repo: &git2::Repository, email: &str) -> Result<RemoveResult, crate::error::AppError>;
pub fn read_strip_list(repo: &git2::Repository) -> Result<HookState, crate::error::AppError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HookExists variant to AppError</name>
  <files>src/error.rs</files>
  <read_first>
    - src/error.rs (entire file, 25 lines)
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Pitfall 6
    - .planning/phases/05-hook-engine/05-PATTERNS.md "src/error.rs (modified — error type)"
  </read_first>
  <action>
    Append exactly ONE new variant to the `AppError` enum in `src/error.rs`: `HookExists(std::path::PathBuf)` with `#[error("Existing commit-msg hook at {0:?} is not managed by git-author-reformer.\nRemove or rename the file, then re-run.")]`.

    Do not touch the existing "Terminal I/O error" wording on the `Io` variant — RESEARCH.md §Pitfall 6 and PATTERNS.md flag it as a known follow-up but explicitly out of scope here (Karpathy Rule 3: Surgical Changes). Do not add `HookIoError` or other variants; the existing `Io(#[from] std::io::Error)` already covers all `fs::*` failures via `?`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` exits 0
    - `grep -c 'HookExists' src/error.rs` returns >= 1
    - `grep -v '^#' src/error.rs | grep -c 'HookIoError'` returns 0 (no bloat variants added)
    - `git diff src/error.rs` shows only additions (no edits to existing variant messages)
  </acceptance_criteria>
  <done>AppError carries a `HookExists(PathBuf)` variant; the existing `Io` `#[from] std::io::Error` still propagates fs errors via `?`.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/hook/ module skeleton with stub implementations</name>
  <files>
    src/hook/mod.rs,
    src/hook/path.rs,
    src/hook/parse.rs,
    src/hook/render.rs,
    src/hook/write.rs
  </files>
  <read_first>
    - src/git/mod.rs (entire file, 9 lines — module-layout analog)
    - src/git/preflight.rs (entire file — single-purpose helper analog)
    - .planning/phases/05-hook-engine/05-PATTERNS.md (whole file — every sub-file pattern is mapped here)
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Architecture Patterns + §Code Examples
  </read_first>
  <action>
    Create `src/hook/mod.rs` declaring sub-modules in alphabetical order (`pub mod parse; pub mod path; pub mod render; pub mod write;`) and exposing the three public functions plus the three public result enums listed in the interfaces block above.

    The three public functions (`install_strip`, `remove_strip`, `read_strip_list`) MUST have real signatures returning `Result<_, crate::error::AppError>` but stub bodies — return `unimplemented!("Plan 04 wires this")` for each. Stubs let the crate compile while leaving behavior under TDD control in later plans.

    `src/hook/path.rs` is NOT a stub — implement `pub(crate) fn commit_msg_hook_path(repo: &git2::Repository) -> PathBuf` per RESEARCH §Code Examples "Resolve the hook path": `repo.path().join("hooks").join("commit-msg")`. This is a one-liner and has no behavior to TDD beyond "what `Repository::path()` returns".

    `src/hook/parse.rs`, `src/hook/render.rs`, `src/hook/write.rs` are intentional stub files — each containing only a doc comment (`//! Implemented in Plan NN`) so the `pub mod` declarations in `mod.rs` resolve. Plans 02/03/04 fill them in.

    Module imports MUST use the patterns from `src/git/mod.rs` (e.g., `crate::error::AppError`). DO NOT call `crate::git::preflight::check_stash` or `check_worktrees` from any hook-module function — HOOK-12 requires hook ops to bypass preflight.
  </action>
  <verify>
    <automated>cargo check --tests</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --tests` exits 0
    - `ls src/hook/{mod,path,parse,render,write}.rs` lists all 5 files
    - `grep -c 'pub mod ' src/hook/mod.rs` returns 4 (parse, path, render, write)
    - `grep -c 'pub fn install_strip' src/hook/mod.rs` returns 1
    - `grep -c 'pub fn remove_strip' src/hook/mod.rs` returns 1
    - `grep -c 'pub fn read_strip_list' src/hook/mod.rs` returns 1
    - `grep -c 'pub enum HookState' src/hook/mod.rs` returns 1
    - `grep -c 'repo.path()' src/hook/path.rs` returns 1
    - `grep -v '^#' src/hook/mod.rs | grep -E 'check_stash|check_worktrees'` returns nothing (HOOK-12)
  </acceptance_criteria>
  <done>The crate compiles with the new hook module. Public API surface (functions + result enums) is frozen for downstream plans. No preflight calls present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire hook module into crate root</name>
  <files>src/lib.rs</files>
  <read_first>
    - src/lib.rs (entire file, 3 lines)
    - .planning/phases/05-hook-engine/05-PATTERNS.md "src/lib.rs (modified — crate root)"
  </read_first>
  <action>
    Add `pub mod hook;` to `src/lib.rs` between `pub mod git;` and `pub mod tui;` (alphabetical order). This is a single-line addition; do not touch the existing two lines.
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep -E 'test result|error' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits 0
    - All pre-existing test suites pass (no regressions)
    - `grep -c '^pub mod hook;' src/lib.rs` returns 1
    - `git diff src/lib.rs` shows exactly one line added
  </acceptance_criteria>
  <done>The hook module is reachable from the crate root; existing test suites continue to pass.</done>
</task>

</tasks>

<verification>
- `cargo check` and `cargo test` both pass.
- Public API signatures match the interfaces block exactly — Plans 02–05 depend on this contract.
- No new dependencies added to `Cargo.toml`.
- No preflight calls (`check_stash`, `check_worktrees`) anywhere in `src/hook/`.
</verification>

<success_criteria>
- Crate compiles cleanly with the hook module declared.
- All seven artifacts listed in `must_haves.artifacts` exist.
- `must_haves.truths` are observable: a quick `cargo check` confirms the API surface; `grep` on the listed files confirms no preflight contamination.
</success_criteria>

<output>
Create `.planning/phases/05-hook-engine/05-01-SUMMARY.md` when done.
</output>
