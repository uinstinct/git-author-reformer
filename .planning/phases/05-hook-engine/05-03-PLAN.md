---
phase: 05-hook-engine
plan: 03
type: tdd
wave: 2
depends_on: [05-01]
files_modified:
  - src/hook/render.rs
autonomous: true
requirements: [HOOK-07, HOOK-08]
must_haves:
  truths:
    - "`render_hook(&[email])` returns a String starting with `#!/bin/sh\\n` (LF, never CRLF)"
    - "Rendered hook contains the BEGIN_MARKER and END_MARKER from `parse.rs`, in order, on their own lines"
    - "Rendered hook lists each input email TWICE: once as `# <email>` comment between markers, once as `strip[\"<email>\"] = 1` inside the awk BEGIN block"
    - "All embedded emails are ASCII-lowercased before being written into both locations (HOOK-08 twin parity)"
    - "The awk filter body uses POSIX-only features (tolower, substr, length, sub, gsub) — no bash-isms, no GNU extensions"
    - "Email strings containing `\"`, `\\\\`, `\\n`, or `\\r` cause the renderer to reject the input (returns error or panics — TBD per task), preventing awk-string injection (RESEARCH §Security Domain)"
  artifacts:
    - path: "src/hook/render.rs"
      provides: "render_hook(emails) -> String + email sanitization"
      contains: "#!/bin/sh"
      min_lines: 70
  key_links:
    - from: "src/hook/render.rs"
      to: "src/hook/parse.rs::BEGIN_MARKER"
      via: "shared marker constants"
      pattern: "parse::BEGIN_MARKER"
---

<objective>
Implement `src/hook/render.rs` test-first: produce the full POSIX `sh` hook-file body from a list of strip emails. The rendered hook embeds the strip list twice — once as comment lines for the Rust parser, once as `awk` array entries for the runtime filter — keeping them in sync (single-input, dual-output template).

Purpose: This is where HOOK-07 (POSIX sh + 0755 markers in file) and HOOK-08 (twin-parser semantics) get materialized. The renderer is pure — no I/O. Plan 05 will run the produced shell script against fixture commit messages to prove end-to-end correctness against `src/git/reader.rs`.

Output: `src/hook/render.rs` with `pub(crate) fn render_hook(emails: &[String]) -> String` plus inline tests, and email-sanitization helper.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/references/tdd.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hook-engine/05-RESEARCH.md
@.planning/phases/05-hook-engine/05-PATTERNS.md
@src/git/rewrite.rs
@src/hook/render.rs

<interfaces>
<!-- Public API the renderer must satisfy. -->

From src/hook/parse.rs (Plan 02 — provides shared marker constants):
```rust
pub(crate) const BEGIN_MARKER: &str = "# >>> git-author-reformer auto-strip BEGIN >>>";
pub(crate) const END_MARKER:   &str = "# <<< git-author-reformer auto-strip END <<<";
```

Renderer API:
```rust
/// Render the full POSIX sh hook file body for a given strip list.
/// Emails are lowercased before embedding. Inputs containing shell-unsafe
/// characters cause a panic (only callable from `install_strip` which validates
/// upstream).
pub(crate) fn render_hook(emails: &[String]) -> String;

/// Returns Err if any character in the email would break the awk string literal
/// (i.e., `"`, `\\`, `\n`, `\r`). Used by `install_strip` for validation before
/// rendering.
pub(crate) fn validate_email_for_embedding(email: &str) -> Result<(), &'static str>;
```

Twin-parser reference (HOOK-08 — the awk script must mirror this Rust code semantically):
```rust
// src/git/reader.rs:84-92  — case-insensitive prefix match on "co-authored-by:"
// src/git/reader.rs:95-107 — LAST-< / LAST-> structural email extraction
// src/git/rewrite.rs:189-191 — eq_ignore_ascii_case email compare
```

Full expected hook-file template — RESEARCH §Code Examples "Hook file template" — must be reproduced verbatim by `render_hook`, with the strip list substituted into both locations (comment lines + awk BEGIN block).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing renderer tests</name>
  <files>src/hook/render.rs</files>
  <read_first>
    - src/hook/render.rs (Plan 01 stub)
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Code Examples "Hook file template", §Twin Parser Specification, §Pitfall 4, §Pitfall 5, §Security Domain
    - .planning/phases/05-hook-engine/05-PATTERNS.md "src/hook/render.rs"
    - src/git/reader.rs lines 84-107 (twin-parser source-of-truth — the rendered awk MUST be semantically equivalent)
  </read_first>
  <behavior>
    Add `#[cfg(test)] mod tests` to `src/hook/render.rs` with these failing tests (function does not exist yet — RED):

    Shape tests:
    - `render_hook_starts_with_posix_shebang_lf` — `render_hook(&["bob@example.com".into()])`.starts_with("#!/bin/sh\n"); rendered output contains NO `\r\n` substring (HOOK-07 + Pitfall §4).
    - `render_hook_contains_both_markers_in_order` — output contains `BEGIN_MARKER` AND `END_MARKER`, and the byte offset of BEGIN is less than that of END.
    - `render_hook_embeds_email_as_comment_line` — input `["bob@example.com"]`; output contains the substring `\n# bob@example.com\n` between BEGIN and END markers.
    - `render_hook_embeds_email_in_awk_strip_array` — same input; output contains the substring `strip["bob@example.com"] = 1`.
    - `render_hook_preserves_order_for_multiple_emails` — input `["zebra@x.com", "alpha@x.com"]`; both the comment list and the awk array list emit them in input order (no sorting).
    - `render_hook_emits_empty_marker_block_when_list_is_empty` — input `&[]`; BEGIN and END markers still present, but no `# <email>` lines between them and no `strip["..."]` lines inside the awk BEGIN block. (This output is never written to disk because `remove_strip` deletes the file when the list empties — but the renderer itself is pure and must handle the empty case for round-trip testing.)

    Lowercasing tests (HOOK-08 twin parity — RESEARCH §Pattern 3):
    - `render_hook_lowercases_emails_in_comments` — input `["BOB@Example.COM"]`; output contains `# bob@example.com` and does NOT contain `BOB@Example.COM`.
    - `render_hook_lowercases_emails_in_awk_array` — same input; output contains `strip["bob@example.com"] = 1` and does NOT contain `strip["BOB@Example.COM"]`.

    POSIX-portability tests (HOOK-07 + Pitfall §5):
    - `render_hook_contains_no_bash_isms` — output does NOT contain `[[`, `]]`, `${`, ` function `, `printf` (use of `${var}` is fine inside the awk `'...'` block since it's not shell-evaluated; we still grep-fail on `${` to be safe by ensuring the only awk uses are inside single-quoted blocks — implementation must keep them there).
    - `render_hook_uses_awk_tolower_for_case_fold` — output contains `tolower(` (proves case-fold happens in the awk script, mirroring `eq_ignore_ascii_case` in `src/git/rewrite.rs:190`).
    - `render_hook_uses_structural_email_extraction` — output contains substring searching for `<` and `>` characters via awk substr/loop logic (grep for `rfind`-equivalent — present as backwards `for` loop or `index(...)` — must contain a `for (` or comparable backward iteration over the line; assert by checking the output contains both `substr(` and a `for (` token inside the awk block).

    Validation tests (RESEARCH §Security Domain — shell-injection defense in depth):
    - `validate_email_for_embedding_rejects_double_quote` — `validate_email_for_embedding("bob\"; rm -rf / #@x.com")` returns `Err(_)`.
    - `validate_email_for_embedding_rejects_backslash` — `validate_email_for_embedding("bob\\\\@x.com")` returns `Err(_)`.
    - `validate_email_for_embedding_rejects_newline` — `validate_email_for_embedding("bob\\n@x.com")` returns `Err(_)`.
    - `validate_email_for_embedding_accepts_normal_email` — `validate_email_for_embedding("bob@example.com")` returns `Ok(())`.

    Round-trip with parser:
    - `render_then_parse_round_trips` — `let input = vec!["bob@example.com".into(), "carol@example.com".into()]; let rendered = render_hook(&input); let (b,e) = parse::detect_markers(&rendered).unwrap(); assert!(e > b); assert_eq!(parse::extract_strip_list(&rendered), input);` — proves parser+renderer agree on the comment-line representation.
  </behavior>
  <action>
    Replace the stub body of `src/hook/render.rs` with `#[cfg(test)] mod tests` containing the 14 tests above. Import `super::*` for the production symbols and `crate::hook::parse` for the round-trip test. Run `cargo test --lib hook::render` — must compile-fail or assert-fail (RED).

    Commit message: `test(05-03): add failing renderer tests for POSIX sh hook template`
  </action>
  <verify>
    <automated>cargo test --lib hook::render 2>&1 | grep -E 'error|FAILED|test result' | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` fails OR `cargo test --lib hook::render` produces errors referencing `render_hook` / `validate_email_for_embedding` not found
    - `grep -c '#\[test\]' src/hook/render.rs` returns 14
    - `grep -c 'render_hook_lowercases_emails_in_comments' src/hook/render.rs` returns 1
    - `grep -c 'render_then_parse_round_trips' src/hook/render.rs` returns 1
    - `grep -c 'validate_email_for_embedding_rejects_double_quote' src/hook/render.rs` returns 1
    - git log --oneline -1 starts with `test(05-03):`
  </acceptance_criteria>
  <done>14 failing tests committed. Renderer API contract is fully specified by tests. RED gate satisfied.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement renderer to pass tests</name>
  <files>src/hook/render.rs</files>
  <read_first>
    - src/hook/render.rs (after Task 1)
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Code Examples "Hook file template" (the verbatim template body — copy into a Rust `const` or `format!` literal)
    - .planning/phases/05-hook-engine/05-PATTERNS.md "src/hook/render.rs"
  </read_first>
  <behavior>
    Implement `render_hook` and `validate_email_for_embedding` so all 14 tests pass:

    - `pub(crate) fn validate_email_for_embedding(email: &str) -> Result<(), &'static str>` — return `Err("email contains forbidden character")` (exact message irrelevant per tests — only `is_err()` is asserted) if email contains any of `"`, `\\`, `\n`, `\r`. Return `Ok(())` otherwise. Empty-string validation belongs in `install_strip` (Plan 04), not here — RESEARCH §Security Domain mentions both checks but renderer only handles the embedding-safety subset.

    - `pub(crate) fn render_hook(emails: &[String]) -> String` —
      1. Lowercase each email via `.to_ascii_lowercase()` (preserving input order).
      2. Build the comment block: each email becomes a `# <email>\n` line between `BEGIN_MARKER\n` and `END_MARKER\n`.
      3. Build the awk strip array: each email becomes `  strip["<email>"] = 1\n` inside the awk `BEGIN { ... }` block.
      4. Assemble the full file using a `format!` literal with `\n` (never `\r\n`) — Pitfall §4.

      Use `crate::hook::parse::BEGIN_MARKER` and `crate::hook::parse::END_MARKER` — DO NOT redefine them locally (single source of truth).

      The full template MUST be byte-identical to RESEARCH §Code Examples "Hook file template" with the strip-list lines substituted. The awk body inside the single-quoted block must be reproduced verbatim from RESEARCH — that body has been hand-verified against the Rust source-of-truth (`src/git/reader.rs:84-107` + `src/git/rewrite.rs:189-191`).

    Run `cargo test --lib hook::render` and `cargo test --lib hook::parse` repeatedly until all tests in both modules pass.
  </behavior>
  <action>
    Implement the two functions per the behavior block. Use a single `format!` literal for the template — Karpathy Rule 2 (Simplicity First). Reuse marker constants from `crate::hook::parse`. Run the full suite until green.

    Commit message: `feat(05-03): implement POSIX sh hook renderer with twin awk filter`
  </action>
  <verify>
    <automated>cargo test --lib hook 2>&1 | grep 'test result'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib hook::render` reports `test result: ok. 14 passed; 0 failed`
    - `cargo test --lib hook::parse` still reports `test result: ok. 10 passed; 0 failed` (no regression in Plan 02)
    - `cargo test` (full suite) reports 0 failures
    - `grep -c 'pub(crate) fn render_hook' src/hook/render.rs` returns 1
    - `grep -c 'pub(crate) fn validate_email_for_embedding' src/hook/render.rs` returns 1
    - `grep -c '#!/bin/sh' src/hook/render.rs` >= 1 (shebang appears in the template literal)
    - `grep -c 'tolower(' src/hook/render.rs` >= 1 (awk case-fold present)
    - `grep -c 'crate::hook::parse::BEGIN_MARKER' src/hook/render.rs` >= 1 (marker constants reused, not redefined)
    - `grep -v '^#' src/hook/render.rs | grep -c '\\\\r\\\\n'` returns 0 (no CRLF in output template — Pitfall §4)
    - git log --oneline -1 starts with `feat(05-03):`
  </acceptance_criteria>
  <done>Renderer produces the full POSIX-sh hook template with embedded strip list in both representations, all 14 tests green, parser tests still passing.</done>
</task>

</tasks>

<verification>
- `cargo test --lib hook` reports 24/24 passing (10 parse + 14 render).
- Marker constants are NOT duplicated — `render.rs` imports from `parse.rs`.
- Output uses LF line endings exclusively.
- Awk filter body byte-matches RESEARCH §Code Examples — semantic twin of `src/git/reader.rs:84-107` and `src/git/rewrite.rs:189-191`.
</verification>

<success_criteria>
- All `must_haves.truths` are observable via the test suite or direct `grep` on the rendered output.
- `render.rs` is the single source of truth for the hook-file template.
- No new crate dependencies.
</success_criteria>

<output>
Create `.planning/phases/05-hook-engine/05-03-SUMMARY.md` when done.
</output>
