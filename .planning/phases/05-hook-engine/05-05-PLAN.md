---
phase: 05-hook-engine
plan: 05
type: tdd
wave: 4
depends_on: [05-04]
files_modified:
  - tests/common/mod.rs
  - tests/hook_test.rs
autonomous: true
requirements: [HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-10, HOOK-12, HOOK-13]
must_haves:
  truths:
    - "`cargo test --test hook_test` runs at least 12 distinct integration tests, all passing"
    - "Fresh install on a repo with no hook creates `.git/hooks/commit-msg` with mode 0755 and embedded marker block"
    - "Install on a tool-managed hook appends an email and rewrites; install on a duplicate is a no-op (file bytes identical)"
    - "Install on a non-tool-managed hook returns `Err(AppError::HookExists(_))` and the existing file is unchanged"
    - "Generated hook script, when run with `/bin/sh`, strips matching `Co-authored-by:` lines case-insensitively (HOOK-08)"
    - "Twin-parity counterexample: hook PRESERVES a line where the target email appears in the name slot but not the email slot (RESEARCH Pitfall §1)"
    - "Removing the last entry deletes the file entirely; removing a non-last entry rewrites with that email gone"
    - "Hook engine ops succeed on a repo that has a stash entry (HOOK-12 regression)"
    - "Round-trip: `render_hook` output parsed by `read_strip_list` yields the same email list"
  artifacts:
    - path: "tests/common/mod.rs"
      provides: "run_hook_on_message(&Path, &str) -> String helper"
      contains: "run_hook_on_message"
    - path: "tests/hook_test.rs"
      provides: "Integration test suite for the hook engine"
      contains: "test_install_fresh_writes_file_with_markers_and_email"
      min_lines: 200
  key_links:
    - from: "tests/hook_test.rs"
      to: "git_author_reformer::hook"
      via: "public API imports"
      pattern: "use git_author_reformer::hook"
    - from: "tests/common/mod.rs::run_hook_on_message"
      to: "/bin/sh"
      via: "std::process::Command"
      pattern: "Command::new\\(\"/bin/sh\"\\)"
---

<objective>
Cover HOOK-13's full test matrix with a TDD-driven integration test suite at `tests/hook_test.rs`. Extend `tests/common/mod.rs` with the shell-execution helper that proves HOOK-08 (twin parser semantics in the generated awk script) against the existing Rust drop flow.

Purpose: HOOK-13 is the meta-requirement that the test suite exists. Each test row in RESEARCH §Validation Architecture "Phase Requirements → Test Map" becomes one `#[test]`. The twin-parity counterexample test (where the target email appears in the name slot of one trailer and the email slot of another on the same line) is the load-bearing assertion that the awk implementation actually mirrors `src/git/reader.rs:84-107`.

Output: `tests/hook_test.rs` with 12+ passing integration tests; `tests/common/mod.rs` extended with `run_hook_on_message`.
</objective>

<execution_context>
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/references/tdd.md
@/Users/instinct/Desktop/working/git-author-reformer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-hook-engine/05-RESEARCH.md
@.planning/phases/05-hook-engine/05-PATTERNS.md
@tests/common/mod.rs
@tests/preflight_test.rs
@tests/rewrite_test.rs
@tests/main_integration_test.rs
@src/hook/mod.rs

<interfaces>
<!-- Public API under test — finalized in Plan 04. -->

From src/hook/mod.rs (Plan 04):
```rust
pub enum HookState { Absent, Managed { emails: Vec<String> }, NotToolManaged(std::path::PathBuf) }
pub enum AddResult { Installed { count: usize }, AlreadyStripped }
pub enum RemoveResult { Updated { remaining: usize }, HookDeleted, NotFound }
pub fn install_strip(repo: &git2::Repository, email: &str) -> Result<AddResult, AppError>;
pub fn remove_strip(repo: &git2::Repository, email: &str) -> Result<RemoveResult, AppError>;
pub fn read_strip_list(repo: &git2::Repository) -> Result<HookState, AppError>;
```

Existing test helpers in tests/common/mod.rs (style to match — `.unwrap()`, `pub fn`, no `pub(crate)`):
```rust
pub fn create_fixture_repo() -> (TempDir, Repository);
pub fn add_commit_with_message(repo: &Repository, name: &str, email: &str, msg: &str);
```

Helper to add (per RESEARCH §Code Examples "Test pattern"):
```rust
pub fn run_hook_on_message(hook_path: &Path, input_msg: &str) -> String;
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing integration tests + add shell-out helper</name>
  <files>tests/hook_test.rs, tests/common/mod.rs</files>
  <read_first>
    - tests/common/mod.rs (lines 1-66 — existing helpers, `.unwrap()` style)
    - tests/preflight_test.rs (lines 1-65 — `mod common;` + `matches!(...)` assertion idiom)
    - tests/rewrite_test.rs (lines 333-505 — case-insensitive drop test fixtures)
    - tests/main_integration_test.rs (lines 3-25 — `std::process::Command` shell-out pattern)
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Validation Architecture "Phase Requirements → Test Map" + §Twin Parser Specification (counterexample) + §Code Examples "Test pattern"
    - .planning/phases/05-hook-engine/05-PATTERNS.md "tests/hook_test.rs" + "tests/common/mod.rs"
  </read_first>
  <behavior>
    First, extend `tests/common/mod.rs` with one new helper (matches existing style — `pub fn`, `.unwrap()`):

    ```rust
    /// Runs the generated commit-msg hook against `input_msg` and returns the
    /// resulting filtered message. Verifies HOOK-08 by exercising the actual
    /// shell script. Asserts the script exits 0.
    pub fn run_hook_on_message(hook_path: &std::path::Path, input_msg: &str) -> String;
    ```

    Implementation per RESEARCH §Code Examples "Test pattern": write `input_msg` to a `TempDir`-relative `MSG` file, invoke `/bin/sh hook_path MSG` via `std::process::Command::new("/bin/sh")`, `.arg(hook_path).arg(&msg_file).status()`, assert success, return `fs::read_to_string(&msg_file).unwrap()`.

    Then create `tests/hook_test.rs` with these 12 failing tests (engine functions exist but writer/glue may not yet — but since this plan runs AFTER 05-04, they exist; tests will fail on the BEHAVIOR side first if Plan 04's logic has bugs). All tests follow `tests/preflight_test.rs`'s style — `mod common;` at top, `use git_author_reformer::...`, one `#[test] fn` per behavior, with a doc comment naming the HOOK-XX requirement.

    Tests (exact names — must match RESEARCH §Validation Architecture row labels for traceability):

    1. `test_install_fresh_writes_file_with_markers_and_email` (HOOK-04) — fresh fixture repo, `install_strip(&repo, "bob@example.com")?` returns `AddResult::Installed { count: 1 }`. `read_strip_list(&repo)?` returns `HookState::Managed { emails: vec!["bob@example.com".into()] }`. Hook file at `repo.path().join("hooks/commit-msg")` exists and contains BOTH `BEGIN >>>` and `END <<<` marker substrings.

    2. `test_install_appends_to_existing_tool_managed_hook` (HOOK-04) — install bob, then install carol. After both: `read_strip_list` returns `Managed { emails: vec!["bob@example.com", "carol@example.com"] }`. The second `install_strip` returned `AddResult::Installed { count: 2 }`.

    3. `test_install_duplicate_email_is_noop_file_bytes_identical` (HOOK-05) — install bob; capture `fs::read(&hook_path).unwrap()` bytes. Install bob again — assert the return is `AddResult::AlreadyStripped` AND the post-call bytes equal the pre-call bytes byte-for-byte (`Vec<u8>` equality). Also test mixed case: installing `BOB@EXAMPLE.COM` after `bob@example.com` is also `AlreadyStripped` (engine lowercases internally per Plan 04 behavior).

    4. `test_install_refuses_to_overwrite_non_tool_managed_hook` (HOOK-06) — pre-populate `repo.path().join("hooks/commit-msg")` with `"#!/bin/sh\necho user hook\n"` (chmod 0755). Capture pre-bytes. Call `install_strip(&repo, "bob@example.com")` — assert `matches!(result, Err(AppError::HookExists(_)))` AND the file's bytes are unchanged AND its mode is unchanged. The error's path payload must equal the hook path.

    5. `test_install_sets_mode_0755_on_unix` (HOOK-07) — `#[cfg(unix)]`-gated. Fresh install. Assert `fs::metadata(&hook_path).unwrap().permissions().mode() & 0o777 == 0o755`.

    6. `test_generated_hook_has_posix_shebang_and_markers` (HOOK-07) — fresh install. Read hook contents as `String`. Assert it starts with `"#!/bin/sh\n"`. Assert it contains both marker substrings. Assert it contains NO `\r\n` byte sequence (Pitfall §4).

    7. `test_shell_hook_strips_case_insensitive_matches` (HOOK-08) — install `bob@example.com`. Run `common::run_hook_on_message` on this fixture message:
       ```
       feat: thing\n\nCo-Authored-By: Bob <BOB@EXAMPLE.COM>\nco-authored-by: Carol <carol@example.com>\nCO-AUTHORED-BY: Bob Two <bob@example.com>\n
       ```
       Assert the result does NOT contain `bob@example.com` (any case). Assert the result still contains `carol@example.com`. Assert the result still contains the `feat: thing` subject line.

    8. `test_shell_hook_preserves_when_email_only_in_name_slot` (HOOK-08, Pitfall §1 counterexample — load-bearing) — install `bob@example.com`. Run the hook on:
       ```
       feat: thing\n\nCo-authored-by: bob@example.com <alice@example.com>\n
       ```
       Assert the result STILL contains the `Co-authored-by:` line (the target email appears only in the name slot; structural extraction returns `alice@example.com`, not `bob@example.com`). This is the test that fails under a naive `grep -i bob@example.com` implementation.

    9. `test_remove_single_entry_rewrites_file` (HOOK-10) — install bob and carol. `remove_strip(&repo, "bob@example.com")?` returns `RemoveResult::Updated { remaining: 1 }`. Hook file still exists. `read_strip_list` returns `Managed { emails: vec!["carol@example.com"] }`.

    10. `test_remove_last_entry_deletes_file` (HOOK-10) — install bob. `remove_strip(&repo, "bob@example.com")?` returns `RemoveResult::HookDeleted`. `repo.path().join("hooks/commit-msg").exists()` is `false`. `read_strip_list` returns `HookState::Absent`.

    11. `test_install_does_not_trigger_preflight_with_stash_present` (HOOK-12) — set up a fixture repo (`common::create_fixture_repo`), then create a stash by writing a file and calling `repo.stash_save(...)` (use the same pattern as `tests/preflight_test.rs` — match its stash-creation helper or call git2's stash API directly). Confirm `refs/stash` exists via `repo.find_reference("refs/stash").is_ok()`. Then call `install_strip(&repo, "bob@example.com")` — assert `Ok(AddResult::Installed { count: 1 })`. This proves the engine itself does not call `check_stash`. (Worktree creation is harder in a test fixture; rely on the `grep` acceptance check in Plan 04 for the worktree leg of HOOK-12.)

    12. `test_read_strip_list_round_trips_through_render` (HOOK-13) — install bob, then carol, then dave. Read the raw file contents, parse via `read_strip_list`, assert `Managed { emails: vec!["bob@example.com", "carol@example.com", "dave@example.com"] }`. Also assert `parse::extract_strip_list` directly on the rendered string (via `render_hook` from `crate::hook::render` — make the call public-in-test via `pub(crate)` already exists; if not accessible from integration tests, drop this leg and rely on the parse round-trip in Plan 02's unit tests). This proves the file format survives a full write→read cycle on disk.

    For the stash-helper: if `tests/preflight_test.rs` already exposes a stash-creating helper via `common::`, reuse it; otherwise add a private helper inside `tests/hook_test.rs` (do NOT pollute `tests/common/mod.rs` with hook-specific stash setup).

    Run `cargo test --test hook_test` — every test should compile (since the engine exists from Plan 04) but at least one must fail on first run if Plan 04 has any bug. If all pass on first attempt, the RED gate degrades to a "tests added + passing immediately" commit — still acceptable per the TDD reference doc when the implementation precedes the integration tests (Plan 04 was implementation; Plan 05 is integration). Commit either way as the RED stage.
  </behavior>
  <action>
    1. Open `tests/common/mod.rs`; add the `run_hook_on_message` helper after the existing helpers, matching the file's `.unwrap()` style and `pub fn` visibility (no `pub(crate)`).
    2. Create `tests/hook_test.rs` with `mod common;` at the top, public-API imports from `git_author_reformer::hook` and `git_author_reformer::error::AppError`, and the 12 `#[test]` functions described above.
    3. Use `common::create_fixture_repo()` for repo setup in every test that needs a repo. For tests that need a tempdir for fixture content only, use `tempfile::TempDir::new().unwrap()` directly (matches existing convention).
    4. Run `cargo test --test hook_test -- --nocapture` — record which tests pass/fail. Commit at this point regardless of pass/fail outcome.

    Commit message: `test(05-05): add integration tests for hook engine (HOOK-04/05/06/07/08/10/12/13)`
  </action>
  <verify>
    <automated>cargo test --test hook_test 2>&1 | grep -E 'test result|FAILED' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/hook_test.rs` exists
    - `grep -c '#\[test\]' tests/hook_test.rs` >= 12
    - `grep -c 'test_shell_hook_preserves_when_email_only_in_name_slot' tests/hook_test.rs` returns 1 (load-bearing counterexample test present)
    - `grep -c 'test_install_does_not_trigger_preflight_with_stash_present' tests/hook_test.rs` returns 1
    - `grep -c 'test_install_duplicate_email_is_noop_file_bytes_identical' tests/hook_test.rs` returns 1
    - `grep -c 'pub fn run_hook_on_message' tests/common/mod.rs` returns 1
    - `cargo test --test hook_test` either reports failures (RED gate) OR reports `12 passed; 0 failed` (acceptable per TDD reference doc when implementation precedes integration tests)
    - `cargo build --tests` exits 0 (all tests compile)
    - git log --oneline -1 starts with `test(05-05):`
  </acceptance_criteria>
  <done>Integration tests written, compile, and exercise every code path in HOOK-04/05/06/07/08/10/12/13. The twin-parity counterexample test is present.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — fix any test failures + final phase verification</name>
  <files>
    src/hook/mod.rs,
    src/hook/parse.rs,
    src/hook/render.rs,
    src/hook/write.rs,
    tests/hook_test.rs
  </files>
  <read_first>
    - Output of `cargo test --test hook_test` from Task 1
    - The specific source files that any failing test points to
    - .planning/phases/05-hook-engine/05-RESEARCH.md §Common Pitfalls (in case a failure is a known pitfall)
  </read_first>
  <behavior>
    If Task 1's `cargo test --test hook_test` reported failures, fix the root cause in `src/hook/` (NOT by weakening test assertions). Likely failure modes per the pitfalls section:

    - HOOK-08 twin-parity test (#8) failing → the awk script is doing substring match instead of structural extraction. Fix the awk body in `src/hook/render.rs` to match RESEARCH §Twin Parser Specification verbatim.
    - HOOK-07 mode test (#5) failing on non-existent file → `set_permissions` ordering bug in `src/hook/write.rs` (Pitfall §3 — permissions must be set BEFORE rename, not after).
    - HOOK-05 byte-equality test (#3) failing → `install_strip` is calling the writer even on duplicate; remove that write path.
    - HOOK-06 refuse-to-overwrite (#4) failing → `install_strip` is overwriting non-tool-managed files; check that `HookState::NotToolManaged` is short-circuiting before the write.
    - CRLF in hook output (#6) → the `format!` literal in `src/hook/render.rs` has a `\r\n` somewhere; replace with `\n`.

    For each failure, FIRST verify the test assertion is correct against the requirement (RESEARCH §Validation Architecture row), THEN fix the source — Karpathy Rule 8: tests verify intent, not just behavior; if a test assertion is wrong, surface that as a conflict per Rule 6, do not silently rewrite.

    If Task 1 already showed 12/12 passing, this task collapses to running the full `cargo test` + `cargo clippy --lib -- -D warnings` + `cargo fmt --check` gate and committing a "verify phase complete" marker. In either case, the final state must satisfy all 12 tests + full suite green.

    Karpathy Rule 9: after this task, no stale branches or worktrees from Phase 5 should remain.
  </behavior>
  <action>
    1. Run `cargo test --test hook_test -- --nocapture` and identify failing tests.
    2. For each failure, locate the root cause per the pitfalls heuristic above, fix in `src/hook/*.rs`, and re-run until 12/12 pass.
    3. Run `cargo test` (full suite) — must report 0 failures across all integration test files (`hook_test`, `preflight_test`, `rewrite_test`, `scan_test`, `reader_test`, `main_integration_test`).
    4. Run `cargo clippy --lib --tests -- -D warnings` — must exit 0.
    5. Run `cargo fmt --check` — must exit 0.

    Commit message: `feat(05-05): green hook-engine integration tests (HOOK-04/05/06/07/08/10/12/13)`
  </action>
  <verify>
    <automated>cargo test 2>&1 | grep 'test result' && cargo clippy --lib --tests -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test hook_test` reports `test result: ok. 12 passed; 0 failed` (or more if extra tests were added)
    - `cargo test` (full suite) reports 0 failures across all integration test files
    - `cargo clippy --lib --tests -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - All HOOK requirements traceable: every HOOK-XX in `[HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-10, HOOK-12, HOOK-13]` has at least one `#[test]` in `tests/hook_test.rs` whose doc comment cites it (`grep -c 'HOOK-04' tests/hook_test.rs >= 1`, same for the others)
    - No stale git branches or worktrees from this phase (per Karpathy Rule 9 — `git branch -a` shows only `main`)
    - git log --oneline -1 starts with `feat(05-05):`
  </acceptance_criteria>
  <done>All 12 integration tests pass. Full suite is green. Clippy and fmt gates pass. Every Phase 5 HOOK requirement has at least one passing test. Phase 5 is ready for `/gsd:verify-work`.</done>
</task>

</tasks>

<verification>
- `cargo test` reports 0 failures across every integration test file.
- `cargo clippy --lib --tests -- -D warnings` and `cargo fmt --check` both clean.
- All HOOK requirements in the frontmatter are exercised by at least one test in `tests/hook_test.rs`.
- The Pitfall §1 counterexample test (`test_shell_hook_preserves_when_email_only_in_name_slot`) passes — proves the awk script structurally mirrors `src/git/reader.rs:84-107`.
</verification>

<success_criteria>
- HOOK-13 fully satisfied: every phase-requirement code path has automated coverage.
- Phase 5 is shippable: a Phase 6 implementer can call the public hook API with confidence that the underlying behavior matches the requirements.
</success_criteria>

<output>
Create `.planning/phases/05-hook-engine/05-05-SUMMARY.md` when done.
</output>
