# Milestone v1.1 Archive: Auto-Strip Co-Author Hook

**Milestone:** v1.1
**Shipped:** 2026-05-21
**Goal:** Extend the TUI with two additional main-menu flows — install a `commit-msg` git hook that automatically strips selected co-authors from every future commit in the repo, and manage (view/remove) the configured strip entries.
**Requirements set:** HOOK-01..HOOK-14 (14 requirements, 8 in Phase 5, 6 in Phase 6)
**Phases:** Phase 5 (Hook Engine) + Phase 6 (Hook TUI Integration)

---

## Accomplishments

- Added a pure-Rust hook engine (`src/hook/`) that owns the `commit-msg` hook file end-to-end: parse, serialize, ownership detection, idempotent install/extend/remove, and a POSIX `sh` hook template with an `awk` twin-parser matching the existing Rust drop flow's case-insensitive `Co-authored-by:` semantics
- Wired two new main-menu flows — "Add co-author auto-strip hook" and "Manage auto-strip hook" — into the TUI state machine on a code path that bypasses the SAFE-01/SAFE-02 preflight gates (hook operations do not rewrite history)
- Enforced refuse-to-overwrite safety: any pre-existing non-tool-written `commit-msg` hook causes the install flow to abort with a clear error naming the file and instructing the user to remove or rename it
- Ship: the hook file is self-contained (strip list embedded between distinctive marker comments between `#!/bin/sh` header and filter body) — no extra config files, survives backup/restore of `.git/hooks/`
- Closed a verification gap (SC4) by adding a dedicated `Screen::HookRemoved` variant that renders "Hook removed — no entries remain." distinct from the never-installed empty state ("No hook installed — no emails configured.")
- Fixed a shell-injection security finding (CR-01): `validate_email_for_embedding` now rejects the single-quote character (`'`), closing an awk string injection vector; defense-in-depth re-validation of emails read back from disk was added to `install_strip` and `remove_strip`

---

## Phase 5: Hook Engine

**Goal:** Pure-Rust module that owns the `commit-msg` hook file end-to-end — read, parse, serialize, install, extend, remove — with no TUI dependencies
**Depends on:** Phase 2 (reuses the case-insensitive `Co-authored-by:` matching semantics from the drop flow; same parser code path embedded in the hook script's filter logic)
**Requirements:** HOOK-04, HOOK-05, HOOK-06, HOOK-07, HOOK-08, HOOK-10, HOOK-12, HOOK-13

**Success Criteria** (what must be TRUE):
1. Installing a strip entry on a repo with no existing `commit-msg` hook writes a POSIX `sh` script (shebang `#!/bin/sh`) at `.git/hooks/commit-msg` with mode 0755 and the email listed between marker comments
2. Installing a strip entry on a repo whose `commit-msg` hook already carries the tool's marker comment appends the new email to the embedded list and rewrites the file; installing an email already in the list is reported as "already stripped" and the file's bytes are unchanged
3. Installing a strip entry on a repo whose `commit-msg` hook exists but lacks the tool's marker comment returns a refuse-to-overwrite error naming the file and instructing the user to remove or rename it — no file is written
4. Removing the last entry from a tool-managed hook deletes the `.git/hooks/commit-msg` file entirely (no empty marker-only file is left behind)
5. Executing the generated hook against a sample commit message strips lines matching `Co-authored-by:` case-insensitively for any email in the embedded list, using the same matching semantics as the existing drop flow (verified by running the script with `sh` on fixture inputs)
6. Calling any hook engine operation does not invoke the SAFE-01/SAFE-02 preflight blockers — a repo with stash entries or linked worktrees can still have hooks installed or managed
7. Automated Rust tests cover every engine code path: fresh install, append-to-existing, no-op duplicate, refuse-to-overwrite, parse tool-managed hook back into strip list, remove single entry, remove last entry (file deleted), mode 0755 verified on Unix, and shell-script execution against fixture commit messages — `cargo test` exercises each path with a dedicated test

**Plans:** 5 plans (all complete)
- [x] 05-01-PLAN.md — Scaffold `src/hook/` module skeleton + `AppError::HookExists` + `lib.rs` wiring
- [x] 05-02-PLAN.md — TDD parser: marker-pair detection + strip-list extraction (`src/hook/parse.rs`)
- [x] 05-03-PLAN.md — TDD renderer: POSIX sh hook template + twin awk filter + email sanitization (`src/hook/render.rs`)
- [x] 05-04-PLAN.md — Atomic writer + public `install_strip`/`remove_strip`/`read_strip_list` API (`src/hook/write.rs`, `src/hook/mod.rs`)
- [x] 05-05-PLAN.md — Integration tests for HOOK-04/05/06/07/08/10/12/13 (`tests/hook_test.rs` + `run_hook_on_message` helper)

**Key constraints:**
- The hook's runtime filter (the `sh` script that strips lines) must use the SAME case-insensitive `Co-authored-by:` matching semantics as the Rust drop flow (HOOK-08). The Rust drop parser is the source of truth; the shell filter is its faithful POSIX reimplementation. Twin-source explicitly documented in render.rs comment.
- The Rust side writes a fixed shell script template with the strip list embedded between two distinctive marker comments. The script itself contains no Rust — it is plain POSIX `sh` using `grep -i` / `awk` to filter `Co-authored-by:` lines whose emails match the embedded list.
- Hook engine entry points must NOT call the existing preflight gates (`check_stash`, `check_worktrees`); installing a hook is a non-history-rewriting operation (HOOK-12). The TUI in Phase 6 routes hook flows around the preflight.
- File mode 0755 must be set on Unix; on Windows (not a v1 target) this is a no-op. The Rust file write path uses `std::os::unix::fs::PermissionsExt`.

**Verification status:** PASSED (7/7 truths verified). One `human_needed` item at initial verification — resolved by manual smoke test confirming `git commit` itself invokes the hook, git discovers `.git/hooks/commit-msg`, honors mode 0755, and passes the temp file as `$1`.

---

## Phase 6: Hook TUI Integration

**Goal:** Two new main-menu flows (Add, Manage) wired to the hook engine, with fuzzy-filterable selectors and success screens
**Depends on:** Phase 5
**Requirements:** HOOK-01, HOOK-02, HOOK-03, HOOK-09, HOOK-11, HOOK-14

**Success Criteria** (what must be TRUE):
1. Launching the tool presents a four-option main menu — "Rename an author", "Drop a co-author", "Add co-author auto-strip hook", "Manage auto-strip hook" — and responds to keyboard navigation
2. The "Manage auto-strip hook" option is always visible and selectable, even when no hook is installed; in that empty state it shows a clear "no entries configured" screen
3. Picking "Add" displays the currently-configured strip list (or "no entries yet"), then a fuzzy-filterable co-author selector reusing the same enumeration as the existing drop flow; selecting an entry hands off to the hook engine and lands on a success screen showing the resulting strip-list state
4. Picking "Manage" displays a fuzzy-filterable list of configured strip emails; selecting an entry removes it via the hook engine and lands on a success screen showing the resulting strip-list state (or "hook removed — no entries remain" when the last entry was removed)
5. Neither Add nor Manage triggers the stash/worktree pre-flight blockers — both flows reach their selectors on a repo with stash entries
6. Automated TUI/state-machine tests cover every user path: main menu routes each of the four options, Add happy path → success screen, Add duplicate → already-stripped screen, Manage empty state, Manage remove single → updated list, Manage remove last → "hook removed" screen, and a regression test verifies Add/Manage on a repo with stash entries does NOT hit the SAFE-01/SAFE-02 preflight

**Plans:** 6 plans (all complete)
- [x] 06-01-PLAN.md — TDD: Move preflight from `main.rs` into `event.rs` Rename/Drop branches (HOOK-12 structural fix)
- [x] 06-02-PLAN.md — Extend `MenuChoice` (4 options), add 4 `Screen` variants + stubs, fix modulus (HOOK-01, HOOK-02)
- [x] 06-03-PLAN.md — TDD: Add flow — `HookAddList`, `install_strip` wiring, `HookSuccess`, `HookAlreadyStripped` (HOOK-03, HOOK-11)
- [x] 06-04-PLAN.md — TDD: Manage flow — `HookManageList`, `remove_strip` wiring, empty state (HOOK-02, HOOK-09, HOOK-11)
- [x] 06-05-PLAN.md — HOOK-14 stash-bypass tests + final phase gate (clippy/fmt) (HOOK-14)
- [x] 06-06-PLAN.md — Gap closure SC4: add `Screen::HookRemoved` for distinct post-removal state (HOOK-09)

**Key constraints:**
- The co-author enumeration in the Add flow must reuse the existing `enumerate_coauthors` from Phase 1, not a parallel implementation (HOOK-03).
- The Add and Manage flows must dispatch to the hook engine on a code path that bypasses the SAFE-01/SAFE-02 preflight (HOOK-12). Plan 06-01 moved preflight calls from `src/main.rs` startup into the Rename and Drop branches of the MainMenu Enter handler in `src/tui/event.rs`.
- Success screens for both flows render the final strip-list state from the hook engine, not from a cached TUI value — the engine is the source of truth (HOOK-11).

**Verification status:** PASSED (6/6 truths verified). Initial verification was 5/6 (gap SC4: `HookDeleted` routed to `HookSuccess(Absent)` instead of a distinct screen). Closed by plan 06-06. Re-verification confirmed 6/6 PASSED, 153 tests pass.

---

## Stats

| Metric | Value |
|--------|-------|
| Commits in milestone | 50 |
| Tests at milestone start | 88 |
| Tests at milestone end | 153 |
| Net new tests | 65 |
| New source files | `src/hook/mod.rs`, `src/hook/parse.rs`, `src/hook/path.rs`, `src/hook/render.rs`, `src/hook/write.rs`, `tests/hook_test.rs` |
| Modified source files | `src/lib.rs`, `src/error.rs`, `src/main.rs`, `src/tui/app.rs`, `src/tui/event.rs`, `src/tui/render.rs`, `tests/common/mod.rs`, `tests/main_integration_test.rs` |
| Total files touched | 14 (6 new + 8 modified) |
| Plans executed | 11 (5 in Phase 5 + 6 in Phase 6) |

---

## Known Tradeoffs (Info-Level Findings)

### Phase 5 Info Findings

| ID | File | Finding | Resolution |
|----|------|---------|------------|
| IN-01 | `src/hook/write.rs:38-40` | `delete_hook` does not guard against symlinks — `fs::remove_file` removes the symlink itself, not the target | Accepted: threat model does not include adversarial filesystem state in v1.1; low practical risk given this tool's scope |
| IN-02 | `src/hook/parse.rs:14-21` | Marker collision if a user's pre-existing hook contains the exact `BEGIN_MARKER` string | Accepted: the two-marker requirement (both BEGIN+END must be present) significantly reduces collision surface; documented in code |

### Phase 6 Info Findings

| ID | File | Finding | Resolution |
|----|------|---------|------------|
| IN-01 | `src/tui/render.rs:516-518` | `render_hook_success` `NotToolManaged` fallback emits "Error: foreign hook (should not reach this screen)." — inconsistent with the actionable entry-handler message | Accepted: this branch is unreachable in practice (entry handlers filter `NotToolManaged` to `Screen::Err` first); informational only |
| IN-02 | `src/tui/event.rs:424-429` | `HookSuccess` with `Absent` (Manage empty state) exits on any key; `HookAlreadyStripped` returns to menu — asymmetric UX for no-op outcomes | Accepted: minor UX inconsistency; user can relaunch if they want to navigate elsewhere after checking Manage on a repo with no hook |

---

## Process Notes

### TDD Discipline

- **Phase 5:** Plans 05-02, 05-03, 05-04 had clean RED→GREEN cadence. Plan 05-01 was a scaffold plan (TDD not applicable). Plan 05-05 had a degenerate RED phase — Plan 04 had already implemented the engine before Plan 05 wrote integration tests; all 12 tests passed on first run. Advisory only; no code defect.
- **Phase 6:** Plans 06-01, 06-03, 06-04, 06-06 had clean RED→GREEN cadence. Plan 06-02 was a scaffold plan (TDD N/A). Plan 06-05 was regression tests only.

### Surgical-Changes Audit

- Phase 5: No violations found in any plan.
- Phase 6: 5 of 6 plans were exact-match clean. Plan 06-01 had an INFO finding — `tests/main_integration_test.rs` was also modified to update test names reflecting the new startup behavior (preflight moved from main.rs to event.rs). This was required to keep the test suite passing and was documented with sound rationale in the SUMMARY.

### Gap Closure History

- Phase 5 initial verification: `human_needed` status due to one item requiring real-world `git commit` invocation to confirm end-to-end hook discovery. Resolved by manual smoke test confirming the hook fires correctly.
- Phase 6 initial verification: 5/6 truths verified. Gap SC4: `RemoveResult::HookDeleted` routed to `Screen::HookSuccess { state: Absent }`, making the post-removal state indistinguishable from the never-installed state. Closed by plan 06-06 (RED commit `22ec053`, GREEN commit `4ed83da`). Re-verification: 6/6 PASSED.

---

*Archived: 2026-05-21*
